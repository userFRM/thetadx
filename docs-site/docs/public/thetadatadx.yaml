openapi: 3.1.0

info:
  title: ThetaDataDx SDK
  description: |
    Canonical checked-in REST contract for the registry endpoints exposed by
    ThetaDataDx's server surface. SDK-only callback-based `*_stream` history
    builders are documented in the API reference rather than this OpenAPI file.

    ThetaDataDx also ships native Rust, Python (PyO3), and C++
    bindings backed by the same Rust core.

    All dates use YYYYMMDD format. Times use milliseconds since midnight ET.
    Intervals are one of the presets: tick, 10ms, 100ms, 500ms, 1s, 5s, 10s, 15s, 30s, 1m, 5m, 10m, 15m, 30m, 1h.
    All prices are f64 (double) -- decoded during parsing.
  version: 0.4.0
  contact:
    name: ThetaDataDx
    url: https://github.com/userFRM/ThetaDataDx
  license:
    name: Apache-2.0
    url: https://github.com/userFRM/ThetaDataDx/blob/main/LICENSE

servers:
  - url: http://localhost:25503
    description: Local ThetaDataDx server (HTTP)

tags:
  - name: Stock / List
    description: Enumerate available stock symbols and dates
  - name: Stock / Snapshot
    description: Real-time stock snapshots (OHLC, trade, quote, market value)
  - name: Stock / History
    description: Historical stock data (EOD, intraday OHLC, trades, quotes)
  - name: Stock / AtTime
    description: Stock data at a specific time of day across a date range
  - name: Option / List
    description: Enumerate option symbols, dates, expirations, strikes, and contracts
  - name: Option / Snapshot
    description: Real-time option snapshots (OHLC, trade, quote, OI, market value)
  - name: Option / Snapshot Greeks
    description: Real-time option Greeks snapshots (IV, all, first/second/third order)
  - name: Option / History
    description: Historical option data (EOD, intraday OHLC, trades, quotes, OI)
  - name: Option / History Greeks
    description: Historical option Greeks (EOD, intraday, IV, first/second/third order)
  - name: Option / History Trade Greeks
    description: Greeks computed on each individual option trade
  - name: Option / AtTime
    description: Option data at a specific time of day across a date range
  - name: Index / List
    description: Enumerate available index symbols and dates
  - name: Index / Snapshot
    description: Real-time index snapshots (OHLC, price, market value)
  - name: Index / History
    description: Historical index data (EOD, intraday OHLC, price)
  - name: Index / AtTime
    description: Index data at a specific time of day across a date range
  - name: Calendar
    description: Market calendar operations (open today, date info, yearly calendar)
  - name: Interest Rate
    description: Interest rate data endpoints
  - name: System
    description: Server status and lifecycle endpoints
  - name: Flat Files
    description: Whole-universe daily flat-file downloads (CSV or JSON Lines)

# No security requirement: the server authenticates its upstream connection
# once at startup, and request paths carry no per-request credential — matching
# the terminal, whose local routes (including shutdown) are all unauthenticated.

# ---------------------------------------------------------------------------
# Reusable anchors for common parameter/schema objects
# ---------------------------------------------------------------------------
x-anchors:
  # --- Common parameter shapes ---
  symbol-param: &symbol-param
    name: symbol
    in: query
    required: true
    schema:
      type: string
    description: Ticker symbol (e.g. "AAPL")

  symbols-param: &symbols-param
    name: symbol
    in: query
    required: true
    schema:
      type: array
      items:
        type: string
    description: One or more ticker symbols

  start_date-param: &start_date-param
    name: start_date
    in: query
    required: true
    schema:
      type: string
      pattern: '^\d{8}$'
    description: Start date in YYYYMMDD format

  end_date-param: &end_date-param
    name: end_date
    in: query
    required: true
    schema:
      type: string
      pattern: '^\d{8}$'
    description: End date in YYYYMMDD format

  date-param: &date-param
    name: date
    in: query
    required: true
    schema:
      type: string
      pattern: '^\d{8}$'
    description: Date in YYYYMMDD format

  date-optional-param: &date-optional-param
    name: date
    in: query
    required: false
    schema:
      type: string
      pattern: '^\d{8}$'
    description: Date in YYYYMMDD format (optional when using start_date/end_date)

  start_date-optional-param: &start_date-optional-param
    name: start_date
    in: query
    required: false
    schema:
      type: string
      pattern: '^\d{8}$'
    description: Range start date in YYYYMMDD format (optional; pair with end_date instead of date)

  end_date-optional-param: &end_date-optional-param
    name: end_date
    in: query
    required: false
    schema:
      type: string
      pattern: '^\d{8}$'
    description: Range end date in YYYYMMDD format (optional; pair with start_date instead of date)

  interval-param: &interval-param
    name: interval
    in: query
    required: true
    schema:
      type: string
    description: 'One of the interval presets: tick, 10ms, 100ms, 500ms, 1s, 5s, 10s, 15s, 30s, 1m, 5m, 10m, 15m, 30m, 1h.'

  time_of_day-param: &time_of_day-param
    name: time_of_day
    in: query
    required: true
    schema:
      type: string
    description: ET wall-clock time in HH:MM:SS.SSS (e.g. "09:30:00.000" for 9:30 AM ET; legacy "34200000" is also accepted)

  venue-param: &venue-param
    name: venue
    in: query
    required: false
    schema:
      type: string
    description: Data venue (e.g. "nqb" for Nasdaq Basic)

  min_time-param: &min_time-param
    name: min_time
    in: query
    required: false
    schema:
      type: string
    description: Minimum timestamp filter

  start_time-param: &start_time-param
    name: start_time
    in: query
    required: false
    schema:
      type: string
    description: Start time in milliseconds from midnight ET

  end_time-param: &end_time-param
    name: end_time
    in: query
    required: false
    schema:
      type: string
    description: End time in milliseconds from midnight ET

  request_type-param: &request_type-param
    name: request_type
    in: query
    required: true
    schema:
      type: string
      enum: [EOD, TRADE, QUOTE, OHLC, OPEN_INTEREST]
    description: The type of data request

  expiration-param: &expiration-param
    name: expiration
    in: query
    required: true
    schema:
      type: string
      pattern: '^(\d{8}|\*)$'
    description: >-
      Option expiration date in YYYYMMDD format. Pass `*` to select all
      expirations for the underlying (chain-wide; query one date at a time).

  expiration-no-star-param: &expiration-no-star-param
    name: expiration
    in: query
    required: true
    schema:
      type: string
      pattern: '^\d{8}$'
    description: >-
      Option expiration date in YYYYMMDD format. This endpoint resolves a
      single concrete expiration and rejects the `*` wildcard.

  strike-param: &strike-param
    name: strike
    in: query
    required: true
    schema:
      type: string
    description: Strike price in dollars as a string (e.g. "500" or "17.5")

  right-param: &right-param
    name: right
    in: query
    required: true
    schema:
      type: string
      enum: [call, put, both, C, P, c, p, CALL, PUT, Call, Put, "*"]
    description: >-
      Option side. Use `both` or `*` (alias) for calls and puts. Accepts
      `call`, `put`, `both` (upstream ThetaData vocabulary; upstream default
      is `both`), plus our short-forms `C` (call), `P` (put), and `*` (alias
      for `both`) for SDK ergonomics. Matching is case-insensitive. Every
      endpoint that exposes `right` accepts the `both` / `*` wildcard.

  max_dte-param: &max_dte-param
    name: max_dte
    in: query
    required: false
    schema:
      type: integer
    description: Maximum days to expiration filter

  strike_range-param: &strike_range-param
    name: strike_range
    in: query
    required: false
    schema:
      type: integer
    description: Number of strikes above/below ATM to include

  exclusive-param: &exclusive-param
    name: exclusive
    in: query
    required: false
    schema:
      type: boolean
    description: Whether time boundaries are exclusive

  # --- Greeks optional parameters ---
  annual_dividend-param: &annual_dividend-param
    name: annual_dividend
    in: query
    required: false
    schema:
      type: number
      format: double
    description: Annual dividend amount for Greeks calculation

  rate_type-param: &rate_type-param
    name: rate_type
    in: query
    required: false
    schema:
      type: string
    description: Interest rate type for Greeks calculation

  rate_value-param: &rate_value-param
    name: rate_value
    in: query
    required: false
    schema:
      type: number
      format: double
    description: Interest rate value for Greeks calculation

  stock_price-param: &stock_price-param
    name: stock_price
    in: query
    required: false
    schema:
      type: number
      format: double
    description: Override underlying stock price for Greeks calculation

  greeks_version-param: &greeks_version-param
    name: version
    in: query
    required: false
    schema:
      type: string
    description: Greeks calculation model version

  use_market_value-param: &use_market_value-param
    name: use_market_value
    in: query
    required: false
    schema:
      type: boolean
    description: Use market value instead of midpoint for Greeks calculation

  underlyer_use_nbbo-param: &underlyer_use_nbbo-param
    name: underlyer_use_nbbo
    in: query
    required: false
    schema:
      type: boolean
    description: Use NBBO midpoint for underlying price

  # --- Common response schemas ---
  ohlc-tick: &ohlc-tick
    type: object
    properties:
      ms_of_day:
        type: integer
        description: Milliseconds since midnight ET
      open:
        type: number
        format: double
        description: Opening price (f64)
      high:
        type: number
        format: double
        description: Highest price (f64)
      low:
        type: number
        format: double
        description: Lowest price (f64)
      close:
        type: number
        format: double
        description: Closing price (f64)
      volume:
        type: integer
        format: int64
        description: Number of shares/contracts traded
      count:
        type: integer
        format: int64
        description: Number of trades
      date:
        type: integer
        description: Date as YYYYMMDD integer

  trade-tick: &trade-tick
    type: object
    properties:
      ms_of_day:
        type: integer
        description: Milliseconds since midnight ET
      sequence:
        type: integer
        description: Exchange sequence number
      size:
        type: integer
        description: Trade size in shares
      price:
        type: number
        format: double
        description: Trade price (f64)
      exchange:
        type: integer
        description: Exchange code
      condition:
        type: integer
        description: Trade condition code
      date:
        type: integer
        description: Date as YYYYMMDD integer

  quote-tick: &quote-tick
    type: object
    properties:
      ms_of_day:
        type: integer
        description: Milliseconds since midnight ET
      bid:
        type: number
        format: double
        description: Bid price (f64)
      ask:
        type: number
        format: double
        description: Ask price (f64)
      bid_size:
        type: integer
        description: Bid size
      ask_size:
        type: integer
        description: Ask size
      date:
        type: integer
        description: Date as YYYYMMDD integer

  eod-tick: &eod-tick
    type: object
    properties:
      ms_of_day:
        type: integer
      open:
        type: number
        format: double
      high:
        type: number
        format: double
      low:
        type: number
        format: double
      close:
        type: number
        format: double
      volume:
        type: integer
        format: int64
      count:
        type: integer
        format: int64
      bid:
        type: number
        format: double
      ask:
        type: number
        format: double
      bid_size:
        type: integer
      ask_size:
        type: integer
      date:
        type: integer
        description: Date as YYYYMMDD integer

  open-interest-tick: &open-interest-tick
    type: object
    properties:
      ms_of_day:
        type: integer
      open_interest:
        type: integer
      date:
        type: integer

  trade-quote-tick: &trade-quote-tick
    type: object
    description: Combined trade + quote tick (25 fields)
    properties:
      ms_of_day:
        type: integer
      sequence:
        type: integer
      size:
        type: integer
      price:
        type: number
        format: double
      exchange:
        type: integer
      condition:
        type: integer
      bid:
        type: number
        format: double
      ask:
        type: number
        format: double
      bid_size:
        type: integer
      ask_size:
        type: integer
      date:
        type: integer

  data-table: &data-table
    type: object
    description: Generic DataTable with string headers and typed cell values
    properties:
      headers:
        type: array
        items:
          type: string
      data_table:
        type: array
        items:
          type: array
          items:
            oneOf:
              - type: string
              - type: integer
              - type: number
              - type: "null"

  symbol-list: &symbol-list
    type: array
    items:
      type: string

  date-list: &date-list
    type: array
    items:
      type: string
      pattern: '^\d{8}$'

  # --- Reusable response wrappers ---
  ohlc-response: &ohlc-response
    description: Successful response
    content:
      application/json:
        schema:
          type: array
          items: *ohlc-tick

  trade-response: &trade-response
    description: Successful response
    content:
      application/json:
        schema:
          type: array
          items: *trade-tick

  quote-response: &quote-response
    description: Successful response
    content:
      application/json:
        schema:
          type: array
          items: *quote-tick

  eod-response: &eod-response
    description: Successful response
    content:
      application/json:
        schema:
          type: array
          items: *eod-tick

  trade-quote-response: &trade-quote-response
    description: Successful response
    content:
      application/json:
        schema:
          type: array
          items: *trade-quote-tick

  option-contract-response: &option-contract-response
    description: Successful response
    content:
      application/json:
        schema:
          type: array
          items:
            type: object
            properties:
              root:
                type: string
              expiration:
                type: string
              strike:
                type: string
              right:
                type: string
                description: >-
                  Option right as emitted by the drop-in REST/WS server.
                  Currently `"C"` (call) or `"P"` (put); no enum constraint
                  so future output formats (e.g. upstream `"CALL"`/`"PUT"`)
                  remain backward-compatible.

  data-table-response: &data-table-response
    description: Successful response
    content:
      application/json:
        schema: *data-table

  symbol-list-response: &symbol-list-response
    description: Successful response
    content:
      application/json:
        schema: *symbol-list

  date-list-response: &date-list-response
    description: Successful response
    content:
      application/json:
        schema: *date-list

# ---------------------------------------------------------------------------
# PATHS
# ---------------------------------------------------------------------------
paths:

  # =========================================================================
  # STOCK / LIST
  # =========================================================================

  /v3/stock/list/symbols:
    x-min-subscription: free
    get:
      operationId: stockListSymbols
      summary: List all stock symbols
      description: |
        Returns all traded stock symbols. Updated overnight.
        gRPC: GetStockListSymbols
      tags: [Stock / List]
      parameters: []
      responses:
        '200': *symbol-list-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let symbols = client.market_data().stock_list_symbols().await?;
        - lang: python
          label: Python
          source: |
            symbols = client.market_data.stock_list_symbols()
        - lang: cpp
          label: C++
          source: |
            auto symbols = client.market_data().stock_list_symbols();

  /v3/stock/list/dates:
    x-min-subscription: free
    get:
      operationId: stockListDates
      summary: List available dates for a stock
      description: |
        Lists all dates of data available for a stock by request type (EOD, TRADE, QUOTE, etc.).
        Updated overnight.
        gRPC: GetStockListDates
      tags: [Stock / List]
      parameters:
        - *request_type-param
        - *symbol-param
      responses:
        '200': *date-list-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let dates = client.market_data().stock_list_dates("TRADE", "AAPL").await?;
        - lang: python
          label: Python
          source: |
            dates = client.market_data.stock_list_dates("TRADE", "AAPL")
        - lang: cpp
          label: C++
          source: |
            auto dates = client.market_data().stock_list_dates("TRADE", "AAPL");

  /v3/stock/list/dates/{request_type}:
    x-min-subscription: free
    get:
      operationId: stockListDatesByRequestType
      summary: List available dates for a stock (path form)
      description: |
        Path-segment form of `/v3/stock/list/dates`: the request type rides the
        URL path instead of a query parameter. Same endpoint and response; this
        is the terminal-compatible `/v3/stock/list/dates/{request_type}` spelling.
        gRPC: GetStockListDates
      tags: [Stock / List]
      parameters:
        - name: request_type
          in: path
          required: true
          schema:
            type: string
            enum: [EOD, TRADE, QUOTE, OHLC, OPEN_INTEREST]
          description: The type of data request (case-insensitive).
        - *symbol-param
      responses:
        '200': *date-list-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let dates = client.market_data().stock_list_dates("TRADE", "AAPL").await?;
        - lang: python
          label: Python
          source: |
            dates = client.market_data.stock_list_dates("TRADE", "AAPL")
        - lang: cpp
          label: C++
          source: |
            auto dates = client.market_data().stock_list_dates("TRADE", "AAPL");

  # =========================================================================
  # STOCK / SNAPSHOT
  # =========================================================================

  /v3/stock/snapshot/ohlc:
    x-min-subscription: value
    get:
      operationId: stockSnapshotOhlc
      summary: Stock OHLC snapshot
      description: |
        Real-time Open, High, Low, Close for the current trading day.
        Returns real-time data with standard/pro subscription, 15-minute delayed with value subscription.
        Snapshot cache resets at midnight ET daily.
        gRPC: GetStockSnapshotOhlc
      tags: [Stock / Snapshot]
      parameters:
        - *symbols-param
        - *venue-param
        - *min_time-param
      responses:
        '200': *ohlc-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let ohlc = client.market_data().stock_snapshot_ohlc(&["AAPL"]).await?;
        - lang: python
          label: Python
          source: |
            ohlc = client.market_data.stock_snapshot_ohlc(["AAPL"])
        - lang: cpp
          label: C++
          source: |
            auto ohlc = client.market_data().stock_snapshot_ohlc({"AAPL"});

  /v3/stock/snapshot/trade:
    x-min-subscription: standard
    get:
      operationId: stockSnapshotTrade
      summary: Stock trade snapshot
      description: |
        Real-time last trade from the Nasdaq Basic feed.
        Requires stocks standard or pro subscription.
        Snapshot cache resets at midnight ET daily.
        gRPC: GetStockSnapshotTrade
      tags: [Stock / Snapshot]
      parameters:
        - *symbols-param
        - *venue-param
        - *min_time-param
      responses:
        '200': *trade-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let trades = client.market_data().stock_snapshot_trade(&["AAPL"]).await?;
        - lang: python
          label: Python
          source: |
            trades = client.market_data.stock_snapshot_trade(["AAPL"])
        - lang: cpp
          label: C++
          source: |
            auto trades = client.market_data().stock_snapshot_trade({"AAPL"});

  /v3/stock/snapshot/quote:
    x-min-subscription: value
    get:
      operationId: stockSnapshotQuote
      summary: Stock quote snapshot
      description: |
        Real-time last BBO quote from the Nasdaq Basic feed (standard/pro) or
        15-minute delayed NBBO quote from UTP/CTA feeds (value subscription).
        Snapshot cache resets at midnight ET daily.
        gRPC: GetStockSnapshotQuote
      tags: [Stock / Snapshot]
      parameters:
        - *symbols-param
        - *venue-param
        - *min_time-param
      responses:
        '200': *quote-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let quotes = client.market_data().stock_snapshot_quote(&["AAPL"]).await?;
        - lang: python
          label: Python
          source: |
            quotes = client.market_data.stock_snapshot_quote(["AAPL"])
        - lang: cpp
          label: C++
          source: |
            auto quotes = client.market_data().stock_snapshot_quote({"AAPL"});

  /v3/stock/snapshot/market_value:
    x-min-subscription: standard
    get:
      operationId: stockSnapshotMarketValue
      summary: Stock market value snapshot
      description: |
        Latest market value snapshot for one or more stocks.
        gRPC: GetStockSnapshotMarketValue
      tags: [Stock / Snapshot]
      parameters:
        - *symbols-param
        - *venue-param
        - *min_time-param
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let mv = client.market_data().stock_snapshot_market_value(&["AAPL"]).await?;
        - lang: python
          label: Python
          source: |
            mv = client.market_data.stock_snapshot_market_value(["AAPL"])
        - lang: cpp
          label: C++
          source: |
            auto mv = client.market_data().stock_snapshot_market_value({"AAPL"});

  # =========================================================================
  # STOCK / HISTORY
  # =========================================================================

  /v3/stock/history/eod:
    x-min-subscription: free
    get:
      operationId: stockHistoryEod
      summary: Stock end-of-day history
      description: |
        End-of-day stock data for a date range including OHLC + closing quote.
        gRPC: GetStockHistoryEod
      tags: [Stock / History]
      parameters:
        - *symbol-param
        - *start_date-param
        - *end_date-param
      responses:
        '200': *eod-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let eod = client.market_data().stock_history_eod("AAPL", "20240101", "20240301").await?;
        - lang: python
          label: Python
          source: |
            df = client.market_data.stock_history_eod("AAPL", "20240101", "20240301").to_pandas()
        - lang: cpp
          label: C++
          source: |
            auto eod = client.market_data().stock_history_eod("AAPL", "20240101", "20240301");

  /v3/stock/history/ohlc:
    x-min-subscription: value
    get:
      operationId: stockHistoryOhlc
      summary: Stock intraday OHLC bars
      description: |
        Intraday OHLC bars. Supply `date` for a single day, or `start_date`/`end_date` for a date range on the same route.
        Interval is one of the presets: tick, 10ms, 100ms, 500ms, 1s, 5s, 10s, 15s, 30s, 1m, 5m, 10m, 15m, 30m, 1h.
        gRPC: GetStockHistoryOhlc
      tags: [Stock / History]
      parameters:
        - *symbol-param
        - *date-optional-param
        - *start_date-optional-param
        - *end_date-optional-param
        - *interval-param
        - *start_time-param
        - *end_time-param
        - *venue-param
      responses:
        '200': *ohlc-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let bars = client.market_data().stock_history_ohlc("AAPL").date("20240315").interval("1m").await?;
        - lang: python
          label: Python
          source: |
            bars = client.market_data.stock_history_ohlc("AAPL", date="20240315", interval="1m")
        - lang: cpp
          label: C++
          source: |
            auto bars = client.market_data().stock_history_ohlc("AAPL", thetadatadx::EndpointRequestOptions{}.with_date("20240315").with_interval("1m"));

  /v3/stock/history/trade:
    x-min-subscription: standard
    get:
      operationId: stockHistoryTrade
      summary: Stock trade history
      description: |
        All trades for a stock on a given date or date range.
        gRPC: GetStockHistoryTrade
      tags: [Stock / History]
      parameters:
        - *symbol-param
        - *date-optional-param
        - *start_time-param
        - *end_time-param
        - *venue-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date for multi-day range (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date for multi-day range (YYYYMMDD)
      responses:
        '200': *trade-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let trades = client.market_data().stock_history_trade("AAPL").date("20240315").await?;
        - lang: python
          label: Python
          source: |
            trades = client.market_data.stock_history_trade("AAPL", date="20240315")
        - lang: cpp
          label: C++
          source: |
            auto trades = client.market_data().stock_history_trade("AAPL", thetadatadx::EndpointRequestOptions{}.with_date("20240315"));
  /v3/stock/history/quote:
    x-min-subscription: value
    get:
      operationId: stockHistoryQuote
      summary: Stock quote history
      description: |
        NBBO quotes at a given interval. Use "0" for every quote change.
        gRPC: GetStockHistoryQuote
      tags: [Stock / History]
      parameters:
        - *symbol-param
        - *date-optional-param
        - *interval-param
        - *start_time-param
        - *end_time-param
        - *venue-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date for multi-day range (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date for multi-day range (YYYYMMDD)
      responses:
        '200': *quote-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let quotes = client.market_data().stock_history_quote("AAPL").date("20240315").interval("1m").await?;
        - lang: python
          label: Python
          source: |
            quotes = client.market_data.stock_history_quote("AAPL", date="20240315", interval="1m")
        - lang: cpp
          label: C++
          source: |
            auto quotes = client.market_data().stock_history_quote("AAPL", thetadatadx::EndpointRequestOptions{}.with_date("20240315").with_interval("1m"));
  /v3/stock/history/trade_quote:
    x-min-subscription: standard
    get:
      operationId: stockHistoryTradeQuote
      summary: Stock combined trade + quote history
      description: |
        Combined trade and quote ticks for a stock on a given date.
        Returns typed TradeQuoteTick data with both trade and quote fields.
        gRPC: GetStockHistoryTradeQuote
      tags: [Stock / History]
      parameters:
        - *symbol-param
        - *date-optional-param
        - *start_time-param
        - *end_time-param
        - *exclusive-param
        - *venue-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date for multi-day range (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date for multi-day range (YYYYMMDD)
      responses:
        '200': *trade-quote-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let tq = client.market_data().stock_history_trade_quote("AAPL").date("20240315").await?;
        - lang: python
          label: Python
          source: |
            tq = client.market_data.stock_history_trade_quote("AAPL", date="20240315")
        - lang: cpp
          label: C++
          source: |
            auto tq = client.market_data().stock_history_trade_quote("AAPL", thetadatadx::EndpointRequestOptions{}.with_date("20240315"));

  # =========================================================================
  # STOCK / AT-TIME
  # =========================================================================

  /v3/stock/at_time/trade:
    x-min-subscription: standard
    get:
      operationId: stockAtTimeTrade
      summary: Stock trade at specific time
      description: |
        Trade at a specific time of day across a date range.
        time_of_day uses ET wall-clock format HH:MM:SS.SSS (e.g. "09:30:00.000" for 9:30 AM ET). Legacy millisecond strings such as "34200000" are also accepted.
        gRPC: GetStockAtTimeTrade
      tags: [Stock / AtTime]
      parameters:
        - *symbol-param
        - *start_date-param
        - *end_date-param
        - *time_of_day-param
        - *venue-param
      responses:
        '200': *trade-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let trades = client.market_data().stock_at_time_trade("AAPL", "20240101", "20240301", "09:30:00.000").await?;
        - lang: python
          label: Python
          source: |
            trades = client.market_data.stock_at_time_trade("AAPL", "20240101", "20240301", "09:30:00.000")
        - lang: cpp
          label: C++
          source: |
            auto trades = client.market_data().stock_at_time_trade("AAPL", "20240101", "20240301", "09:30:00.000");

  /v3/stock/at_time/quote:
    x-min-subscription: value
    get:
      operationId: stockAtTimeQuote
      summary: Stock quote at specific time
      description: |
        Quote at a specific time of day across a date range.
        gRPC: GetStockAtTimeQuote
      tags: [Stock / AtTime]
      parameters:
        - *symbol-param
        - *start_date-param
        - *end_date-param
        - *time_of_day-param
        - *venue-param
      responses:
        '200': *quote-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let quotes = client.market_data().stock_at_time_quote("AAPL", "20240101", "20240301", "09:30:00.000").await?;
        - lang: python
          label: Python
          source: |
            quotes = client.market_data.stock_at_time_quote("AAPL", "20240101", "20240301", "09:30:00.000")
        - lang: cpp
          label: C++
          source: |
            auto quotes = client.market_data().stock_at_time_quote("AAPL", "20240101", "20240301", "09:30:00.000");

  # =========================================================================
  # OPTION / LIST
  # =========================================================================

  /v3/option/list/symbols:
    x-min-subscription: free
    get:
      operationId: optionListSymbols
      summary: List all option underlying symbols
      description: |
        Returns all option underlying symbols. Updated overnight.
        gRPC: GetOptionListSymbols
      tags: [Option / List]
      parameters: []
      responses:
        '200': *symbol-list-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let symbols = client.market_data().option_list_symbols().await?;
        - lang: python
          label: Python
          source: |
            symbols = client.market_data.option_list_symbols()
        - lang: cpp
          label: C++
          source: |
            auto symbols = client.market_data().option_list_symbols();

  /v3/option/list/dates:
    x-min-subscription: free
    get:
      operationId: optionListDates
      summary: List available dates for an option contract
      description: |
        Lists all dates of data available for an option contract by request type.
        gRPC: GetOptionListDates
      tags: [Option / List]
      parameters:
        - *request_type-param
        - *symbol-param
        - *expiration-no-star-param
        - *strike-param
        - *right-param
      responses:
        '200': *date-list-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let dates = client.market_data().option_list_dates("TRADE", "SPY", "20240419", "500", "C").await?;
        - lang: python
          label: Python
          source: |
            dates = client.market_data.option_list_dates("TRADE", "SPY", "20240419", "500", "C")
        - lang: cpp
          label: C++
          source: |
            auto dates = client.market_data().option_list_dates("TRADE", "SPY", "20240419", "500", "C");

  /v3/option/list/dates/{request_type}:
    x-min-subscription: free
    get:
      operationId: optionListDatesByRequestType
      summary: List available dates for an option contract (path form)
      description: |
        Path-segment form of `/v3/option/list/dates`: the request type rides the
        URL path instead of a query parameter. Same endpoint and response; this
        is the terminal-compatible `/v3/option/list/dates/{request_type}` spelling.
        gRPC: GetOptionListDates
      tags: [Option / List]
      parameters:
        - name: request_type
          in: path
          required: true
          schema:
            type: string
            enum: [EOD, TRADE, QUOTE, OHLC, OPEN_INTEREST]
          description: The type of data request (case-insensitive).
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
      responses:
        '200': *date-list-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let dates = client.market_data().option_list_dates("TRADE", "SPY", "20240419", "500", "C").await?;
        - lang: python
          label: Python
          source: |
            dates = client.market_data.option_list_dates("TRADE", "SPY", "20240419", "500", "C")
        - lang: cpp
          label: C++
          source: |
            auto dates = client.market_data().option_list_dates("TRADE", "SPY", "20240419", "500", "C");

  /v3/option/list/expirations:
    x-min-subscription: free
    get:
      operationId: optionListExpirations
      summary: List option expiration dates
      description: |
        Returns all expiration dates for an underlying symbol as YYYYMMDD strings.
        gRPC: GetOptionListExpirations
      tags: [Option / List]
      parameters:
        - *symbol-param
      responses:
        '200': *date-list-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let exps = client.market_data().option_list_expirations("SPY").await?;
        - lang: python
          label: Python
          source: |
            exps = client.market_data.option_list_expirations("SPY")
        - lang: cpp
          label: C++
          source: |
            auto exps = client.market_data().option_list_expirations("SPY");

  /v3/option/list/strikes:
    x-min-subscription: free
    get:
      operationId: optionListStrikes
      summary: List option strike prices
      description: |
        Returns all strike prices for a given underlying + expiration.
        gRPC: GetOptionListStrikes
      tags: [Option / List]
      parameters:
        - *symbol-param
        - *expiration-no-star-param
      responses:
        '200':
          description: Successful response
          content:
            application/json:
              schema:
                type: array
                items:
                  type: string
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let strikes = client.market_data().option_list_strikes("SPY", "20240419").await?;
        - lang: python
          label: Python
          source: |
            strikes = client.market_data.option_list_strikes("SPY", "20240419")
        - lang: cpp
          label: C++
          source: |
            auto strikes = client.market_data().option_list_strikes("SPY", "20240419");

  /v3/option/list/contracts:
    x-min-subscription: free
    get:
      operationId: optionListContracts
      summary: List all option contracts for a date
      description: |
        Returns all option contracts for a symbol on a given date. Returns typed
        OptionContract data with root, expiration, strike, and right fields.
        gRPC: GetOptionListContracts
      tags: [Option / List]
      parameters:
        - *request_type-param
        - *symbol-param
        - *date-param
        - *max_dte-param
      responses:
        '200': *option-contract-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let contracts = client.market_data().option_list_contracts("TRADE", "SPY", "20240315").await?;
        - lang: python
          label: Python
          source: |
            contracts = client.market_data.option_list_contracts("TRADE", "SPY", "20240315")
        - lang: cpp
          label: C++
          source: |
            auto contracts = client.market_data().option_list_contracts("TRADE", "SPY", "20240315");

  /v3/option/list/contracts/{request_type}:
    x-min-subscription: free
    get:
      operationId: optionListContractsByRequestType
      summary: List all option contracts for a date (path form)
      description: |
        Path-segment form of `/v3/option/list/contracts`: the request type rides
        the URL path instead of a query parameter. Same endpoint and response;
        this is the terminal-compatible
        `/v3/option/list/contracts/{request_type}` spelling.
        gRPC: GetOptionListContracts
      tags: [Option / List]
      parameters:
        - name: request_type
          in: path
          required: true
          schema:
            type: string
            enum: [EOD, TRADE, QUOTE, OHLC, OPEN_INTEREST]
          description: The type of data request (case-insensitive).
        - *symbol-param
        - *date-param
        - *max_dte-param
      responses:
        '200': *option-contract-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let contracts = client.market_data().option_list_contracts("TRADE", "SPY", "20240315").await?;
        - lang: python
          label: Python
          source: |
            contracts = client.market_data.option_list_contracts("TRADE", "SPY", "20240315")
        - lang: cpp
          label: C++
          source: |
            auto contracts = client.market_data().option_list_contracts("TRADE", "SPY", "20240315");

  # =========================================================================
  # OPTION / SNAPSHOT
  # =========================================================================

  /v3/option/snapshot/ohlc:
    x-min-subscription: value
    get:
      operationId: optionSnapshotOhlc
      summary: Option OHLC snapshot
      description: |
        Latest OHLC snapshot for option contracts.
        gRPC: GetOptionSnapshotOhlc
      tags: [Option / Snapshot]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *max_dte-param
        - *strike_range-param
        - *min_time-param
      responses:
        '200': *ohlc-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let ohlc = client.market_data().option_snapshot_ohlc("SPY", "20240419", "500", "C").await?;
        - lang: python
          label: Python
          source: |
            ohlc = client.market_data.option_snapshot_ohlc("SPY", "20240419", "500", "C")
        - lang: cpp
          label: C++
          source: |
            auto ohlc = client.market_data().option_snapshot_ohlc("SPY", "20240419", "500", "C");

  /v3/option/snapshot/trade:
    x-min-subscription: standard
    get:
      operationId: optionSnapshotTrade
      summary: Option trade snapshot
      description: |
        Latest trade snapshot for option contracts.
        gRPC: GetOptionSnapshotTrade
      tags: [Option / Snapshot]
      parameters:
        - *symbol-param
        - *expiration-no-star-param
        - *strike-param
        - *right-param
        - *strike_range-param
        - *min_time-param
      responses:
        '200': *trade-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let trades = client.market_data().option_snapshot_trade("SPY", "20240419", "500", "C").await?;
        - lang: python
          label: Python
          source: |
            trades = client.market_data.option_snapshot_trade("SPY", "20240419", "500", "C")
        - lang: cpp
          label: C++
          source: |
            auto trades = client.market_data().option_snapshot_trade("SPY", "20240419", "500", "C");

  /v3/option/snapshot/quote:
    x-min-subscription: value
    get:
      operationId: optionSnapshotQuote
      summary: Option quote snapshot
      description: |
        Latest NBBO quote snapshot for option contracts.
        gRPC: GetOptionSnapshotQuote
      tags: [Option / Snapshot]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *max_dte-param
        - *strike_range-param
        - *min_time-param
      responses:
        '200': *quote-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let quotes = client.market_data().option_snapshot_quote("SPY", "20240419", "500", "C").await?;
        - lang: python
          label: Python
          source: |
            quotes = client.market_data.option_snapshot_quote("SPY", "20240419", "500", "C")
        - lang: cpp
          label: C++
          source: |
            auto quotes = client.market_data().option_snapshot_quote("SPY", "20240419", "500", "C");

  /v3/option/snapshot/open_interest:
    x-min-subscription: value
    get:
      operationId: optionSnapshotOpenInterest
      summary: Option open interest snapshot
      description: |
        Latest open interest snapshot for option contracts.
        gRPC: GetOptionSnapshotOpenInterest
      tags: [Option / Snapshot]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *max_dte-param
        - *strike_range-param
        - *min_time-param
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let oi = client.market_data().option_snapshot_open_interest("SPY", "20240419", "500", "C").await?;
        - lang: python
          label: Python
          source: |
            oi = client.market_data.option_snapshot_open_interest("SPY", "20240419", "500", "C")
        - lang: cpp
          label: C++
          source: |
            auto oi = client.market_data().option_snapshot_open_interest("SPY", "20240419", "500", "C");

  /v3/option/snapshot/market_value:
    x-min-subscription: standard
    get:
      operationId: optionSnapshotMarketValue
      summary: Option market value snapshot
      description: |
        Latest market value snapshot for option contracts.
        gRPC: GetOptionSnapshotMarketValue
      tags: [Option / Snapshot]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *max_dte-param
        - *strike_range-param
        - *min_time-param
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let mv = client.market_data().option_snapshot_market_value("SPY", "20240419", "500", "C").await?;
        - lang: python
          label: Python
          source: |
            mv = client.market_data.option_snapshot_market_value("SPY", "20240419", "500", "C")
        - lang: cpp
          label: C++
          source: |
            auto mv = client.market_data().option_snapshot_market_value("SPY", "20240419", "500", "C");

  # =========================================================================
  # OPTION / SNAPSHOT GREEKS
  # =========================================================================

  /v3/option/snapshot/greeks/implied_volatility:
    x-min-subscription: standard
    get:
      operationId: optionSnapshotGreeksImpliedVolatility
      summary: Option implied volatility snapshot
      description: |
        Real-time implied volatility snapshot for option contracts.
        gRPC: GetOptionSnapshotGreeksImpliedVolatility
      tags: [Option / Snapshot Greeks]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *max_dte-param
        - *strike_range-param
        - *min_time-param
        - *annual_dividend-param
        - *rate_type-param
        - *rate_value-param
        - *stock_price-param
        - *greeks_version-param
        - *use_market_value-param
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let iv = client.market_data().option_snapshot_greeks_implied_volatility("SPY", "20240419", "500", "C").await?;
        - lang: python
          label: Python
          source: |
            iv = client.market_data.option_snapshot_greeks_implied_volatility("SPY", "20240419", "500", "C")
        - lang: cpp
          label: C++
          source: |
            auto iv = client.market_data().option_snapshot_greeks_implied_volatility("SPY", "20240419", "500", "C");

  /v3/option/snapshot/greeks/all:
    x-min-subscription: professional
    get:
      operationId: optionSnapshotGreeksAll
      summary: Option all Greeks snapshot
      description: |
        Snapshot of all 22 Greeks (delta, gamma, theta, vega, rho, vanna, charm, vomma,
        veta, speed, zomma, color, ultima, d1, d2, dual_delta, dual_gamma, epsilon, lambda, IV, value).
        gRPC: GetOptionSnapshotGreeksAll
      tags: [Option / Snapshot Greeks]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *max_dte-param
        - *strike_range-param
        - *min_time-param
        - *annual_dividend-param
        - *rate_type-param
        - *rate_value-param
        - *stock_price-param
        - *greeks_version-param
        - *use_market_value-param
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let greeks = client.market_data().option_snapshot_greeks_all("SPY", "20240419", "500", "C").await?;
        - lang: python
          label: Python
          source: |
            greeks = client.market_data.option_snapshot_greeks_all("SPY", "20240419", "500", "C")
        - lang: cpp
          label: C++
          source: |
            auto greeks = client.market_data().option_snapshot_greeks_all("SPY", "20240419", "500", "C");

  /v3/option/snapshot/greeks/first_order:
    x-min-subscription: standard
    get:
      operationId: optionSnapshotGreeksFirstOrder
      summary: Option first-order Greeks snapshot
      description: |
        First-order Greeks snapshot (delta, theta, vega, rho, epsilon, lambda).
        gRPC: GetOptionSnapshotGreeksFirstOrder
      tags: [Option / Snapshot Greeks]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *max_dte-param
        - *strike_range-param
        - *min_time-param
        - *annual_dividend-param
        - *rate_type-param
        - *rate_value-param
        - *stock_price-param
        - *greeks_version-param
        - *use_market_value-param
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let g1 = client.market_data().option_snapshot_greeks_first_order("SPY", "20240419", "500", "C").await?;
        - lang: python
          label: Python
          source: |
            g1 = client.market_data.option_snapshot_greeks_first_order("SPY", "20240419", "500", "C")
        - lang: cpp
          label: C++
          source: |
            auto g1 = client.market_data().option_snapshot_greeks_first_order("SPY", "20240419", "500", "C");

  /v3/option/snapshot/greeks/second_order:
    x-min-subscription: professional
    get:
      operationId: optionSnapshotGreeksSecondOrder
      summary: Option second-order Greeks snapshot
      description: |
        Second-order Greeks snapshot (gamma, vanna, charm, vomma, veta, dual_delta, dual_gamma).
        gRPC: GetOptionSnapshotGreeksSecondOrder
      tags: [Option / Snapshot Greeks]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *max_dte-param
        - *strike_range-param
        - *min_time-param
        - *annual_dividend-param
        - *rate_type-param
        - *rate_value-param
        - *stock_price-param
        - *greeks_version-param
        - *use_market_value-param
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let g2 = client.market_data().option_snapshot_greeks_second_order("SPY", "20240419", "500", "C").await?;
        - lang: python
          label: Python
          source: |
            g2 = client.market_data.option_snapshot_greeks_second_order("SPY", "20240419", "500", "C")
        - lang: cpp
          label: C++
          source: |
            auto g2 = client.market_data().option_snapshot_greeks_second_order("SPY", "20240419", "500", "C");

  /v3/option/snapshot/greeks/third_order:
    x-min-subscription: professional
    get:
      operationId: optionSnapshotGreeksThirdOrder
      summary: Option third-order Greeks snapshot
      description: |
        Third-order Greeks snapshot (speed, zomma, color, ultima).
        gRPC: GetOptionSnapshotGreeksThirdOrder
      tags: [Option / Snapshot Greeks]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *max_dte-param
        - *strike_range-param
        - *min_time-param
        - *annual_dividend-param
        - *rate_type-param
        - *rate_value-param
        - *stock_price-param
        - *greeks_version-param
        - *use_market_value-param
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let g3 = client.market_data().option_snapshot_greeks_third_order("SPY", "20240419", "500", "C").await?;
        - lang: python
          label: Python
          source: |
            g3 = client.market_data.option_snapshot_greeks_third_order("SPY", "20240419", "500", "C")
        - lang: cpp
          label: C++
          source: |
            auto g3 = client.market_data().option_snapshot_greeks_third_order("SPY", "20240419", "500", "C");

  # =========================================================================
  # OPTION / HISTORY
  # =========================================================================

  /v3/option/history/eod:
    x-min-subscription: free
    get:
      operationId: optionHistoryEod
      summary: Option end-of-day history
      description: |
        End-of-day option data for a date range. Includes OHLC + closing quote.
        gRPC: GetOptionHistoryEod
      tags: [Option / History]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *start_date-param
        - *end_date-param
        - *max_dte-param
        - *strike_range-param
      responses:
        '200': *eod-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let eod = client.market_data().option_history_eod("SPY", "20240419", "500", "C", "20240101", "20240301").await?;
        - lang: python
          label: Python
          source: |
            eod = client.market_data.option_history_eod("SPY", "20240419", "500", "C", "20240101", "20240301")
            df = client.market_data.option_history_eod("SPY", "20240419", "500", "C", "20240101", "20240301").to_pandas()
        - lang: cpp
          label: C++
          source: |
            auto eod = client.market_data().option_history_eod("SPY", "20240419", "500", "C", "20240101", "20240301");

  /v3/option/history/ohlc:
    x-min-subscription: value
    get:
      operationId: optionHistoryOhlc
      summary: Option intraday OHLC bars
      description: |
        Intraday option OHLC bars for a given date and interval.
        gRPC: GetOptionHistoryOhlc
      tags: [Option / History]
      parameters:
        - *symbol-param
        - *expiration-no-star-param
        - *strike-param
        - *right-param
        - *date-optional-param
        - *interval-param
        - *start_time-param
        - *end_time-param
        - *strike_range-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date for multi-day range (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date for multi-day range (YYYYMMDD)
      responses:
        '200': *ohlc-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let bars = client.market_data().option_history_ohlc("SPY", "20240419", "500", "C", "20240315", "1m").await?;
        - lang: python
          label: Python
          source: |
            bars = client.market_data.option_history_ohlc("SPY", "20240419", "500", "C", "20240315", "1m")
        - lang: cpp
          label: C++
          source: |
            auto bars = client.market_data().option_history_ohlc("SPY", "20240419", "500", "C", "20240315", "1m");

  /v3/option/history/trade:
    x-min-subscription: standard
    get:
      operationId: optionHistoryTrade
      summary: Option trade history
      description: |
        All trades for an option contract on a given date.
        gRPC: GetOptionHistoryTrade
      tags: [Option / History]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *date-optional-param
        - *start_time-param
        - *end_time-param
        - *max_dte-param
        - *strike_range-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date (YYYYMMDD)
      responses:
        '200': *trade-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let trades = client.market_data().option_history_trade("SPY", "20240419", "500", "C", "20240315").await?;
        - lang: python
          label: Python
          source: |
            trades = client.market_data.option_history_trade("SPY", "20240419", "500", "C", "20240315")
        - lang: cpp
          label: C++
          source: |
            auto trades = client.market_data().option_history_trade("SPY", "20240419", "500", "C", "20240315");
  /v3/option/history/quote:
    x-min-subscription: value
    get:
      operationId: optionHistoryQuote
      summary: Option quote history
      description: |
        NBBO quotes for an option contract at a given interval.
        gRPC: GetOptionHistoryQuote
      tags: [Option / History]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *date-optional-param
        - *interval-param
        - *start_time-param
        - *end_time-param
        - *max_dte-param
        - *strike_range-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date (YYYYMMDD)
      responses:
        '200': *quote-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let quotes = client.market_data().option_history_quote("SPY", "20240419", "500", "C", "20240315", "1m").await?;
        - lang: python
          label: Python
          source: |
            quotes = client.market_data.option_history_quote("SPY", "20240419", "500", "C", "20240315", "1m")
        - lang: cpp
          label: C++
          source: |
            auto quotes = client.market_data().option_history_quote("SPY", "20240419", "500", "C", "20240315", "1m");
  /v3/option/history/trade_quote:
    x-min-subscription: standard
    get:
      operationId: optionHistoryTradeQuote
      summary: Option combined trade + quote history
      description: |
        Combined trade and quote ticks for an option contract.
        gRPC: GetOptionHistoryTradeQuote
      tags: [Option / History]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *date-optional-param
        - *start_time-param
        - *end_time-param
        - *exclusive-param
        - *max_dte-param
        - *strike_range-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date (YYYYMMDD)
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let tq = client.market_data().option_history_trade_quote("SPY", "20240419", "500", "C", "20240315").await?;
        - lang: python
          label: Python
          source: |
            tq = client.market_data.option_history_trade_quote("SPY", "20240419", "500", "C", "20240315")
        - lang: cpp
          label: C++
          source: |
            auto tq = client.market_data().option_history_trade_quote("SPY", "20240419", "500", "C", "20240315");

  /v3/option/history/open_interest:
    x-min-subscription: value
    get:
      operationId: optionHistoryOpenInterest
      summary: Option open interest history
      description: |
        Open interest history for an option contract.
        gRPC: GetOptionHistoryOpenInterest
      tags: [Option / History]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *date-optional-param
        - *max_dte-param
        - *strike_range-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date (YYYYMMDD)
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let oi = client.market_data().option_history_open_interest("SPY", "20240419", "500", "C", "20240315").await?;
        - lang: python
          label: Python
          source: |
            oi = client.market_data.option_history_open_interest("SPY", "20240419", "500", "C", "20240315")
        - lang: cpp
          label: C++
          source: |
            auto oi = client.market_data().option_history_open_interest("SPY", "20240419", "500", "C", "20240315");

  # =========================================================================
  # OPTION / HISTORY GREEKS
  # =========================================================================

  /v3/option/history/greeks/eod:
    x-min-subscription: standard
    get:
      operationId: optionHistoryGreeksEod
      summary: Option EOD Greeks history
      description: |
        End-of-day Greeks history for an option contract.
        gRPC: GetOptionHistoryGreeksEod
      tags: [Option / History Greeks]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *start_date-param
        - *end_date-param
        - *annual_dividend-param
        - *rate_type-param
        - *rate_value-param
        - *greeks_version-param
        - *underlyer_use_nbbo-param
        - *max_dte-param
        - *strike_range-param
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let greeks = client.market_data().option_history_greeks_eod("SPY", "20240419", "500", "C", "20240101", "20240301").await?;
        - lang: python
          label: Python
          source: |
            greeks = client.market_data.option_history_greeks_eod("SPY", "20240419", "500", "C", "20240101", "20240301")
        - lang: cpp
          label: C++
          source: |
            auto greeks = client.market_data().option_history_greeks_eod("SPY", "20240419", "500", "C", "20240101", "20240301");

  /v3/option/history/greeks/all:
    x-min-subscription: professional
    get:
      operationId: optionHistoryGreeksAll
      summary: Option all Greeks history (intraday)
      description: |
        All 22 Greeks history, sampled at a given interval.
        gRPC: GetOptionHistoryGreeksAll
      tags: [Option / History Greeks]
      parameters:
        - *symbol-param
        - *expiration-no-star-param
        - *strike-param
        - *right-param
        - *date-optional-param
        - *interval-param
        - *start_time-param
        - *end_time-param
        - *annual_dividend-param
        - *rate_type-param
        - *rate_value-param
        - *greeks_version-param
        - *strike_range-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date (YYYYMMDD)
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let greeks = client.market_data().option_history_greeks_all("SPY", "20240419", "500", "C", "20240315", "1m").await?;
        - lang: python
          label: Python
          source: |
            greeks = client.market_data.option_history_greeks_all("SPY", "20240419", "500", "C", "20240315", "1m")
        - lang: cpp
          label: C++
          source: |
            auto greeks = client.market_data().option_history_greeks_all("SPY", "20240419", "500", "C", "20240315", "1m");

  /v3/option/history/greeks/first_order:
    x-min-subscription: standard
    get:
      operationId: optionHistoryGreeksFirstOrder
      summary: Option first-order Greeks history (intraday)
      description: |
        First-order Greeks history (delta, theta, vega, rho), sampled at a given interval.
        gRPC: GetOptionHistoryGreeksFirstOrder
      tags: [Option / History Greeks]
      parameters:
        - *symbol-param
        - *expiration-no-star-param
        - *strike-param
        - *right-param
        - *date-optional-param
        - *interval-param
        - *start_time-param
        - *end_time-param
        - *annual_dividend-param
        - *rate_type-param
        - *rate_value-param
        - *greeks_version-param
        - *strike_range-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date (YYYYMMDD)
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let g1 = client.market_data().option_history_greeks_first_order("SPY", "20240419", "500", "C", "20240315", "1m").await?;
        - lang: python
          label: Python
          source: |
            g1 = client.market_data.option_history_greeks_first_order("SPY", "20240419", "500", "C", "20240315", "1m")
        - lang: cpp
          label: C++
          source: |
            auto g1 = client.market_data().option_history_greeks_first_order("SPY", "20240419", "500", "C", "20240315", "1m");

  /v3/option/history/greeks/second_order:
    x-min-subscription: professional
    get:
      operationId: optionHistoryGreeksSecondOrder
      summary: Option second-order Greeks history (intraday)
      description: |
        Second-order Greeks history (gamma, vanna, charm, vomma, veta), sampled at a given interval.
        gRPC: GetOptionHistoryGreeksSecondOrder
      tags: [Option / History Greeks]
      parameters:
        - *symbol-param
        - *expiration-no-star-param
        - *strike-param
        - *right-param
        - *date-optional-param
        - *interval-param
        - *start_time-param
        - *end_time-param
        - *annual_dividend-param
        - *rate_type-param
        - *rate_value-param
        - *greeks_version-param
        - *strike_range-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date (YYYYMMDD)
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let g2 = client.market_data().option_history_greeks_second_order("SPY", "20240419", "500", "C", "20240315", "1m").await?;
        - lang: python
          label: Python
          source: |
            g2 = client.market_data.option_history_greeks_second_order("SPY", "20240419", "500", "C", "20240315", "1m")
        - lang: cpp
          label: C++
          source: |
            auto g2 = client.market_data().option_history_greeks_second_order("SPY", "20240419", "500", "C", "20240315", "1m");

  /v3/option/history/greeks/third_order:
    x-min-subscription: professional
    get:
      operationId: optionHistoryGreeksThirdOrder
      summary: Option third-order Greeks history (intraday)
      description: |
        Third-order Greeks history (speed, zomma, color, ultima), sampled at a given interval.
        gRPC: GetOptionHistoryGreeksThirdOrder
      tags: [Option / History Greeks]
      parameters:
        - *symbol-param
        - *expiration-no-star-param
        - *strike-param
        - *right-param
        - *date-optional-param
        - *interval-param
        - *start_time-param
        - *end_time-param
        - *annual_dividend-param
        - *rate_type-param
        - *rate_value-param
        - *greeks_version-param
        - *strike_range-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date (YYYYMMDD)
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let g3 = client.market_data().option_history_greeks_third_order("SPY", "20240419", "500", "C", "20240315", "1m").await?;
        - lang: python
          label: Python
          source: |
            g3 = client.market_data.option_history_greeks_third_order("SPY", "20240419", "500", "C", "20240315", "1m")
        - lang: cpp
          label: C++
          source: |
            auto g3 = client.market_data().option_history_greeks_third_order("SPY", "20240419", "500", "C", "20240315", "1m");

  /v3/option/history/greeks/implied_volatility:
    x-min-subscription: standard
    get:
      operationId: optionHistoryGreeksImpliedVolatility
      summary: Option implied volatility history (intraday)
      description: |
        Implied volatility history, sampled at a given interval.
        gRPC: GetOptionHistoryGreeksImpliedVolatility
      tags: [Option / History Greeks]
      parameters:
        - *symbol-param
        - *expiration-no-star-param
        - *strike-param
        - *right-param
        - *date-optional-param
        - *interval-param
        - *start_time-param
        - *end_time-param
        - *annual_dividend-param
        - *rate_type-param
        - *rate_value-param
        - *greeks_version-param
        - *strike_range-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date (YYYYMMDD)
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let iv = client.market_data().option_history_greeks_implied_volatility("SPY", "20240419", "500", "C", "20240315", "1m").await?;
        - lang: python
          label: Python
          source: |
            iv = client.market_data.option_history_greeks_implied_volatility("SPY", "20240419", "500", "C", "20240315", "1m")
        - lang: cpp
          label: C++
          source: |
            auto iv = client.market_data().option_history_greeks_implied_volatility("SPY", "20240419", "500", "C", "20240315", "1m");

  # =========================================================================
  # OPTION / HISTORY TRADE GREEKS
  # =========================================================================

  /v3/option/history/trade_greeks/all:
    x-min-subscription: professional
    get:
      operationId: optionHistoryTradeGreeksAll
      summary: Option all Greeks on each trade
      description: |
        All 22 Greeks computed on each individual trade for an option contract.
        gRPC: GetOptionHistoryTradeGreeksAll
      tags: [Option / History Trade Greeks]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *date-optional-param
        - *start_time-param
        - *end_time-param
        - *annual_dividend-param
        - *rate_type-param
        - *rate_value-param
        - *greeks_version-param
        - *max_dte-param
        - *strike_range-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date (YYYYMMDD)
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let tg = client.market_data().option_history_trade_greeks_all("SPY", "20240419", "500", "C", "20240315").await?;
        - lang: python
          label: Python
          source: |
            tg = client.market_data.option_history_trade_greeks_all("SPY", "20240419", "500", "C", "20240315")
        - lang: cpp
          label: C++
          source: |
            auto tg = client.market_data().option_history_trade_greeks_all("SPY", "20240419", "500", "C", "20240315");

  /v3/option/history/trade_greeks/first_order:
    x-min-subscription: professional
    get:
      operationId: optionHistoryTradeGreeksFirstOrder
      summary: Option first-order Greeks on each trade
      description: |
        First-order Greeks (delta, theta, vega, rho) computed on each individual trade.
        gRPC: GetOptionHistoryTradeGreeksFirstOrder
      tags: [Option / History Trade Greeks]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *date-optional-param
        - *start_time-param
        - *end_time-param
        - *annual_dividend-param
        - *rate_type-param
        - *rate_value-param
        - *greeks_version-param
        - *max_dte-param
        - *strike_range-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date (YYYYMMDD)
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let tg1 = client.market_data().option_history_trade_greeks_first_order("SPY", "20240419", "500", "C", "20240315").await?;
        - lang: python
          label: Python
          source: |
            tg1 = client.market_data.option_history_trade_greeks_first_order("SPY", "20240419", "500", "C", "20240315")
        - lang: cpp
          label: C++
          source: |
            auto tg1 = client.market_data().option_history_trade_greeks_first_order("SPY", "20240419", "500", "C", "20240315");

  /v3/option/history/trade_greeks/second_order:
    x-min-subscription: professional
    get:
      operationId: optionHistoryTradeGreeksSecondOrder
      summary: Option second-order Greeks on each trade
      description: |
        Second-order Greeks (gamma, vanna, charm, vomma, veta) computed on each individual trade.
        gRPC: GetOptionHistoryTradeGreeksSecondOrder
      tags: [Option / History Trade Greeks]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *date-optional-param
        - *start_time-param
        - *end_time-param
        - *annual_dividend-param
        - *rate_type-param
        - *rate_value-param
        - *greeks_version-param
        - *max_dte-param
        - *strike_range-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date (YYYYMMDD)
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let tg2 = client.market_data().option_history_trade_greeks_second_order("SPY", "20240419", "500", "C", "20240315").await?;
        - lang: python
          label: Python
          source: |
            tg2 = client.market_data.option_history_trade_greeks_second_order("SPY", "20240419", "500", "C", "20240315")
        - lang: cpp
          label: C++
          source: |
            auto tg2 = client.market_data().option_history_trade_greeks_second_order("SPY", "20240419", "500", "C", "20240315");

  /v3/option/history/trade_greeks/third_order:
    x-min-subscription: professional
    get:
      operationId: optionHistoryTradeGreeksThirdOrder
      summary: Option third-order Greeks on each trade
      description: |
        Third-order Greeks (speed, zomma, color, ultima) computed on each individual trade.
        gRPC: GetOptionHistoryTradeGreeksThirdOrder
      tags: [Option / History Trade Greeks]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *date-optional-param
        - *start_time-param
        - *end_time-param
        - *annual_dividend-param
        - *rate_type-param
        - *rate_value-param
        - *greeks_version-param
        - *max_dte-param
        - *strike_range-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date (YYYYMMDD)
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let tg3 = client.market_data().option_history_trade_greeks_third_order("SPY", "20240419", "500", "C", "20240315").await?;
        - lang: python
          label: Python
          source: |
            tg3 = client.market_data.option_history_trade_greeks_third_order("SPY", "20240419", "500", "C", "20240315")
        - lang: cpp
          label: C++
          source: |
            auto tg3 = client.market_data().option_history_trade_greeks_third_order("SPY", "20240419", "500", "C", "20240315");

  /v3/option/history/trade_greeks/implied_volatility:
    x-min-subscription: professional
    get:
      operationId: optionHistoryTradeGreeksImpliedVolatility
      summary: Option implied volatility on each trade
      description: |
        Implied volatility computed on each individual trade.
        gRPC: GetOptionHistoryTradeGreeksImpliedVolatility
      tags: [Option / History Trade Greeks]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *date-optional-param
        - *start_time-param
        - *end_time-param
        - *annual_dividend-param
        - *rate_type-param
        - *rate_value-param
        - *greeks_version-param
        - *max_dte-param
        - *strike_range-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date (YYYYMMDD)
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let tiv = client.market_data().option_history_trade_greeks_implied_volatility("SPY", "20240419", "500", "C", "20240315").await?;
        - lang: python
          label: Python
          source: |
            tiv = client.market_data.option_history_trade_greeks_implied_volatility("SPY", "20240419", "500", "C", "20240315")
        - lang: cpp
          label: C++
          source: |
            auto tiv = client.market_data().option_history_trade_greeks_implied_volatility("SPY", "20240419", "500", "C", "20240315");

  # =========================================================================
  # OPTION / AT-TIME
  # =========================================================================

  /v3/option/at_time/trade:
    x-min-subscription: standard
    get:
      operationId: optionAtTimeTrade
      summary: Option trade at specific time
      description: |
        Trade at a specific time of day across a date range for an option contract.
        gRPC: GetOptionAtTimeTrade
      tags: [Option / AtTime]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *start_date-param
        - *end_date-param
        - *time_of_day-param
        - *max_dte-param
        - *strike_range-param
      responses:
        '200': *trade-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let trades = client.market_data().option_at_time_trade("SPY", "20240419", "500", "C", "20240101", "20240301", "09:30:00.000").await?;
        - lang: python
          label: Python
          source: |
            trades = client.market_data.option_at_time_trade("SPY", "20240419", "500", "C", "20240101", "20240301", "09:30:00.000")
        - lang: cpp
          label: C++
          source: |
            auto trades = client.market_data().option_at_time_trade("SPY", "20240419", "500", "C", "20240101", "20240301", "09:30:00.000");

  /v3/option/at_time/quote:
    x-min-subscription: value
    get:
      operationId: optionAtTimeQuote
      summary: Option quote at specific time
      description: |
        Quote at a specific time of day across a date range for an option contract.
        gRPC: GetOptionAtTimeQuote
      tags: [Option / AtTime]
      parameters:
        - *symbol-param
        - *expiration-param
        - *strike-param
        - *right-param
        - *start_date-param
        - *end_date-param
        - *time_of_day-param
        - *max_dte-param
        - *strike_range-param
      responses:
        '200': *quote-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let quotes = client.market_data().option_at_time_quote("SPY", "20240419", "500", "C", "20240101", "20240301", "09:30:00.000").await?;
        - lang: python
          label: Python
          source: |
            quotes = client.market_data.option_at_time_quote("SPY", "20240419", "500", "C", "20240101", "20240301", "09:30:00.000")
        - lang: cpp
          label: C++
          source: |
            auto quotes = client.market_data().option_at_time_quote("SPY", "20240419", "500", "C", "20240101", "20240301", "09:30:00.000");

  # =========================================================================
  # INDEX / LIST
  # =========================================================================

  /v3/index/list/symbols:
    x-min-subscription: free
    get:
      operationId: indexListSymbols
      summary: List all index symbols
      description: |
        Returns all available index symbols. Updated overnight.
        gRPC: GetIndexListSymbols
      tags: [Index / List]
      parameters: []
      responses:
        '200': *symbol-list-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let symbols = client.market_data().index_list_symbols().await?;
        - lang: python
          label: Python
          source: |
            symbols = client.market_data.index_list_symbols()
        - lang: cpp
          label: C++
          source: |
            auto symbols = client.market_data().index_list_symbols();

  /v3/index/list/dates:
    x-min-subscription: free
    get:
      operationId: indexListDates
      summary: List available dates for an index
      description: |
        Lists all dates of data available for an index symbol.
        gRPC: GetIndexListDates
      tags: [Index / List]
      parameters:
        - *symbol-param
      responses:
        '200': *date-list-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let dates = client.market_data().index_list_dates("SPX").await?;
        - lang: python
          label: Python
          source: |
            dates = client.market_data.index_list_dates("SPX")
        - lang: cpp
          label: C++
          source: |
            auto dates = client.market_data().index_list_dates("SPX");

  # =========================================================================
  # INDEX / SNAPSHOT
  # =========================================================================

  /v3/index/snapshot/ohlc:
    x-min-subscription: standard
    get:
      operationId: indexSnapshotOhlc
      summary: Index OHLC snapshot
      description: |
        Latest OHLC snapshot for one or more indices.
        gRPC: GetIndexSnapshotOhlc
      tags: [Index / Snapshot]
      parameters:
        - *symbols-param
        - *min_time-param
      responses:
        '200': *ohlc-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let ohlc = client.market_data().index_snapshot_ohlc(&["SPX"]).await?;
        - lang: python
          label: Python
          source: |
            ohlc = client.market_data.index_snapshot_ohlc(["SPX"])
        - lang: cpp
          label: C++
          source: |
            auto ohlc = client.market_data().index_snapshot_ohlc({"SPX"});

  /v3/index/snapshot/price:
    x-min-subscription: standard
    get:
      operationId: indexSnapshotPrice
      summary: Index price snapshot
      description: |
        Latest price snapshot for one or more indices.
        gRPC: GetIndexSnapshotPrice
      tags: [Index / Snapshot]
      parameters:
        - *symbols-param
        - *min_time-param
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let price = client.market_data().index_snapshot_price(&["SPX"]).await?;
        - lang: python
          label: Python
          source: |
            price = client.market_data.index_snapshot_price(["SPX"])
        - lang: cpp
          label: C++
          source: |
            auto price = client.market_data().index_snapshot_price({"SPX"});

  /v3/index/snapshot/market_value:
    x-min-subscription: standard
    get:
      operationId: indexSnapshotMarketValue
      summary: Index market value snapshot
      description: |
        Latest market value snapshot for one or more indices.
        gRPC: GetIndexSnapshotMarketValue
      tags: [Index / Snapshot]
      parameters:
        - *symbols-param
        - *min_time-param
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let mv = client.market_data().index_snapshot_market_value(&["SPX"]).await?;
        - lang: python
          label: Python
          source: |
            mv = client.market_data.index_snapshot_market_value(["SPX"])
        - lang: cpp
          label: C++
          source: |
            auto mv = client.market_data().index_snapshot_market_value({"SPX"});

  # =========================================================================
  # INDEX / HISTORY
  # =========================================================================

  /v3/index/history/eod:
    x-min-subscription: free
    get:
      operationId: indexHistoryEod
      summary: Index end-of-day history
      description: |
        End-of-day index data for a date range.
        gRPC: GetIndexHistoryEod
      tags: [Index / History]
      parameters:
        - *symbol-param
        - *start_date-param
        - *end_date-param
      responses:
        '200': *eod-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let eod = client.market_data().index_history_eod("SPX", "20240101", "20240301").await?;
        - lang: python
          label: Python
          source: |
            eod = client.market_data.index_history_eod("SPX", "20240101", "20240301")
        - lang: cpp
          label: C++
          source: |
            auto eod = client.market_data().index_history_eod("SPX", "20240101", "20240301");

  /v3/index/history/ohlc:
    x-min-subscription: standard
    get:
      operationId: indexHistoryOhlc
      summary: Index intraday OHLC bars
      description: |
        Intraday OHLC bars for an index across a date range.
        gRPC: GetIndexHistoryOhlc
      tags: [Index / History]
      parameters:
        - *symbol-param
        - *start_date-param
        - *end_date-param
        - *interval-param
        - *start_time-param
        - *end_time-param
      responses:
        '200': *ohlc-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let bars = client.market_data().index_history_ohlc("SPX", "20240101", "20240301", "1m").await?;
        - lang: python
          label: Python
          source: |
            bars = client.market_data.index_history_ohlc("SPX", "20240101", "20240301", "1m")
        - lang: cpp
          label: C++
          source: |
            auto bars = client.market_data().index_history_ohlc("SPX", "20240101", "20240301", "1m");

  /v3/index/history/price:
    x-min-subscription: value
    get:
      operationId: indexHistoryPrice
      summary: Index intraday price history
      description: |
        Intraday price history for an index.
        gRPC: GetIndexHistoryPrice
      tags: [Index / History]
      parameters:
        - *symbol-param
        - *date-optional-param
        - *interval-param
        - *start_time-param
        - *end_time-param
        - name: start_date
          in: query
          required: false
          schema:
            type: string
          description: Start date (YYYYMMDD)
        - name: end_date
          in: query
          required: false
          schema:
            type: string
          description: End date (YYYYMMDD)
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let prices = client.market_data().index_history_price("SPX", "20240315", "1m").await?;
        - lang: python
          label: Python
          source: |
            prices = client.market_data.index_history_price("SPX", "20240315", "1m")
        - lang: cpp
          label: C++
          source: |
            auto prices = client.market_data().index_history_price("SPX", "20240315", "1m");

  # =========================================================================
  # INDEX / AT-TIME
  # =========================================================================

  /v3/index/at_time/price:
    x-min-subscription: value
    get:
      operationId: indexAtTimePrice
      summary: Index price at specific time
      description: |
        Index price at a specific time of day across a date range.
        gRPC: GetIndexAtTimePrice
      tags: [Index / AtTime]
      parameters:
        - *symbol-param
        - *start_date-param
        - *end_date-param
        - *time_of_day-param
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let prices = client.market_data().index_at_time_price("SPX", "20240101", "20240301", "09:30:00.000").await?;
        - lang: python
          label: Python
          source: |
            prices = client.market_data.index_at_time_price("SPX", "20240101", "20240301", "09:30:00.000")
        - lang: cpp
          label: C++
          source: |
            auto prices = client.market_data().index_at_time_price("SPX", "20240101", "20240301", "09:30:00.000");

  # =========================================================================
  # CALENDAR
  # =========================================================================

  /v3/calendar/open_today:
    x-min-subscription: free
    get:
      operationId: calendarOpenToday
      summary: Is the market open today?
      description: |
        Returns whether the market is open today.
        gRPC: GetCalendarOpenToday
      tags: [Calendar]
      parameters: []
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let open = client.market_data().calendar_open_today().await?;
        - lang: python
          label: Python
          source: |
            open = client.market_data.calendar_open_today()
        - lang: cpp
          label: C++
          source: |
            auto open = client.market_data().calendar_open_today();

  /v3/calendar/on_date:
    x-min-subscription: free
    get:
      operationId: calendarOnDate
      summary: Calendar info for a specific date
      description: |
        Returns calendar information (open/close times, early close, holiday) for a specific date.
        gRPC: GetCalendarOnDate
      tags: [Calendar]
      parameters:
        - *date-param
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let cal = client.market_data().calendar_on_date("20240315").await?;
        - lang: python
          label: Python
          source: |
            cal = client.market_data.calendar_on_date("20240315")
        - lang: cpp
          label: C++
          source: |
            auto cal = client.market_data().calendar_on_date("20240315");

  /v3/calendar/year:
    x-min-subscription: free
    get:
      operationId: calendarYear
      summary: Calendar for an entire year
      description: |
        Returns full calendar information for all trading days in a year.
        gRPC: GetCalendarYear
      tags: [Calendar]
      parameters:
        - name: year
          in: query
          required: true
          schema:
            type: string
            pattern: '^\d{4}$'
          description: Four-digit year (e.g. "2024")
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let cal = client.market_data().calendar_year("2024").await?;
        - lang: python
          label: Python
          source: |
            cal = client.market_data.calendar_year("2024")
        - lang: cpp
          label: C++
          source: |
            auto cal = client.market_data().calendar_year("2024");

  /v3/calendar/today:
    x-min-subscription: free
    get:
      operationId: calendarToday
      summary: Is the market open today?
      description: |
        Path alias of `/v3/calendar/open_today`: returns whether the market is
        open today. Same endpoint, same response; this is the `/v3` path name.
        gRPC: GetCalendarOpenToday
      tags: [Calendar]
      parameters: []
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let open = client.market_data().calendar_open_today().await?;
        - lang: python
          label: Python
          source: |
            open = client.market_data.calendar_open_today()
        - lang: cpp
          label: C++
          source: |
            auto open = client.market_data().calendar_open_today();

  /v3/calendar/year_holidays:
    x-min-subscription: free
    get:
      operationId: calendarYearHolidays
      summary: Calendar for an entire year
      description: |
        Path alias of `/v3/calendar/year`: returns full calendar information for
        all trading days in a year. Same endpoint, same response; this is the
        `/v3` path name.
        gRPC: GetCalendarYear
      tags: [Calendar]
      parameters:
        - name: year
          in: query
          required: true
          schema:
            type: string
            pattern: '^\d{4}$'
          description: Four-digit year (e.g. "2024")
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let cal = client.market_data().calendar_year("2024").await?;
        - lang: python
          label: Python
          source: |
            cal = client.market_data.calendar_year("2024")
        - lang: cpp
          label: C++
          source: |
            auto cal = client.market_data().calendar_year("2024");

  # =========================================================================
  # INTEREST RATE
  # =========================================================================

  /v3/rate/history/eod:
    x-min-subscription: free
    get:
      operationId: interestRateHistoryEod
      summary: Interest rate EOD history
      description: |
        End-of-day interest rate history for a given rate symbol (e.g. "SOFR").
        gRPC: GetInterestRateHistoryEod
      tags: [Interest Rate]
      parameters:
        - *symbol-param
        - *start_date-param
        - *end_date-param
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let rates = client.market_data().interest_rate_history_eod("SOFR", "20240101", "20240301").await?;
        - lang: python
          label: Python
          source: |
            rates = client.market_data.interest_rate_history_eod("SOFR", "20240101", "20240301")
        - lang: cpp
          label: C++
          source: |
            auto rates = client.market_data().interest_rate_history_eod("SOFR", "20240101", "20240301");

  /v3/interest_rate/history/eod:
    x-min-subscription: free
    get:
      operationId: interestRateHistoryEodPath
      summary: Interest rate EOD history
      description: |
        Path alias of `/v3/rate/history/eod`: end-of-day interest rate history
        for a given rate symbol (e.g. "SOFR"). Same endpoint, same response;
        this is the spelled-out `/v3` path name.
        gRPC: GetInterestRateHistoryEod
      tags: [Interest Rate]
      parameters:
        - *symbol-param
        - *start_date-param
        - *end_date-param
      responses:
        '200': *data-table-response
      x-code-examples:
        - lang: rust
          label: Rust
          source: |
            let rates = client.market_data().interest_rate_history_eod("SOFR", "20240101", "20240301").await?;
        - lang: python
          label: Python
          source: |
            rates = client.market_data.interest_rate_history_eod("SOFR", "20240101", "20240301")
        - lang: cpp
          label: C++
          source: |
            auto rates = client.market_data().interest_rate_history_eod("SOFR", "20240101", "20240301");

  # =========================================================================
  # TERMINAL SYSTEM
  # =========================================================================
  # Mirrored 1:1 from the JVM terminal this server replaces: three
  # unauthenticated GET routes with bare `text/plain` bodies, so operator
  # tooling written against the terminal's mgmt surface works unchanged.

  /v3/terminal/shutdown:
    get:
      operationId: terminalShutdown
      tags: [Terminal]
      summary: Shut down the server
      description: |
        Kills the server process, matching the JVM terminal. Unauthenticated;
        returns the plain-text body `OK`.
      parameters: []
      responses:
        '200':
          description: Shutdown initiated.
          content:
            text/plain:
              schema:
                type: string
                enum: [OK]

  /v3/terminal/fpss/status:
    get:
      operationId: terminalStreamingStatus
      tags: [Terminal]
      summary: Streaming channel status (text/plain)
      description: |
        Streaming channel health as the terminal's one-word `text/plain`
        body.
      parameters: []
      responses:
        '200':
          description: Streaming channel state as a one-word plain-text body.
          content:
            text/plain:
              schema:
                type: string
                enum: [CONNECTED, DISCONNECTED]

  /v3/terminal/mdds/status:
    get:
      operationId: terminalMarketDataStatus
      tags: [Terminal]
      summary: Market-data channel status (text/plain)
      description: |
        Market-data channel health as the terminal's one-word `text/plain`
        body.
      parameters: []
      responses:
        '200':
          description: Market-data channel state as a one-word plain-text body.
          content:
            text/plain:
              schema:
                type: string
                enum: [CONNECTED, DISCONNECTED]


  # =========================================================================
  # FLAT FILES
  # =========================================================================

  /v3/{sec_type}/flat_file/{req_type}:
    get:
      operationId: flatfileGetBySecType
      tags: [Flat Files]
      summary: Download a daily flat file (sec_type/flat_file/req_type path form)
      description: |
        The terminal's flat-file path form: the security type leads the path
        and `flat_file` separates it from the request type. The `sec_type` and
        `req_type` segments are matched case-insensitively. The response body
        is the file bytes, streamed as an attachment. Only these pairs are
        served (any other pairing returns 400):
        `option/trade_quote`, `option/open_interest`, `option/eod`,
        `stock/trade_quote`, `stock/eod`. The path form takes the
        parameters independently, so an unserved pairing (for example
        `stock/open_interest`) is accepted by the route but rejected at request
        time with 400.
      parameters:
        - name: sec_type
          in: path
          required: true
          schema:
            type: string
            enum: [option, stock]
          description: Security type (case-insensitive).
        - name: req_type
          in: path
          required: true
          schema:
            type: string
            enum: [trade_quote, open_interest, eod]
          description: |
            Request type (case-insensitive). `open_interest` is served for
            `option` only.
        - name: date
          in: query
          required: true
          schema:
            type: string
            format: date
          description: Trading date in YYYY-MM-DD or YYYYMMDD format.
        - name: format
          in: query
          required: false
          schema:
            type: string
            enum: [csv, json, ndjson, jsonl, html]
            default: csv
          description: |
            On-disk format. Defaults to csv. `json` streams a single JSON
            array; `ndjson` (alias `jsonl`) newline-delimited JSON; `html` an
            HTML table.
      responses:
        '200':
          description: |
            Flat-file bytes streamed as an attachment. Content type is
            `text/csv` for csv, `application/json` for json,
            `application/x-ndjson` for ndjson / jsonl, or `text/html` for html.
          content:
            text/csv:
              schema:
                type: string
                format: binary
            application/json:
              schema:
                type: string
                format: binary
            application/x-ndjson:
              schema:
                type: string
                format: binary
            text/html:
              schema:
                type: string
                format: binary
        '400':
          description: Unknown security type, request type, format, or date, or an unserved pair.
        '502':
          description: The flat-file distribution is unavailable upstream.

# ---------------------------------------------------------------------------
# COMPONENTS
# ---------------------------------------------------------------------------
components:

  schemas:
    OhlcTick: *ohlc-tick
    TradeTick: *trade-tick
    QuoteTick: *quote-tick
    EodTick: *eod-tick
    OpenInterestTick: *open-interest-tick
    TradeQuoteTick: *trade-quote-tick
    DataTable: *data-table

    ContractSpec:
      type: object
      description: Option contract specification
      required: [symbol, expiration]
      properties:
        symbol:
          type: string
          description: Underlying symbol (e.g. "SPY")
        expiration:
          type: string
          description: Expiration date in YYYYMMDD format
        strike:
          type: string
          description: Strike price in dollars as a string
        right:
          type: string
          enum: [C, P]
          description: '"C" for call, "P" for put'

    Price:
      type: number
      format: double
      description: All prices are decoded to f64 (double) during parsing.

    StreamEventKind:
      type: string
      description: |
        Discriminator for streaming event variants. This is a literal-union
        string, not a TypeScript `const enum`, so the type information
        stays self-contained under `isolatedModules: true` and works
        in every toolchain (Vite, esbuild, ts-jest, Next.js).
      enum:
        - ohlcvc
        - open_interest
        - quote
        - trade
        - simple
        - raw_data

    StreamEvent:
      type: object
      description: Real-time streaming event
      oneOf:
        - type: object
          description: Quote tick (best bid/ask update)
          properties:
            kind:
              type: string
              enum: [quote]
            contract_id:
              type: integer
            ms_of_day:
              type: integer
            bid:
              type: number
              format: double
            ask:
              type: number
              format: double
            bid_size:
              type: integer
            ask_size:
              type: integer
            bid_exchange:
              type: integer
            ask_exchange:
              type: integer
            date:
              type: integer
            received_at_ns:
              type: integer
              format: int64
              description: Nanosecond monotonic receipt timestamp
        - type: object
          description: Trade tick
          properties:
            kind:
              type: string
              enum: [trade]
            contract_id:
              type: integer
            ms_of_day:
              type: integer
            sequence:
              type: integer
            price:
              type: number
              format: double
            size:
              type: integer
            exchange:
              type: integer
            condition:
              type: integer
            date:
              type: integer
            received_at_ns:
              type: integer
              format: int64
              description: Nanosecond monotonic receipt timestamp
        - type: object
          description: Open interest tick
          properties:
            kind:
              type: string
              enum: [open_interest]
            contract_id:
              type: integer
            ms_of_day:
              type: integer
            open_interest:
              type: integer
            date:
              type: integer
            received_at_ns:
              type: integer
              format: int64
              description: Nanosecond monotonic receipt timestamp
        - type: object
          description: |
            OHLCVC bar (open/high/low/close + volume + count).
            Volume and count are `i64`.
          properties:
            kind:
              type: string
              enum: [ohlcvc]
            contract_id:
              type: integer
            ms_of_day:
              type: integer
            open:
              type: number
              format: double
            high:
              type: number
              format: double
            low:
              type: number
              format: double
            close:
              type: number
              format: double
            volume:
              type: integer
              format: int64
            count:
              type: integer
              format: int64
            date:
              type: integer
            received_at_ns:
              type: integer
              format: int64
              description: Nanosecond monotonic receipt timestamp
        - type: object
          description: |
            Control / diagnostic event (streaming lifecycle). SDKs flatten
            every `StreamControl` variant into a single `Simple` pyclass / TS
            object with `kind: "simple"` and an `event_type` discriminator.
            Use `event_type` to branch on the concrete variant; `detail` is
            a free-form diagnostic string; `id` is the contract_id / req_id
            / reconnect-attempt number where applicable.
          properties:
            kind:
              type: string
              enum: [simple]
              description: Top-level variant discriminator — always "simple" for control events.
            event_type:
              type: string
              enum:
                - login_success
                - contract_assigned
                - req_response
                - market_open
                - market_close
                - server_error
                - disconnected
                - reconnecting
                - reconnected
                - error
                - unknown_frame
                - unknown_event
              description: Concrete control-event name.
            detail:
              type: string
              nullable: true
              description: Free-form diagnostic payload; `null` for variants with no payload.
            id:
              type: integer
              format: int32
              nullable: true
              description: contract_id / req_id / reconnect attempt number (variant-specific).
            received_at_ns:
              type: integer
              format: int64
              description: Nanosecond monotonic receipt timestamp
        - type: object
          description: |
            Raw (non-decoded) streaming payload. `payload` is a base64-encoded
            copy of the original bytes (escape-hatch for forward-compat /
            debugging; SDKs should prefer decoded variants above).
          properties:
            kind:
              type: string
              enum: [raw_data]
            code:
              type: integer
              format: int32
              minimum: 0
              maximum: 255
              description: Wire byte code (0-255). Original streaming protocol uses unsigned 8-bit.
            payload:
              type: string
              description: Base64-encoded raw bytes
            received_at_ns:
              type: integer
              format: int64
              description: Nanosecond monotonic receipt timestamp

    Error:
      type: object
      properties:
        code:
          type: string
          enum: [Auth, Http, Grpc, NoData]
        message:
          type: string

  parameters:
    symbol:
      <<: *symbol-param
    symbols:
      <<: *symbols-param
    start_date:
      <<: *start_date-param
    end_date:
      <<: *end_date-param
    date:
      <<: *date-param
    interval:
      <<: *interval-param
    time_of_day:
      <<: *time_of_day-param
    venue:
      <<: *venue-param
    min_time:
      <<: *min_time-param
    request_type:
      <<: *request_type-param
    expiration:
      <<: *expiration-param
    strike:
      <<: *strike-param
    right:
      <<: *right-param
    max_dte:
      <<: *max_dte-param
    strike_range:
      <<: *strike_range-param
