// @generated DO NOT EDIT — regenerated by build.rs from endpoint_surface.toml
/// Optional builder parameters for registry-driven endpoint wrappers.
///
/// `timeout_ms` is the cross-cutting per-call deadline; see `with_timeout_ms`
/// and `with_deadline`. On expiry the in-flight gRPC call is cancelled and a
/// `thetadatadx::Error` carrying "Request deadline exceeded" is thrown.
///
/// This is one shared options bag passed to every endpoint, so the compiler
/// cannot reject a setter that does not apply to the endpoint you call: a
/// `with_annual_dividend(...)` on a `stock_history_ohlc` call, or a
/// `with_strike(...)` on `calendar_on_date`, compiles and is silently
/// dropped — only the parameters the endpoint actually accepts reach the
/// wire. Each endpoint method documents the parameters it honours; consult
/// that list rather than relying on a setter compiling. The Python keyword
/// signatures and the TypeScript per-method options objects enforce this at
/// the call site by construction; in C++ the shared bag trades that
/// per-endpoint checking for one stable struct across all endpoints.
struct EndpointRequestOptions {
    /// Venue/exchange filter. Accepted values: `nqb`, `utp_cta`.
    std::optional<std::string> venue;
    /// Minimum time filter
    std::optional<std::string> min_time;
    /// Single date YYYYMMDD. Supply this for a single-day pull, or supply `start_date`/`end_date` for a range. When present, `date` takes precedence over the range.
    std::optional<std::string> date;
    /// Interval preset. Defaults to `1s` when omitted — matching the upstream ThetaData Python library. Accepted values: `tick`, `10ms`, `100ms`, `500ms`, `1s`, `5s`, `10s`, `15s`, `30s`, `1m`, `5m`, `10m`, `15m`, `30m`, `1h`.
    std::optional<std::string> interval;
    /// Start time filter
    std::optional<std::string> start_time;
    /// End time filter
    std::optional<std::string> end_time;
    /// Start date YYYYMMDD
    std::optional<std::string> start_date;
    /// End date YYYYMMDD
    std::optional<std::string> end_date;
    /// When true, quotes whose timestamp equals the trade timestamp are excluded; only quotes strictly before the trade are paired. Defaults to true, matching the terminal, which injects exclusive=true when the value is omitted.
    std::optional<bool> exclusive;
    /// Strike price in dollars as a string (e.g. 500 or 17.5). Use `*` for wildcard selection.
    std::optional<std::string> strike;
    /// Option side. Use `both` or `*` (alias) for calls and puts. Accepted values: `call`, `put`, `both`, `*`.
    std::optional<std::string> right;
    /// Ticker symbol to filter by (e.g. AAPL). Omit to list every contract for the date.
    std::optional<std::string> symbol;
    /// Maximum days to expiration
    std::optional<int32_t> max_dte;
    /// Strike range filter
    std::optional<int32_t> strike_range;
    /// Annualized expected dividend amount, in dollars per share, used in the Greeks calculation (e.g. 2.5 is $2.50 per share per year).
    std::optional<double> annual_dividend;
    /// Risk-free-rate source used in the Greeks calculation. Accepted values: `sofr`, `treasury_m1`, `treasury_m3`, `treasury_m6`, `treasury_y1`, `treasury_y2`, `treasury_y3`, `treasury_y5`, `treasury_y7`, `treasury_y10`, `treasury_y20`, `treasury_y30`.
    std::optional<std::string> rate_type;
    /// Interest rate as a percent (4.36 means 4.36%, matching the InterestRateTick.rate convention) used in the Greeks calculation. Applied when rate_type selects a manual rate.
    std::optional<double> rate_value;
    /// Underlying price in dollars used in the Greeks calculation, overriding the observed underlying when set.
    std::optional<double> stock_price;
    /// Greeks model version. Accepted values: `latest`, `1`.
    std::optional<std::string> version;
    /// When true, calculate Greeks against the option market value (mid-price) instead of the NBBO bid/ask pair.
    std::optional<bool> use_market_value;
    /// When true, use the NBBO-derived underlyer price as the Greeks input instead of the last trade.
    std::optional<bool> underlyer_use_nbbo;
    /// Per-call deadline in milliseconds.
    std::optional<uint64_t> timeout_ms;

    EndpointRequestOptions& with_venue(std::string value) {
        venue = std::move(value);
        return *this;
    }
    EndpointRequestOptions& with_min_time(std::string value) {
        min_time = std::move(value);
        return *this;
    }
    EndpointRequestOptions& with_date(std::string value) {
        date = std::move(value);
        return *this;
    }
    EndpointRequestOptions& with_interval(std::string value) {
        interval = std::move(value);
        return *this;
    }
    EndpointRequestOptions& with_start_time(std::string value) {
        start_time = std::move(value);
        return *this;
    }
    EndpointRequestOptions& with_end_time(std::string value) {
        end_time = std::move(value);
        return *this;
    }
    EndpointRequestOptions& with_start_date(std::string value) {
        start_date = std::move(value);
        return *this;
    }
    EndpointRequestOptions& with_end_date(std::string value) {
        end_date = std::move(value);
        return *this;
    }
    EndpointRequestOptions& with_exclusive(bool value) {
        exclusive = value;
        return *this;
    }
    EndpointRequestOptions& with_strike(std::string value) {
        strike = std::move(value);
        return *this;
    }
    EndpointRequestOptions& with_right(std::string value) {
        right = std::move(value);
        return *this;
    }
    EndpointRequestOptions& with_symbol(std::string value) {
        symbol = std::move(value);
        return *this;
    }
    EndpointRequestOptions& with_max_dte(int32_t value) {
        max_dte = value;
        return *this;
    }
    EndpointRequestOptions& with_strike_range(int32_t value) {
        strike_range = value;
        return *this;
    }
    EndpointRequestOptions& with_annual_dividend(double value) {
        annual_dividend = value;
        return *this;
    }
    EndpointRequestOptions& with_rate_type(std::string value) {
        rate_type = std::move(value);
        return *this;
    }
    EndpointRequestOptions& with_rate_value(double value) {
        rate_value = value;
        return *this;
    }
    EndpointRequestOptions& with_stock_price(double value) {
        stock_price = value;
        return *this;
    }
    EndpointRequestOptions& with_version(std::string value) {
        version = std::move(value);
        return *this;
    }
    EndpointRequestOptions& with_use_market_value(bool value) {
        use_market_value = value;
        return *this;
    }
    EndpointRequestOptions& with_underlyer_use_nbbo(bool value) {
        underlyer_use_nbbo = value;
        return *this;
    }
    EndpointRequestOptions& with_timeout_ms(uint64_t value) {
        timeout_ms = value;
        return *this;
    }
    EndpointRequestOptions& with_deadline(std::chrono::milliseconds value) {
        if (value.count() < 0) {
            throw InvalidParameterError(
                "thetadatadx: deadline must be non-negative");
        }
        timeout_ms = static_cast<uint64_t>(value.count());
        return *this;
    }
};

namespace detail {

struct FfiEndpointRequestOptions {
    ThetaDataDxEndpointRequestOptions raw{};
    std::string venue_storage;
    std::string min_time_storage;
    std::string date_storage;
    std::string interval_storage;
    std::string start_time_storage;
    std::string end_time_storage;
    std::string start_date_storage;
    std::string end_date_storage;
    std::string strike_storage;
    std::string right_storage;
    std::string symbol_storage;
    std::string rate_type_storage;
    std::string version_storage;

    explicit FfiEndpointRequestOptions(const EndpointRequestOptions& options) {
        if (options.venue) {
            venue_storage = *options.venue;
            raw.venue = venue_storage.c_str();
        }
        if (options.min_time) {
            min_time_storage = *options.min_time;
            raw.min_time = min_time_storage.c_str();
        }
        if (options.date) {
            date_storage = *options.date;
            raw.date = date_storage.c_str();
        }
        if (options.interval) {
            interval_storage = *options.interval;
            raw.interval = interval_storage.c_str();
        }
        if (options.start_time) {
            start_time_storage = *options.start_time;
            raw.start_time = start_time_storage.c_str();
        }
        if (options.end_time) {
            end_time_storage = *options.end_time;
            raw.end_time = end_time_storage.c_str();
        }
        if (options.start_date) {
            start_date_storage = *options.start_date;
            raw.start_date = start_date_storage.c_str();
        }
        if (options.end_date) {
            end_date_storage = *options.end_date;
            raw.end_date = end_date_storage.c_str();
        }
        if (options.exclusive) {
            raw.exclusive = *options.exclusive ? 1 : 0;
            raw.has_exclusive = 1;
        }
        if (options.strike) {
            strike_storage = *options.strike;
            raw.strike = strike_storage.c_str();
        }
        if (options.right) {
            right_storage = *options.right;
            raw.right = right_storage.c_str();
        }
        if (options.symbol) {
            symbol_storage = *options.symbol;
            raw.symbol = symbol_storage.c_str();
        }
        if (options.max_dte) {
            raw.max_dte = *options.max_dte;
            raw.has_max_dte = 1;
        }
        if (options.strike_range) {
            raw.strike_range = *options.strike_range;
            raw.has_strike_range = 1;
        }
        if (options.annual_dividend) {
            raw.annual_dividend = *options.annual_dividend;
            raw.has_annual_dividend = 1;
        }
        if (options.rate_type) {
            rate_type_storage = *options.rate_type;
            raw.rate_type = rate_type_storage.c_str();
        }
        if (options.rate_value) {
            raw.rate_value = *options.rate_value;
            raw.has_rate_value = 1;
        }
        if (options.stock_price) {
            raw.stock_price = *options.stock_price;
            raw.has_stock_price = 1;
        }
        if (options.version) {
            version_storage = *options.version;
            raw.version = version_storage.c_str();
        }
        if (options.use_market_value) {
            raw.use_market_value = *options.use_market_value ? 1 : 0;
            raw.has_use_market_value = 1;
        }
        if (options.underlyer_use_nbbo) {
            raw.underlyer_use_nbbo = *options.underlyer_use_nbbo ? 1 : 0;
            raw.has_underlyer_use_nbbo = 1;
        }
        if (options.timeout_ms) {
            raw.timeout_ms = *options.timeout_ms;
            raw.has_timeout_ms = 1;
        }
    }

    // Non-copyable and non-movable: `raw`'s `const char*` fields borrow into the
    // `*_storage` members of this same object, so any copy or move would leave the
    // destination's `raw` pointers dangling into the source's buffers. This type is
    // only ever constructed in place and consumed by reference (`&raw`), so deleting
    // these makes the borrow invariant unbreakable by construction.
    FfiEndpointRequestOptions(const FfiEndpointRequestOptions&) = delete;
    FfiEndpointRequestOptions(FfiEndpointRequestOptions&&) = delete;
    FfiEndpointRequestOptions& operator=(const FfiEndpointRequestOptions&) = delete;
    FfiEndpointRequestOptions& operator=(FfiEndpointRequestOptions&&) = delete;
};

} // namespace detail
