// @generated DO NOT EDIT — regenerated by build.rs from endpoint_surface.toml

/// List all available stock ticker symbols.
///
/// A symbol can be defined as a unique identifier for a stock / underlying asset. Common terms also include: root, ticker, and underlying. This endpoint returns all traded symbols for stocks. This endpoint is updated overnight.
#[pyclass(module = "thetadatadx", name = "StockListSymbolsBuilder")]
pub struct StockListSymbolsBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl StockListSymbolsBuilder {
    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed `StringList` wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<StringList>> {
        let client = self.client.clone();
        let timeout_ms = self.timeout_ms;
        let values: Vec<String> = run_blocking(py, async move {
            let call = client.market_data().stock_list_symbols();
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        })?;
        strings_to_string_list(py, values, "symbol")
    }

    /// Async companion to `list()` — awaitable yields the `StringList` wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let call = client.market_data().stock_list_symbols();
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        }, |py, values| strings_to_string_list(py, values, "symbol").map(|p| p.into_any()))
    }
}

/// List available dates for a stock by request type (EOD, TRADE, QUOTE, etc.).
///
/// Lists all dates of data that are available for a stock with a given request type and symbol. This endpoint is updated overnight.
#[pyclass(module = "thetadatadx", name = "StockListDatesBuilder")]
pub struct StockListDatesBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    request_type: String,
    symbol: String,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl StockListDatesBuilder {
    /// Replace the required `request_type` on the pending request.
    fn request_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.request_type = value.into_string();
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed `StringList` wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<StringList>> {
        let client = self.client.clone();
        let timeout_ms = self.timeout_ms;
        let request_type = self.request_type.clone();
        let symbol = self.symbol.clone();
        let values: Vec<String> = run_blocking(py, async move {
            let call = client.market_data().stock_list_dates(&request_type, &symbol);
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        })?;
        strings_to_string_list(py, values, "date")
    }

    /// Async companion to `list()` — awaitable yields the `StringList` wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let request_type = self.request_type.clone();
        let symbol = self.symbol.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let call = client.market_data().stock_list_dates(&request_type, &symbol);
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        }, |py, values| strings_to_string_list(py, values, "date").map(|p| p.into_any()))
    }
}

/// Get the latest OHLC snapshot for one or more stocks.
///
/// Provides a real-time Open, High, Low, Close for the current day.
/// * Returns a real-time session OHLC from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
/// * Returns a 15-minute delayed session OHLC from the UTP & CTA feeds if the account has the stocks value subscription.
/// * Theta Data resets its snapshot cache at midnight ET every day. This endpoint may not work on a weekend where there were no eligible messages sent over exchange feeds. We recommend using historic requests during the weekend.
///
/// Defaults (upstream):
/// - `venue`: `"nqb"`
#[pyclass(module = "thetadatadx", name = "StockSnapshotOhlcBuilder")]
pub struct StockSnapshotOhlcBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbols: Vec<String>,
    venue: Option<String>,
    min_time: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl StockSnapshotOhlcBuilder {
    /// Set `venue` on the pending request.
    fn venue<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.venue = Some(value.into_string());
        slf
    }

    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbols<'py>(mut slf: PyRefMut<'py, Self>, value: PySymbols) -> PyRefMut<'py, Self> {
        slf.symbols = value.into_vec();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<OhlcTickList>> {
        let client = self.client.clone();
        let symbols = self.symbols.clone();
        let venue = self.venue.clone();
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().stock_snapshot_ohlc(&refs);
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        ohlc_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbols = self.symbols.clone();
        let venue = self.venue.clone();
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().stock_snapshot_ohlc(&refs);
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| ohlc_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Get the latest trade snapshot for one or more stocks.
///
/// Returns a real-time last trade from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
///
/// - Theta Data resets its snapshot cache at midnight ET every day. This endpoint may not work on a weekend where there were no eligible messages sent over exchange feeds. We recommend using historic requests during the weekend.
///
/// Defaults (upstream):
/// - `venue`: `"nqb"`
#[pyclass(module = "thetadatadx", name = "StockSnapshotTradeBuilder")]
pub struct StockSnapshotTradeBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbols: Vec<String>,
    venue: Option<String>,
    min_time: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl StockSnapshotTradeBuilder {
    /// Set `venue` on the pending request.
    fn venue<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.venue = Some(value.into_string());
        slf
    }

    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbols<'py>(mut slf: PyRefMut<'py, Self>, value: PySymbols) -> PyRefMut<'py, Self> {
        slf.symbols = value.into_vec();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<TradeTickList>> {
        let client = self.client.clone();
        let symbols = self.symbols.clone();
        let venue = self.venue.clone();
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().stock_snapshot_trade(&refs);
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        trade_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbols = self.symbols.clone();
        let venue = self.venue.clone();
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().stock_snapshot_trade(&refs);
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Get the latest NBBO quote snapshot for one or more stocks.
///
/// * Returns a real-time last BBO quote from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
/// * Returns a 15-minute delayed NBBO quote from the UTP & CTA feeds account has the stocks value subscription subscription.
/// - Theta Data resets its snapshot cache at midnight ET every day. This endpoint may not work on a weekend where there were no eligible messages sent over exchange feeds. We recommend using historic requests during the weekend.
///
/// Defaults (upstream):
/// - `venue`: `"nqb"`
#[pyclass(module = "thetadatadx", name = "StockSnapshotQuoteBuilder")]
pub struct StockSnapshotQuoteBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbols: Vec<String>,
    venue: Option<String>,
    min_time: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl StockSnapshotQuoteBuilder {
    /// Set `venue` on the pending request.
    fn venue<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.venue = Some(value.into_string());
        slf
    }

    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbols<'py>(mut slf: PyRefMut<'py, Self>, value: PySymbols) -> PyRefMut<'py, Self> {
        slf.symbols = value.into_vec();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<QuoteTickList>> {
        let client = self.client.clone();
        let symbols = self.symbols.clone();
        let venue = self.venue.clone();
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().stock_snapshot_quote(&refs);
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        quote_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbols = self.symbols.clone();
        let venue = self.venue.clone();
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().stock_snapshot_quote(&refs);
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| quote_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Get the latest market value snapshot for one or more stocks.
///
/// * Returns a real-time market value derived from the last BBO quote from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
/// * Returns a 15-minute delayed market value derived from an NBBO quote from the UTP & CTA feeds if the account has the stocks value subscription subscription.
/// - Theta Data resets its snapshot cache at midnight ET every day. This endpoint may not work on a weekend where there were no eligible messages sent over exchange feeds. We recommend using historic requests during the weekend.
///
/// Defaults (upstream):
/// - `venue`: `"nqb"`
#[pyclass(module = "thetadatadx", name = "StockSnapshotMarketValueBuilder")]
pub struct StockSnapshotMarketValueBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbols: Vec<String>,
    venue: Option<String>,
    min_time: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl StockSnapshotMarketValueBuilder {
    /// Set `venue` on the pending request.
    fn venue<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.venue = Some(value.into_string());
        slf
    }

    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbols<'py>(mut slf: PyRefMut<'py, Self>, value: PySymbols) -> PyRefMut<'py, Self> {
        slf.symbols = value.into_vec();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<MarketValueTickList>> {
        let client = self.client.clone();
        let symbols = self.symbols.clone();
        let venue = self.venue.clone();
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().stock_snapshot_market_value(&refs);
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        market_value_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbols = self.symbols.clone();
        let venue = self.venue.clone();
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().stock_snapshot_market_value(&refs);
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| market_value_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Fetch end-of-day stock data for a date range. Returns OHLCV + bid/ask per trading day.
///
/// Since the equity SIPs only generate a partial EOD report, Theta Data generates a national EOD report at 17:15 ET each day. ``created`` represents the datetime the report was generated and ``last_trade`` represents the datetime of the last trade. The quote in the response represents the last NBBO reported by CTA or UTP at the time of report generation. You can read more about EOD & OHLC data here. Theta Data plans to avail SIP EOD reports in the near future.
#[pyclass(module = "thetadatadx", name = "StockHistoryEodBuilder")]
pub struct StockHistoryEodBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    start_date: String,
    end_date: String,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl StockHistoryEodBuilder {
    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = value.into_string();
        slf
    }

    /// Replace the required `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<EodTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().stock_history_eod(&symbol, &start_date, &end_date);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        eod_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_history_eod(&symbol, &start_date, &end_date);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| eod_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `stock_history_eod` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().stock_history_eod(&symbol, &start_date, &end_date);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match eod_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `stock_history_eod` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_history_eod(&symbol, &start_date, &end_date);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match eod_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch intraday OHLC bars for a stock on a single date.
///
/// - Aggregated OHLC bars that use SIP rules for each bar. Time timestamp of the bar represents the opening time of the bar. For a trade to be part of the bar:  ``bar time`` <= ``trade time`` < ``bar timestamp + ivl``, where ivl is the specified interval size in milliseconds. 
/// - Set the ``venue`` parameter to ``nqb`` to access current-day real-time historic data from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
/// - Multi-day requests are limited to 1 month of data.
///
/// Defaults (upstream):
/// - `interval`: `"1s"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
/// - `venue`: `"nqb"`
#[pyclass(module = "thetadatadx", name = "StockHistoryOhlcBuilder")]
pub struct StockHistoryOhlcBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    date: Option<String>,
    interval: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    venue: Option<String>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl StockHistoryOhlcBuilder {
    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `interval` on the pending request.
    fn interval<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.interval = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `venue` on the pending request.
    fn venue<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.venue = Some(value.into_string());
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<OhlcTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let venue = self.venue.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().stock_history_ohlc(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        ohlc_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let venue = self.venue.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_history_ohlc(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| ohlc_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `stock_history_ohlc` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let venue = self.venue.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().stock_history_ohlc(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match ohlc_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `stock_history_ohlc` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let venue = self.venue.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_history_ohlc(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match ohlc_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch all trades for a stock on a given date.
///
/// Returns every trade reported by UTP & CTA. Set the ``venue`` parameter to ``nqb`` to access current-day real-time historic data from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
/// - Multi-day requests are limited to 1 month of data.
///
/// Defaults (upstream):
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
/// - `venue`: `"nqb"`
#[pyclass(module = "thetadatadx", name = "StockHistoryTradeBuilder")]
pub struct StockHistoryTradeBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    date: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    venue: Option<String>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl StockHistoryTradeBuilder {
    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `venue` on the pending request.
    fn venue<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.venue = Some(value.into_string());
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<TradeTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let venue = self.venue.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().stock_history_trade(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        trade_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let venue = self.venue.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_history_trade(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `stock_history_trade` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let venue = self.venue.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().stock_history_trade(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match trade_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `stock_history_trade` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let venue = self.venue.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_history_trade(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match trade_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch NBBO quotes for a stock on a given date at a given interval.
///
/// - Returns every NBBO quote reported by UTP and CTA. 
/// - If the ``interval`` parameter is specified, the quote for each interval represents the last quote prior to the interval's timestamp. 
/// - Set the ``venue`` parameter to ``nqb`` to access current-day real-time historic data from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
/// - Multi-day requests are limited to 1 month of data.
///
/// Defaults (upstream):
/// - `interval`: `"1s"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
/// - `venue`: `"nqb"`
#[pyclass(module = "thetadatadx", name = "StockHistoryQuoteBuilder")]
pub struct StockHistoryQuoteBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    date: Option<String>,
    interval: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    venue: Option<String>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl StockHistoryQuoteBuilder {
    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `interval` on the pending request.
    fn interval<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.interval = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `venue` on the pending request.
    fn venue<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.venue = Some(value.into_string());
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<QuoteTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let venue = self.venue.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().stock_history_quote(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        quote_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let venue = self.venue.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_history_quote(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| quote_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `stock_history_quote` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let venue = self.venue.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().stock_history_quote(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match quote_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `stock_history_quote` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let venue = self.venue.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_history_quote(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match quote_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch combined trade + quote ticks for a stock on a given date. Returns raw DataTable.
///
/// Returns every trade reported by UTP & CTA paired with the last BBO quote reported by UTP or CTA at the time of trade. A quote is matched with a trade if its timestamp ``<=`` the trade timestamp. If you prefer to match quotes with timestamps that are ``<`` the trade timestamp, specify the ``exclusive`` parameter to ``true``. Set the ``venue`` parameter to ``nqb`` to access current-day real-time historic data from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
/// - Multi-day requests are limited to 1 month of data.
///
/// Defaults (upstream):
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
/// - `exclusive`: `true`
/// - `venue`: `"nqb"`
#[pyclass(module = "thetadatadx", name = "StockHistoryTradeQuoteBuilder")]
pub struct StockHistoryTradeQuoteBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    date: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    exclusive: Option<bool>,
    venue: Option<String>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl StockHistoryTradeQuoteBuilder {
    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `exclusive` on the pending request.
    fn exclusive<'py>(mut slf: PyRefMut<'py, Self>, value: bool) -> PyRefMut<'py, Self> {
        slf.exclusive = Some(value);
        slf
    }

    /// Set `venue` on the pending request.
    fn venue<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.venue = Some(value.into_string());
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<TradeQuoteTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let exclusive = self.exclusive;
        let venue = self.venue.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().stock_history_trade_quote(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &exclusive {
                request = request.exclusive(*value);
            }
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        trade_quote_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let exclusive = self.exclusive;
        let venue = self.venue.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_history_trade_quote(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &exclusive {
                request = request.exclusive(*value);
            }
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_quote_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `stock_history_trade_quote` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let exclusive = self.exclusive;
        let venue = self.venue.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().stock_history_trade_quote(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &exclusive {
                request = request.exclusive(*value);
            }
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match trade_quote_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `stock_history_trade_quote` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let exclusive = self.exclusive;
        let venue = self.venue.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_history_trade_quote(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &exclusive {
                request = request.exclusive(*value);
            }
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match trade_quote_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch the trade at a specific time of day across a date range.
///
/// #### Real-time request:
/// - Returns a real-time session from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
/// - Returns a 15-minute delayed session from the UTP & CTA feeds account has the stocks value subscription subscription.
///
/// #### Historical request:
/// Returns the last trade reported by UTP & CTA feeds at a specified millisecond of the day.
/// Trade condition mappings can be found here.
///
/// Defaults (upstream):
/// - `venue`: `"nqb"`
#[pyclass(module = "thetadatadx", name = "StockAtTimeTradeBuilder")]
pub struct StockAtTimeTradeBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    start_date: String,
    end_date: String,
    time_of_day: String,
    venue: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl StockAtTimeTradeBuilder {
    /// Set `venue` on the pending request.
    fn venue<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.venue = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = value.into_string();
        slf
    }

    /// Replace the required `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = value.into_string();
        slf
    }

    /// Replace the required `time_of_day` on the pending request.
    fn time_of_day<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.time_of_day = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<TradeTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let venue = self.venue.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().stock_at_time_trade(&symbol, &start_date, &end_date, &time_of_day);
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        trade_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let venue = self.venue.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_at_time_trade(&symbol, &start_date, &end_date, &time_of_day);
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `stock_at_time_trade` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let venue = self.venue.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().stock_at_time_trade(&symbol, &start_date, &end_date, &time_of_day);
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match trade_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `stock_at_time_trade` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let venue = self.venue.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_at_time_trade(&symbol, &start_date, &end_date, &time_of_day);
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match trade_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch the quote at a specific time of day across a date range.
///
/// #### Real-time request:
///   - Subscription tier standard or higher will default to NQB.
///   - Real-time last BBO quote at-time_of_day-time from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
///   - 15-minute delayed NBBO quote at-time_of_day-time from the UTP & CTA feeds account has the stocks value subscription subscription.
///
/// #### Historical request:
///   Returns the last NBBO quote reported by UTP & CTA feeds at a specified millisecond of the day.
///
/// Defaults (upstream):
/// - `venue`: `"nqb"`
#[pyclass(module = "thetadatadx", name = "StockAtTimeQuoteBuilder")]
pub struct StockAtTimeQuoteBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    start_date: String,
    end_date: String,
    time_of_day: String,
    venue: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl StockAtTimeQuoteBuilder {
    /// Set `venue` on the pending request.
    fn venue<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.venue = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = value.into_string();
        slf
    }

    /// Replace the required `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = value.into_string();
        slf
    }

    /// Replace the required `time_of_day` on the pending request.
    fn time_of_day<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.time_of_day = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<QuoteTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let venue = self.venue.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().stock_at_time_quote(&symbol, &start_date, &end_date, &time_of_day);
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        quote_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let venue = self.venue.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_at_time_quote(&symbol, &start_date, &end_date, &time_of_day);
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| quote_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `stock_at_time_quote` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let venue = self.venue.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().stock_at_time_quote(&symbol, &start_date, &end_date, &time_of_day);
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match quote_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `stock_at_time_quote` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let venue = self.venue.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_at_time_quote(&symbol, &start_date, &end_date, &time_of_day);
            if let Some(value) = &venue {
                request = request.venue(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match quote_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// List all available option underlying symbols.
///
/// A symbol can be defined as a unique identifier for a stock / underlying asset. Common terms also include: root, ticker, and underlying. This endpoint returns all traded symbols for options. This endpoint is updated overnight.
#[pyclass(module = "thetadatadx", name = "OptionListSymbolsBuilder")]
pub struct OptionListSymbolsBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionListSymbolsBuilder {
    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed `StringList` wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<StringList>> {
        let client = self.client.clone();
        let timeout_ms = self.timeout_ms;
        let values: Vec<String> = run_blocking(py, async move {
            let call = client.market_data().option_list_symbols();
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        })?;
        strings_to_string_list(py, values, "symbol")
    }

    /// Async companion to `list()` — awaitable yields the `StringList` wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let call = client.market_data().option_list_symbols();
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        }, |py, values| strings_to_string_list(py, values, "symbol").map(|p| p.into_any()))
    }
}

/// List available dates for an option contract by request type.
///
/// Lists all dates of data that are available for an option with a given symbol, request type, and expiration.
/// This endpoint is updated overnight.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
#[pyclass(module = "thetadatadx", name = "OptionListDatesBuilder")]
pub struct OptionListDatesBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    request_type: String,
    symbol: String,
    expiration: String,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionListDatesBuilder {
    /// Replace the required `request_type` on the pending request.
    fn request_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.request_type = value.into_string();
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed `StringList` wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<StringList>> {
        let client = self.client.clone();
        let timeout_ms = self.timeout_ms;
        let request_type = self.request_type.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let values: Vec<String> = run_blocking(py, async move {
            let call = client.market_data().option_list_dates(&request_type, &symbol, &expiration);
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        })?;
        strings_to_string_list(py, values, "date")
    }

    /// Async companion to `list()` — awaitable yields the `StringList` wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let request_type = self.request_type.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let call = client.market_data().option_list_dates(&request_type, &symbol, &expiration);
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        }, |py, values| strings_to_string_list(py, values, "date").map(|p| p.into_any()))
    }
}

/// List available expiration dates for an option underlying.
///
/// Lists all dates of expirations that are available for an option with a given symbol.
/// This endpoint is updated overnight.
#[pyclass(module = "thetadatadx", name = "OptionListExpirationsBuilder")]
pub struct OptionListExpirationsBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionListExpirationsBuilder {
    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed `StringList` wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<StringList>> {
        let client = self.client.clone();
        let timeout_ms = self.timeout_ms;
        let symbol = self.symbol.clone();
        let values: Vec<String> = run_blocking(py, async move {
            let call = client.market_data().option_list_expirations(&symbol);
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        })?;
        strings_to_string_list(py, values, "expiration")
    }

    /// Async companion to `list()` — awaitable yields the `StringList` wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let call = client.market_data().option_list_expirations(&symbol);
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        }, |py, values| strings_to_string_list(py, values, "expiration").map(|p| p.into_any()))
    }
}

/// List available strike prices for an option at a given expiration.
///
/// Lists all strikes that are available for an option with a given symbol and expiration date.
/// This endpoint is updated overnight.
#[pyclass(module = "thetadatadx", name = "OptionListStrikesBuilder")]
pub struct OptionListStrikesBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionListStrikesBuilder {
    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed `StringList` wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<StringList>> {
        let client = self.client.clone();
        let timeout_ms = self.timeout_ms;
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let values: Vec<String> = run_blocking(py, async move {
            let call = client.market_data().option_list_strikes(&symbol, &expiration);
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        })?;
        strings_to_string_list(py, values, "strike")
    }

    /// Async companion to `list()` — awaitable yields the `StringList` wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let call = client.market_data().option_list_strikes(&symbol, &expiration);
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        }, |py, values| strings_to_string_list(py, values, "strike").map(|p| p.into_any()))
    }
}

/// List all option contracts traded or quoted on a given date, optionally filtered to a symbol.
///
/// Lists all contracts that were traded or quoted on a particular date.
///
/// If the ``symbol`` parameter is specified, the returned contracts will be filtered to match the symbol.
/// When ``symbol`` is omitted the full universe of contracts for that date is returned.
/// This endpoint is updated real-time.
#[pyclass(module = "thetadatadx", name = "OptionListContractsBuilder")]
pub struct OptionListContractsBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    request_type: String,
    date: String,
    symbol: Option<String>,
    max_dte: Option<i32>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionListContractsBuilder {
    /// Set `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Replace the required `request_type` on the pending request.
    fn request_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.request_type = value.into_string();
        slf
    }

    /// Replace the required `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<OptionContractList>> {
        let client = self.client.clone();
        let request_type = self.request_type.clone();
        let date = self.date.clone();
        let symbol = self.symbol.clone();
        let max_dte = self.max_dte;
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_list_contracts(&request_type, &date);
            if let Some(value) = &symbol {
                request = request.symbol(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        option_contracts_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let request_type = self.request_type.clone();
        let date = self.date.clone();
        let symbol = self.symbol.clone();
        let max_dte = self.max_dte;
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_list_contracts(&request_type, &date);
            if let Some(value) = &symbol {
                request = request.symbol(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| option_contracts_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_list_contracts` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let request_type = self.request_type.clone();
        let date = self.date.clone();
        let symbol = self.symbol.clone();
        let max_dte = self.max_dte;
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_list_contracts(&request_type, &date);
            if let Some(value) = &symbol {
                request = request.symbol(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match option_contracts_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_list_contracts` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let request_type = self.request_type.clone();
        let date = self.date.clone();
        let symbol = self.symbol.clone();
        let max_dte = self.max_dte;
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_list_contracts(&request_type, &date);
            if let Some(value) = &symbol {
                request = request.symbol(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match option_contracts_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Get the latest OHLC snapshot for an option contract.
///
/// - Retrieve a real-time last ohlc of an option contract for the trading day.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
#[pyclass(module = "thetadatadx", name = "OptionSnapshotOhlcBuilder")]
pub struct OptionSnapshotOhlcBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    min_time: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionSnapshotOhlcBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<OhlcTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_snapshot_ohlc(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        ohlc_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_ohlc(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| ohlc_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Get the latest trade snapshot for an option contract.
///
/// - Retrieve the real-time last trade of an option contract.
/// - This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
#[pyclass(module = "thetadatadx", name = "OptionSnapshotTradeBuilder")]
pub struct OptionSnapshotTradeBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    strike_range: Option<i32>,
    min_time: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionSnapshotTradeBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<TradeTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_snapshot_trade(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        trade_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_trade(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Get the latest NBBO quote snapshot for an option contract.
///
/// - Retrieve a real-time last NBBO quote of an option contract.
/// - This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
#[pyclass(module = "thetadatadx", name = "OptionSnapshotQuoteBuilder")]
pub struct OptionSnapshotQuoteBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    min_time: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionSnapshotQuoteBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<QuoteTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_snapshot_quote(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        quote_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_quote(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| quote_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Get the latest open interest snapshot for an option contract.
///
/// - Retrieve the last open interest message of an option contract.
/// - Open interest is reported around 06:30 ET every morning by OPRA and reflects the open interest at the end of the previous trading day.
/// - This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
#[pyclass(module = "thetadatadx", name = "OptionSnapshotOpenInterestBuilder")]
pub struct OptionSnapshotOpenInterestBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    min_time: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionSnapshotOpenInterestBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<OpenInterestTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_snapshot_open_interest(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        open_interest_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_open_interest(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| open_interest_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Get the latest market value snapshot for an option contract.
///
/// * Returns a real-time market value derived from the last NBBO quote of an option contract.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
#[pyclass(module = "thetadatadx", name = "OptionSnapshotMarketValueBuilder")]
pub struct OptionSnapshotMarketValueBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    min_time: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionSnapshotMarketValueBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<MarketValueTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_snapshot_market_value(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        market_value_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_market_value(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| market_value_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Get implied volatility snapshot for an option contract (from ThetaData server).
///
/// Returns implied volatilies calculated using the national best bid, mid, and ask price
/// of the option respectively. The underlying price represents whatever the last underlying price was at the
/// ``underlying_timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `rate_type`: `"sofr"`
/// - `version`: `"latest"`
/// - `use_market_value`: `false`
#[pyclass(module = "thetadatadx", name = "OptionSnapshotGreeksImpliedVolatilityBuilder")]
pub struct OptionSnapshotGreeksImpliedVolatilityBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    annual_dividend: Option<f64>,
    rate_type: Option<String>,
    rate_value: Option<f64>,
    stock_price: Option<f64>,
    version: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    min_time: Option<String>,
    use_market_value: Option<bool>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionSnapshotGreeksImpliedVolatilityBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `annual_dividend` on the pending request.
    fn annual_dividend<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.annual_dividend = Some(value);
        slf
    }

    /// Set `rate_type` on the pending request.
    fn rate_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.rate_type = Some(value.into_string());
        slf
    }

    /// Set `rate_value` on the pending request.
    fn rate_value<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.rate_value = Some(value);
        slf
    }

    /// Set `stock_price` on the pending request.
    fn stock_price<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.stock_price = Some(value);
        slf
    }

    /// Set `version` on the pending request.
    fn version<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.version = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Set `use_market_value` on the pending request.
    fn use_market_value<'py>(mut slf: PyRefMut<'py, Self>, value: bool) -> PyRefMut<'py, Self> {
        slf.use_market_value = Some(value);
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<IvTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let stock_price = self.stock_price;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let use_market_value = self.use_market_value;
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_snapshot_greeks_implied_volatility(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &stock_price {
                request = request.stock_price(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(value) = &use_market_value {
                request = request.use_market_value(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        iv_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let stock_price = self.stock_price;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let use_market_value = self.use_market_value;
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_greeks_implied_volatility(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &stock_price {
                request = request.stock_price(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(value) = &use_market_value {
                request = request.use_market_value(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| iv_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Get all Greeks snapshot for an option contract (from ThetaData server).
///
/// - Retrieve a real-time last greeks calculation for all option contracts that lie on a provided expiration.
/// > This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `rate_type`: `"sofr"`
/// - `version`: `"latest"`
/// - `use_market_value`: `false`
#[pyclass(module = "thetadatadx", name = "OptionSnapshotGreeksAllBuilder")]
pub struct OptionSnapshotGreeksAllBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    annual_dividend: Option<f64>,
    rate_type: Option<String>,
    rate_value: Option<f64>,
    stock_price: Option<f64>,
    version: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    min_time: Option<String>,
    use_market_value: Option<bool>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionSnapshotGreeksAllBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `annual_dividend` on the pending request.
    fn annual_dividend<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.annual_dividend = Some(value);
        slf
    }

    /// Set `rate_type` on the pending request.
    fn rate_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.rate_type = Some(value.into_string());
        slf
    }

    /// Set `rate_value` on the pending request.
    fn rate_value<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.rate_value = Some(value);
        slf
    }

    /// Set `stock_price` on the pending request.
    fn stock_price<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.stock_price = Some(value);
        slf
    }

    /// Set `version` on the pending request.
    fn version<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.version = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Set `use_market_value` on the pending request.
    fn use_market_value<'py>(mut slf: PyRefMut<'py, Self>, value: bool) -> PyRefMut<'py, Self> {
        slf.use_market_value = Some(value);
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<GreeksAllTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let stock_price = self.stock_price;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let use_market_value = self.use_market_value;
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_snapshot_greeks_all(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &stock_price {
                request = request.stock_price(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(value) = &use_market_value {
                request = request.use_market_value(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        greeks_all_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let stock_price = self.stock_price;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let use_market_value = self.use_market_value;
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_greeks_all(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &stock_price {
                request = request.stock_price(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(value) = &use_market_value {
                request = request.use_market_value(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_all_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Get first-order Greeks snapshot (delta, theta, rho) for an option contract.
///
/// - Retrieve a real-time last greeks calculation for all option contracts that lie on a provided expiration.
/// > This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `rate_type`: `"sofr"`
/// - `version`: `"latest"`
/// - `use_market_value`: `false`
#[pyclass(module = "thetadatadx", name = "OptionSnapshotGreeksFirstOrderBuilder")]
pub struct OptionSnapshotGreeksFirstOrderBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    annual_dividend: Option<f64>,
    rate_type: Option<String>,
    rate_value: Option<f64>,
    stock_price: Option<f64>,
    version: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    min_time: Option<String>,
    use_market_value: Option<bool>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionSnapshotGreeksFirstOrderBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `annual_dividend` on the pending request.
    fn annual_dividend<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.annual_dividend = Some(value);
        slf
    }

    /// Set `rate_type` on the pending request.
    fn rate_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.rate_type = Some(value.into_string());
        slf
    }

    /// Set `rate_value` on the pending request.
    fn rate_value<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.rate_value = Some(value);
        slf
    }

    /// Set `stock_price` on the pending request.
    fn stock_price<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.stock_price = Some(value);
        slf
    }

    /// Set `version` on the pending request.
    fn version<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.version = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Set `use_market_value` on the pending request.
    fn use_market_value<'py>(mut slf: PyRefMut<'py, Self>, value: bool) -> PyRefMut<'py, Self> {
        slf.use_market_value = Some(value);
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<GreeksFirstOrderTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let stock_price = self.stock_price;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let use_market_value = self.use_market_value;
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_snapshot_greeks_first_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &stock_price {
                request = request.stock_price(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(value) = &use_market_value {
                request = request.use_market_value(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        greeks_first_order_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let stock_price = self.stock_price;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let use_market_value = self.use_market_value;
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_greeks_first_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &stock_price {
                request = request.stock_price(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(value) = &use_market_value {
                request = request.use_market_value(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_first_order_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Get second-order Greeks snapshot (gamma, vanna, charm) for an option contract.
///
/// - Retrieve a real-time last second order greeks calculation for all option contracts that lie on a provided expiration.
/// > This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `rate_type`: `"sofr"`
/// - `version`: `"latest"`
/// - `use_market_value`: `false`
#[pyclass(module = "thetadatadx", name = "OptionSnapshotGreeksSecondOrderBuilder")]
pub struct OptionSnapshotGreeksSecondOrderBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    annual_dividend: Option<f64>,
    rate_type: Option<String>,
    rate_value: Option<f64>,
    stock_price: Option<f64>,
    version: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    min_time: Option<String>,
    use_market_value: Option<bool>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionSnapshotGreeksSecondOrderBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `annual_dividend` on the pending request.
    fn annual_dividend<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.annual_dividend = Some(value);
        slf
    }

    /// Set `rate_type` on the pending request.
    fn rate_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.rate_type = Some(value.into_string());
        slf
    }

    /// Set `rate_value` on the pending request.
    fn rate_value<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.rate_value = Some(value);
        slf
    }

    /// Set `stock_price` on the pending request.
    fn stock_price<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.stock_price = Some(value);
        slf
    }

    /// Set `version` on the pending request.
    fn version<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.version = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Set `use_market_value` on the pending request.
    fn use_market_value<'py>(mut slf: PyRefMut<'py, Self>, value: bool) -> PyRefMut<'py, Self> {
        slf.use_market_value = Some(value);
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<GreeksSecondOrderTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let stock_price = self.stock_price;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let use_market_value = self.use_market_value;
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_snapshot_greeks_second_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &stock_price {
                request = request.stock_price(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(value) = &use_market_value {
                request = request.use_market_value(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        greeks_second_order_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let stock_price = self.stock_price;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let use_market_value = self.use_market_value;
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_greeks_second_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &stock_price {
                request = request.stock_price(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(value) = &use_market_value {
                request = request.use_market_value(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_second_order_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Get third-order Greeks snapshot (speed, color, ultima) for an option contract.
///
/// - Retrieve a real-time last third order greeks calculation for all option contracts that lie on a provided expiration.
/// > This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `rate_type`: `"sofr"`
/// - `version`: `"latest"`
/// - `use_market_value`: `false`
#[pyclass(module = "thetadatadx", name = "OptionSnapshotGreeksThirdOrderBuilder")]
pub struct OptionSnapshotGreeksThirdOrderBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    annual_dividend: Option<f64>,
    rate_type: Option<String>,
    rate_value: Option<f64>,
    stock_price: Option<f64>,
    version: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    min_time: Option<String>,
    use_market_value: Option<bool>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionSnapshotGreeksThirdOrderBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `annual_dividend` on the pending request.
    fn annual_dividend<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.annual_dividend = Some(value);
        slf
    }

    /// Set `rate_type` on the pending request.
    fn rate_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.rate_type = Some(value.into_string());
        slf
    }

    /// Set `rate_value` on the pending request.
    fn rate_value<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.rate_value = Some(value);
        slf
    }

    /// Set `stock_price` on the pending request.
    fn stock_price<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.stock_price = Some(value);
        slf
    }

    /// Set `version` on the pending request.
    fn version<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.version = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Set `use_market_value` on the pending request.
    fn use_market_value<'py>(mut slf: PyRefMut<'py, Self>, value: bool) -> PyRefMut<'py, Self> {
        slf.use_market_value = Some(value);
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<GreeksThirdOrderTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let stock_price = self.stock_price;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let use_market_value = self.use_market_value;
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_snapshot_greeks_third_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &stock_price {
                request = request.stock_price(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(value) = &use_market_value {
                request = request.use_market_value(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        greeks_third_order_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let stock_price = self.stock_price;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let min_time = self.min_time.clone();
        let use_market_value = self.use_market_value;
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_greeks_third_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &stock_price {
                request = request.stock_price(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(value) = &use_market_value {
                request = request.use_market_value(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_third_order_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Fetch end-of-day option data for a contract over a date range.
///
/// - Since OPRA does not provide a national EOD report for options, Theta Data generates a national EOD report at 17:15 ET each day.
/// - ``created`` represents the datetime the report was generated and ``last_trade`` represents the datetime of the last trade. 
/// - The quote in the response represents the last NBBO reported by OPRA at the time of report generation. 
/// - You can read more about EOD & OHLC data here.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
#[pyclass(module = "thetadatadx", name = "OptionHistoryEodBuilder")]
pub struct OptionHistoryEodBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    start_date: String,
    end_date: String,
    strike: Option<String>,
    right: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryEodBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Replace the required `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = value.into_string();
        slf
    }

    /// Replace the required `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<EodTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_eod(&symbol, &expiration, &start_date, &end_date);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        eod_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_eod(&symbol, &expiration, &start_date, &end_date);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| eod_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_eod` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_eod(&symbol, &expiration, &start_date, &end_date);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match eod_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_eod` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_eod(&symbol, &expiration, &start_date, &end_date);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match eod_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch intraday OHLC bars for an option contract.
///
/// - Aggregated OHLC bars that use SIP rules for each bar. 
/// - Time timestamp of the bar represents the opening time of the bar. For a trade to be part of the bar:  ``bar timestamp`` <= ``trade time`` < ``bar timestamp + interval``.
/// - Multi-day requests are limited to 1 month of data.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `interval`: `"1s"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
#[pyclass(module = "thetadatadx", name = "OptionHistoryOhlcBuilder")]
pub struct OptionHistoryOhlcBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    date: Option<String>,
    interval: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    strike_range: Option<i32>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryOhlcBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `interval` on the pending request.
    fn interval<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.interval = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<OhlcTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_ohlc(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        ohlc_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_ohlc(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| ohlc_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_ohlc` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_ohlc(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match ohlc_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_ohlc` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_ohlc(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match ohlc_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch all trades for an option contract on a given date.
///
/// - Returns every trade reported by OPRA. 
/// - Trade condition mappings can be found here.
/// - Extended trade conditions are not reported by OPRA for options, so they can be ignored.
/// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
#[pyclass(module = "thetadatadx", name = "OptionHistoryTradeBuilder")]
pub struct OptionHistoryTradeBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    date: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryTradeBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<TradeTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_trade(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        trade_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_trade` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_trade(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match trade_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_trade` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match trade_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch NBBO quotes for an option contract on a given date.
///
/// - Returns every NBBO quote reported by OPRA. 
/// - If the ``interval`` parameter is specified, the quote for each interval represents the last quote at the interval's timestamp.
/// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `interval`: `"1s"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
#[pyclass(module = "thetadatadx", name = "OptionHistoryQuoteBuilder")]
pub struct OptionHistoryQuoteBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    date: Option<String>,
    interval: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryQuoteBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `interval` on the pending request.
    fn interval<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.interval = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<QuoteTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_quote(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        quote_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_quote(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| quote_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_quote` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_quote(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match quote_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_quote` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_quote(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match quote_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch combined trade + quote ticks for an option contract.
///
/// - Returns every trade reported by OPRA paired with the last NBBO quote reported by OPRA at the time of trade.
/// - A quote is matched with a trade if its timestamp ``<=`` the trade timestamp. 
/// - To match trades with quotes timestamps that are ``<`` the trade timestamp, specify the ``exclusive``parameter to ``true``. After thorough testing, we have determined that using ``exclusive=true`` might yield better results for various applications.
/// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
/// - `exclusive`: `true`
#[pyclass(module = "thetadatadx", name = "OptionHistoryTradeQuoteBuilder")]
pub struct OptionHistoryTradeQuoteBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    date: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    exclusive: Option<bool>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryTradeQuoteBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `exclusive` on the pending request.
    fn exclusive<'py>(mut slf: PyRefMut<'py, Self>, value: bool) -> PyRefMut<'py, Self> {
        slf.exclusive = Some(value);
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<TradeQuoteTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let exclusive = self.exclusive;
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_trade_quote(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &exclusive {
                request = request.exclusive(*value);
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        trade_quote_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let exclusive = self.exclusive;
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_quote(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &exclusive {
                request = request.exclusive(*value);
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_quote_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_trade_quote` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let exclusive = self.exclusive;
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_trade_quote(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &exclusive {
                request = request.exclusive(*value);
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match trade_quote_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_trade_quote` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let exclusive = self.exclusive;
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_quote(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &exclusive {
                request = request.exclusive(*value);
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match trade_quote_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch open interest history for an option contract.
///
/// - Open Interest is normally reported once per day by OPRA at approximately 06:30 ET.
/// - A new open interest message might not be sent by OPRA if there is no open interest for the option contract.
/// - The reported open interest represents the open interest at the end of the previous trading day.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
#[pyclass(module = "thetadatadx", name = "OptionHistoryOpenInterestBuilder")]
pub struct OptionHistoryOpenInterestBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    date: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryOpenInterestBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<OpenInterestTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_open_interest(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        open_interest_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_open_interest(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| open_interest_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_open_interest` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_open_interest(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match open_interest_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_open_interest` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_open_interest(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match open_interest_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch end-of-day Greeks history for an option contract.
///
/// - Returns the data for all contracts that share the same provided symbol and expiration.
/// - Uses Theta Data's EOD reports that get generated at 17:15 ET each day. The closing option price and closing underlying price are used for the greeks calculation.
/// - **Any ``expiration=*`` request must be made day by day.**
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `rate_type`: `"sofr"`
/// - `version`: `"latest"`
/// - `underlyer_use_nbbo`: `false`
#[pyclass(module = "thetadatadx", name = "OptionHistoryGreeksEodBuilder")]
pub struct OptionHistoryGreeksEodBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    start_date: String,
    end_date: String,
    strike: Option<String>,
    right: Option<String>,
    annual_dividend: Option<f64>,
    rate_type: Option<String>,
    rate_value: Option<f64>,
    version: Option<String>,
    underlyer_use_nbbo: Option<bool>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryGreeksEodBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `annual_dividend` on the pending request.
    fn annual_dividend<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.annual_dividend = Some(value);
        slf
    }

    /// Set `rate_type` on the pending request.
    fn rate_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.rate_type = Some(value.into_string());
        slf
    }

    /// Set `rate_value` on the pending request.
    fn rate_value<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.rate_value = Some(value);
        slf
    }

    /// Set `version` on the pending request.
    fn version<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.version = Some(value.into_string());
        slf
    }

    /// Set `underlyer_use_nbbo` on the pending request.
    fn underlyer_use_nbbo<'py>(mut slf: PyRefMut<'py, Self>, value: bool) -> PyRefMut<'py, Self> {
        slf.underlyer_use_nbbo = Some(value);
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Replace the required `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = value.into_string();
        slf
    }

    /// Replace the required `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<GreeksEodTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let underlyer_use_nbbo = self.underlyer_use_nbbo;
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_greeks_eod(&symbol, &expiration, &start_date, &end_date);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &underlyer_use_nbbo {
                request = request.underlyer_use_nbbo(*value);
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        greeks_eod_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let underlyer_use_nbbo = self.underlyer_use_nbbo;
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_eod(&symbol, &expiration, &start_date, &end_date);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &underlyer_use_nbbo {
                request = request.underlyer_use_nbbo(*value);
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_eod_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_greeks_eod` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let underlyer_use_nbbo = self.underlyer_use_nbbo;
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_greeks_eod(&symbol, &expiration, &start_date, &end_date);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &underlyer_use_nbbo {
                request = request.underlyer_use_nbbo(*value);
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match greeks_eod_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_greeks_eod` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let underlyer_use_nbbo = self.underlyer_use_nbbo;
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_eod(&symbol, &expiration, &start_date, &end_date);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &underlyer_use_nbbo {
                request = request.underlyer_use_nbbo(*value);
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match greeks_eod_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch all Greeks history for an option contract (intraday, sampled by interval).
///
/// - Returns the data for all contracts that share the same provided symbol and expiration. 
/// - Calculated using the option and underlying midpoint price. If an interval size is specified (*highly recommended*), the option quote used in the calculation follows the same rules as the quote endpoint. 
/// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
/// - Multi-day requests are limited to 1 month of data.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `interval`: `"1s"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
/// - `rate_type`: `"sofr"`
/// - `version`: `"latest"`
#[pyclass(module = "thetadatadx", name = "OptionHistoryGreeksAllBuilder")]
pub struct OptionHistoryGreeksAllBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    date: Option<String>,
    interval: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    annual_dividend: Option<f64>,
    rate_type: Option<String>,
    rate_value: Option<f64>,
    version: Option<String>,
    strike_range: Option<i32>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryGreeksAllBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `interval` on the pending request.
    fn interval<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.interval = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `annual_dividend` on the pending request.
    fn annual_dividend<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.annual_dividend = Some(value);
        slf
    }

    /// Set `rate_type` on the pending request.
    fn rate_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.rate_type = Some(value.into_string());
        slf
    }

    /// Set `rate_value` on the pending request.
    fn rate_value<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.rate_value = Some(value);
        slf
    }

    /// Set `version` on the pending request.
    fn version<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.version = Some(value.into_string());
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<GreeksAllTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_greeks_all(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        greeks_all_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_all(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_all_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_greeks_all` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_greeks_all(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match greeks_all_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_greeks_all` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_all(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match greeks_all_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch all Greeks on each trade for an option contract.
///
/// - Returns the data for all contracts that share the same provided symbol and expiration. 
/// - Calculates greeks for every trade reported by OPRA.
/// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
/// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
/// - `rate_type`: `"sofr"`
/// - `version`: `"latest"`
#[pyclass(module = "thetadatadx", name = "OptionHistoryTradeGreeksAllBuilder")]
pub struct OptionHistoryTradeGreeksAllBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    date: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    annual_dividend: Option<f64>,
    rate_type: Option<String>,
    rate_value: Option<f64>,
    version: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryTradeGreeksAllBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `annual_dividend` on the pending request.
    fn annual_dividend<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.annual_dividend = Some(value);
        slf
    }

    /// Set `rate_type` on the pending request.
    fn rate_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.rate_type = Some(value.into_string());
        slf
    }

    /// Set `rate_value` on the pending request.
    fn rate_value<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.rate_value = Some(value);
        slf
    }

    /// Set `version` on the pending request.
    fn version<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.version = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<TradeGreeksAllTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_all(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        trade_greeks_all_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_all(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_greeks_all_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_trade_greeks_all` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_all(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match trade_greeks_all_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_trade_greeks_all` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_all(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match trade_greeks_all_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch first-order Greeks history (intraday, sampled by interval).
///
/// - Returns the data for all contracts that share the same provided symbol and expiration. 
/// - Calculated using the option and underlying midpoint price. If an interval size is specified (*highly recommended*), the option quote used in the calculation follows the same rules as the quote endpoint. 
/// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
/// - Multi-day requests are limited to 1 month of data.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `interval`: `"1s"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
/// - `rate_type`: `"sofr"`
/// - `version`: `"latest"`
#[pyclass(module = "thetadatadx", name = "OptionHistoryGreeksFirstOrderBuilder")]
pub struct OptionHistoryGreeksFirstOrderBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    date: Option<String>,
    interval: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    annual_dividend: Option<f64>,
    rate_type: Option<String>,
    rate_value: Option<f64>,
    version: Option<String>,
    strike_range: Option<i32>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryGreeksFirstOrderBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `interval` on the pending request.
    fn interval<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.interval = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `annual_dividend` on the pending request.
    fn annual_dividend<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.annual_dividend = Some(value);
        slf
    }

    /// Set `rate_type` on the pending request.
    fn rate_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.rate_type = Some(value.into_string());
        slf
    }

    /// Set `rate_value` on the pending request.
    fn rate_value<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.rate_value = Some(value);
        slf
    }

    /// Set `version` on the pending request.
    fn version<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.version = Some(value.into_string());
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<GreeksFirstOrderTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_greeks_first_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        greeks_first_order_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_first_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_first_order_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_greeks_first_order` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_greeks_first_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match greeks_first_order_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_greeks_first_order` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_first_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match greeks_first_order_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch first-order Greeks on each trade for an option contract.
///
/// - Returns the data for all contracts that share the same provided symbol and expiration.
/// - Calculates greeks for every trade reported by OPRA.
/// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
/// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
/// - `rate_type`: `"sofr"`
/// - `version`: `"latest"`
#[pyclass(module = "thetadatadx", name = "OptionHistoryTradeGreeksFirstOrderBuilder")]
pub struct OptionHistoryTradeGreeksFirstOrderBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    date: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    annual_dividend: Option<f64>,
    rate_type: Option<String>,
    rate_value: Option<f64>,
    version: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryTradeGreeksFirstOrderBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `annual_dividend` on the pending request.
    fn annual_dividend<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.annual_dividend = Some(value);
        slf
    }

    /// Set `rate_type` on the pending request.
    fn rate_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.rate_type = Some(value.into_string());
        slf
    }

    /// Set `rate_value` on the pending request.
    fn rate_value<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.rate_value = Some(value);
        slf
    }

    /// Set `version` on the pending request.
    fn version<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.version = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<TradeGreeksFirstOrderTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_first_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        trade_greeks_first_order_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_first_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_greeks_first_order_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_trade_greeks_first_order` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_first_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match trade_greeks_first_order_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_trade_greeks_first_order` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_first_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match trade_greeks_first_order_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch second-order Greeks history (intraday, sampled by interval).
///
/// - Returns the data for all contracts that share the same provided symbol and expiration. 
/// - Calculated using the option and underlying midpoint price. If an interval size is specified (*highly recommended*), the option quote used in the calculation follows the same rules as the quote endpoint. 
/// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
/// - Multi-day requests are limited to 1 month of data.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `interval`: `"1s"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
/// - `rate_type`: `"sofr"`
/// - `version`: `"latest"`
#[pyclass(module = "thetadatadx", name = "OptionHistoryGreeksSecondOrderBuilder")]
pub struct OptionHistoryGreeksSecondOrderBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    date: Option<String>,
    interval: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    annual_dividend: Option<f64>,
    rate_type: Option<String>,
    rate_value: Option<f64>,
    version: Option<String>,
    strike_range: Option<i32>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryGreeksSecondOrderBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `interval` on the pending request.
    fn interval<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.interval = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `annual_dividend` on the pending request.
    fn annual_dividend<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.annual_dividend = Some(value);
        slf
    }

    /// Set `rate_type` on the pending request.
    fn rate_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.rate_type = Some(value.into_string());
        slf
    }

    /// Set `rate_value` on the pending request.
    fn rate_value<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.rate_value = Some(value);
        slf
    }

    /// Set `version` on the pending request.
    fn version<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.version = Some(value.into_string());
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<GreeksSecondOrderTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_greeks_second_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        greeks_second_order_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_second_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_second_order_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_greeks_second_order` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_greeks_second_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match greeks_second_order_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_greeks_second_order` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_second_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match greeks_second_order_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch second-order Greeks on each trade for an option contract.
///
/// - Returns the data for all contracts that share the same provided symbol and expiration.
/// - Calculates greeks for every trade reported by OPRA.
/// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
/// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
/// - `rate_type`: `"sofr"`
/// - `version`: `"latest"`
#[pyclass(module = "thetadatadx", name = "OptionHistoryTradeGreeksSecondOrderBuilder")]
pub struct OptionHistoryTradeGreeksSecondOrderBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    date: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    annual_dividend: Option<f64>,
    rate_type: Option<String>,
    rate_value: Option<f64>,
    version: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryTradeGreeksSecondOrderBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `annual_dividend` on the pending request.
    fn annual_dividend<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.annual_dividend = Some(value);
        slf
    }

    /// Set `rate_type` on the pending request.
    fn rate_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.rate_type = Some(value.into_string());
        slf
    }

    /// Set `rate_value` on the pending request.
    fn rate_value<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.rate_value = Some(value);
        slf
    }

    /// Set `version` on the pending request.
    fn version<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.version = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<TradeGreeksSecondOrderTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_second_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        trade_greeks_second_order_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_second_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_greeks_second_order_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_trade_greeks_second_order` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_second_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match trade_greeks_second_order_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_trade_greeks_second_order` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_second_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match trade_greeks_second_order_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch third-order Greeks history (intraday, sampled by interval).
///
/// - Returns the data for all contracts that share the same provided symbol and expiration. 
/// - Calculated using the option and underlying midpoint price. If an interval size is specified (*highly recommended*), the option quote used in the calculation follows the same rules as the quote endpoint. 
/// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
/// - Multi-day requests are limited to 1 month of data.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `interval`: `"1s"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
/// - `rate_type`: `"sofr"`
/// - `version`: `"latest"`
#[pyclass(module = "thetadatadx", name = "OptionHistoryGreeksThirdOrderBuilder")]
pub struct OptionHistoryGreeksThirdOrderBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    date: Option<String>,
    interval: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    annual_dividend: Option<f64>,
    rate_type: Option<String>,
    rate_value: Option<f64>,
    version: Option<String>,
    strike_range: Option<i32>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryGreeksThirdOrderBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `interval` on the pending request.
    fn interval<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.interval = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `annual_dividend` on the pending request.
    fn annual_dividend<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.annual_dividend = Some(value);
        slf
    }

    /// Set `rate_type` on the pending request.
    fn rate_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.rate_type = Some(value.into_string());
        slf
    }

    /// Set `rate_value` on the pending request.
    fn rate_value<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.rate_value = Some(value);
        slf
    }

    /// Set `version` on the pending request.
    fn version<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.version = Some(value.into_string());
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<GreeksThirdOrderTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_greeks_third_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        greeks_third_order_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_third_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_third_order_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_greeks_third_order` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_greeks_third_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match greeks_third_order_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_greeks_third_order` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_third_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match greeks_third_order_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch third-order Greeks on each trade for an option contract.
///
/// - Returns the data for all contracts that share the same provided symbol and expiration.
/// - Calculates greeks for every trade reported by OPRA.
/// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
/// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
/// - `rate_type`: `"sofr"`
/// - `version`: `"latest"`
#[pyclass(module = "thetadatadx", name = "OptionHistoryTradeGreeksThirdOrderBuilder")]
pub struct OptionHistoryTradeGreeksThirdOrderBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    date: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    annual_dividend: Option<f64>,
    rate_type: Option<String>,
    rate_value: Option<f64>,
    version: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryTradeGreeksThirdOrderBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `annual_dividend` on the pending request.
    fn annual_dividend<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.annual_dividend = Some(value);
        slf
    }

    /// Set `rate_type` on the pending request.
    fn rate_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.rate_type = Some(value.into_string());
        slf
    }

    /// Set `rate_value` on the pending request.
    fn rate_value<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.rate_value = Some(value);
        slf
    }

    /// Set `version` on the pending request.
    fn version<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.version = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<TradeGreeksThirdOrderTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_third_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        trade_greeks_third_order_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_third_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_greeks_third_order_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_trade_greeks_third_order` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_third_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match trade_greeks_third_order_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_trade_greeks_third_order` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_third_order(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match trade_greeks_third_order_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch implied volatility history (intraday, sampled by interval).
///
/// - Returns implied volatilies calculated using the national best bid, mid, and ask price of the option respectively. 
/// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
/// - Multi-day requests are limited to 1 month of data.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `interval`: `"1s"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
/// - `rate_type`: `"sofr"`
/// - `version`: `"latest"`
#[pyclass(module = "thetadatadx", name = "OptionHistoryGreeksImpliedVolatilityBuilder")]
pub struct OptionHistoryGreeksImpliedVolatilityBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    date: Option<String>,
    interval: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    annual_dividend: Option<f64>,
    rate_type: Option<String>,
    rate_value: Option<f64>,
    version: Option<String>,
    strike_range: Option<i32>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryGreeksImpliedVolatilityBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `interval` on the pending request.
    fn interval<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.interval = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `annual_dividend` on the pending request.
    fn annual_dividend<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.annual_dividend = Some(value);
        slf
    }

    /// Set `rate_type` on the pending request.
    fn rate_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.rate_type = Some(value.into_string());
        slf
    }

    /// Set `rate_value` on the pending request.
    fn rate_value<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.rate_value = Some(value);
        slf
    }

    /// Set `version` on the pending request.
    fn version<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.version = Some(value.into_string());
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<IvTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_greeks_implied_volatility(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        iv_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_implied_volatility(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| iv_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_greeks_implied_volatility` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_greeks_implied_volatility(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match iv_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_greeks_implied_volatility` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_implied_volatility(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match iv_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch implied volatility on each trade for an option contract.
///
/// - Returns implied volatilies calculated using the trade reported by OPRA. 
/// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
/// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
/// - `rate_type`: `"sofr"`
/// - `version`: `"latest"`
#[pyclass(module = "thetadatadx", name = "OptionHistoryTradeGreeksImpliedVolatilityBuilder")]
pub struct OptionHistoryTradeGreeksImpliedVolatilityBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    strike: Option<String>,
    right: Option<String>,
    date: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    annual_dividend: Option<f64>,
    rate_type: Option<String>,
    rate_value: Option<f64>,
    version: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionHistoryTradeGreeksImpliedVolatilityBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `annual_dividend` on the pending request.
    fn annual_dividend<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.annual_dividend = Some(value);
        slf
    }

    /// Set `rate_type` on the pending request.
    fn rate_type<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.rate_type = Some(value.into_string());
        slf
    }

    /// Set `rate_value` on the pending request.
    fn rate_value<'py>(mut slf: PyRefMut<'py, Self>, value: f64) -> PyRefMut<'py, Self> {
        slf.rate_value = Some(value);
        slf
    }

    /// Set `version` on the pending request.
    fn version<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.version = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<TradeGreeksImpliedVolatilityTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_implied_volatility(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        trade_greeks_implied_volatility_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_implied_volatility(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_greeks_implied_volatility_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_history_trade_greeks_implied_volatility` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_implied_volatility(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match trade_greeks_implied_volatility_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_history_trade_greeks_implied_volatility` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let date = self.date.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let annual_dividend = self.annual_dividend;
        let rate_type = self.rate_type.clone();
        let rate_value = self.rate_value;
        let version = self.version.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_implied_volatility(&symbol, &expiration);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &annual_dividend {
                request = request.annual_dividend(*value);
            }
            if let Some(value) = &rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = &rate_value {
                request = request.rate_value(*value);
            }
            if let Some(value) = &version {
                request = request.version(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match trade_greeks_implied_volatility_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch the trade at a specific time of day across a date range for an option.
///
/// - Returns the last trade reported by OPRA at a specified millisecond of the day.
/// - Trade condition mappings can be found here.
/// - Extended trade conditions are not reported by OPRA for options, so they can be ignored.
/// - The ``time_of_day``parameter represents the 00:00:00.000 ET that the trade should be provided for.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
#[pyclass(module = "thetadatadx", name = "OptionAtTimeTradeBuilder")]
pub struct OptionAtTimeTradeBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    start_date: String,
    end_date: String,
    time_of_day: String,
    strike: Option<String>,
    right: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionAtTimeTradeBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Replace the required `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = value.into_string();
        slf
    }

    /// Replace the required `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = value.into_string();
        slf
    }

    /// Replace the required `time_of_day` on the pending request.
    fn time_of_day<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.time_of_day = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<TradeTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_at_time_trade(&symbol, &expiration, &start_date, &end_date, &time_of_day);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        trade_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_at_time_trade(&symbol, &expiration, &start_date, &end_date, &time_of_day);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_at_time_trade` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_at_time_trade(&symbol, &expiration, &start_date, &end_date, &time_of_day);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match trade_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_at_time_trade` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_at_time_trade(&symbol, &expiration, &start_date, &end_date, &time_of_day);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match trade_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch the quote at a specific time of day across a date range for an option.
///
/// - Returns the last NBBO quote reported by OPRA at a specified millisecond of the day.
/// - The ``time_of_day``parameter represents the 00:00:00.000 ET that the quote should be provided for.
///
/// Defaults (upstream):
/// - `strike`: `"*"`
/// - `right`: `"both"`
#[pyclass(module = "thetadatadx", name = "OptionAtTimeQuoteBuilder")]
pub struct OptionAtTimeQuoteBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    expiration: String,
    start_date: String,
    end_date: String,
    time_of_day: String,
    strike: Option<String>,
    right: Option<String>,
    max_dte: Option<i32>,
    strike_range: Option<i32>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl OptionAtTimeQuoteBuilder {
    /// Set `strike` on the pending request.
    fn strike<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.strike = Some(value.into_string());
        slf
    }

    /// Set `right` on the pending request.
    fn right<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.right = Some(value.into_string());
        slf
    }

    /// Set `max_dte` on the pending request.
    fn max_dte<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.max_dte = Some(value);
        slf
    }

    /// Set `strike_range` on the pending request.
    fn strike_range<'py>(mut slf: PyRefMut<'py, Self>, value: i32) -> PyRefMut<'py, Self> {
        slf.strike_range = Some(value);
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `expiration` on the pending request.
    fn expiration<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.expiration = value.into_string();
        slf
    }

    /// Replace the required `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = value.into_string();
        slf
    }

    /// Replace the required `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = value.into_string();
        slf
    }

    /// Replace the required `time_of_day` on the pending request.
    fn time_of_day<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.time_of_day = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<QuoteTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().option_at_time_quote(&symbol, &expiration, &start_date, &end_date, &time_of_day);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        quote_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_at_time_quote(&symbol, &expiration, &start_date, &end_date, &time_of_day);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| quote_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `option_at_time_quote` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().option_at_time_quote(&symbol, &expiration, &start_date, &end_date, &time_of_day);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match quote_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `option_at_time_quote` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let expiration = self.expiration.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let strike = self.strike.clone();
        let right = self.right.clone();
        let max_dte = self.max_dte;
        let strike_range = self.strike_range;
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_at_time_quote(&symbol, &expiration, &start_date, &end_date, &time_of_day);
            if let Some(value) = &strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = &right {
                request = request.right(value.as_str());
            }
            if let Some(value) = &max_dte {
                request = request.max_dte(*value);
            }
            if let Some(value) = &strike_range {
                request = request.strike_range(*value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match quote_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// List all available index symbols.
///
/// A symbol can be defined as a unique identifier for a stock / underlying asset. Common terms also include: root, ticker, and underlying. This endpoint returns all traded symbols for options. This endpoint is updated overnight.
#[pyclass(module = "thetadatadx", name = "IndexListSymbolsBuilder")]
pub struct IndexListSymbolsBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl IndexListSymbolsBuilder {
    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed `StringList` wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<StringList>> {
        let client = self.client.clone();
        let timeout_ms = self.timeout_ms;
        let values: Vec<String> = run_blocking(py, async move {
            let call = client.market_data().index_list_symbols();
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        })?;
        strings_to_string_list(py, values, "symbol")
    }

    /// Async companion to `list()` — awaitable yields the `StringList` wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let call = client.market_data().index_list_symbols();
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        }, |py, values| strings_to_string_list(py, values, "symbol").map(|p| p.into_any()))
    }
}

/// List available dates for an index symbol.
///
/// Lists all dates of data that are available for a index with a given request type and symbol. This endpoint is updated overnight.
#[pyclass(module = "thetadatadx", name = "IndexListDatesBuilder")]
pub struct IndexListDatesBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl IndexListDatesBuilder {
    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed `StringList` wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<StringList>> {
        let client = self.client.clone();
        let timeout_ms = self.timeout_ms;
        let symbol = self.symbol.clone();
        let values: Vec<String> = run_blocking(py, async move {
            let call = client.market_data().index_list_dates(&symbol);
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        })?;
        strings_to_string_list(py, values, "date")
    }

    /// Async companion to `list()` — awaitable yields the `StringList` wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let call = client.market_data().index_list_dates(&symbol);
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        }, |py, values| strings_to_string_list(py, values, "date").map(|p| p.into_any()))
    }
}

/// Get the latest OHLC snapshot for one or more indices.
///
/// - Retrieves the real-time current day OHLC.
/// - Exchanges typically generate a price report every second for popular indices like SPX.
#[pyclass(module = "thetadatadx", name = "IndexSnapshotOhlcBuilder")]
pub struct IndexSnapshotOhlcBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbols: Vec<String>,
    min_time: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl IndexSnapshotOhlcBuilder {
    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbols<'py>(mut slf: PyRefMut<'py, Self>, value: PySymbols) -> PyRefMut<'py, Self> {
        slf.symbols = value.into_vec();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<OhlcTickList>> {
        let client = self.client.clone();
        let symbols = self.symbols.clone();
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().index_snapshot_ohlc(&refs);
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        ohlc_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbols = self.symbols.clone();
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().index_snapshot_ohlc(&refs);
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| ohlc_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Get the latest price snapshot for one or more indices.
///
/// - Retrieves a real-time last index price.
/// - Exchanges typically generate a price report every second for popular indices like SPX.
#[pyclass(module = "thetadatadx", name = "IndexSnapshotPriceBuilder")]
pub struct IndexSnapshotPriceBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbols: Vec<String>,
    min_time: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl IndexSnapshotPriceBuilder {
    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbols<'py>(mut slf: PyRefMut<'py, Self>, value: PySymbols) -> PyRefMut<'py, Self> {
        slf.symbols = value.into_vec();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<PriceTickList>> {
        let client = self.client.clone();
        let symbols = self.symbols.clone();
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().index_snapshot_price(&refs);
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        price_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbols = self.symbols.clone();
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().index_snapshot_price(&refs);
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| price_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Get the latest market value snapshot for one or more indices.
///
/// - Retrieves a real-time last index market value.
/// - Exchanges typically generate a price report every second for popular indices like SPX.
#[pyclass(module = "thetadatadx", name = "IndexSnapshotMarketValueBuilder")]
pub struct IndexSnapshotMarketValueBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbols: Vec<String>,
    min_time: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl IndexSnapshotMarketValueBuilder {
    /// Set `min_time` on the pending request.
    fn min_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.min_time = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbols<'py>(mut slf: PyRefMut<'py, Self>, value: PySymbols) -> PyRefMut<'py, Self> {
        slf.symbols = value.into_vec();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<MarketValueTickList>> {
        let client = self.client.clone();
        let symbols = self.symbols.clone();
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().index_snapshot_market_value(&refs);
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        market_value_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbols = self.symbols.clone();
        let min_time = self.min_time.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().index_snapshot_market_value(&refs);
            if let Some(value) = &min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| market_value_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Fetch end-of-day index data for a date range.
///
/// - Since the indices feeds do not provide a national EOD report, Theta Data generates a national EOD report at 17:15 each day.
#[pyclass(module = "thetadatadx", name = "IndexHistoryEodBuilder")]
pub struct IndexHistoryEodBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    start_date: String,
    end_date: String,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl IndexHistoryEodBuilder {
    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = value.into_string();
        slf
    }

    /// Replace the required `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<EodTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().index_history_eod(&symbol, &start_date, &end_date);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        eod_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().index_history_eod(&symbol, &start_date, &end_date);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| eod_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `index_history_eod` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().index_history_eod(&symbol, &start_date, &end_date);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match eod_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `index_history_eod` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().index_history_eod(&symbol, &start_date, &end_date);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match eod_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch intraday OHLC bars for an index.
///
/// - Aggregated OHLC bars that use SIP rules for each bar.
/// - Time timestamp of the bar represents the opening time of the bar. For a trade to be part of the bar:  ``bar timestamp`` <= ``trade time`` < ``bar timestamp + interval``.
/// - Exchanges typically generate a price report every second for popular indices like SPX.
///
/// Defaults (upstream):
/// - `interval`: `"1s"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
#[pyclass(module = "thetadatadx", name = "IndexHistoryOhlcBuilder")]
pub struct IndexHistoryOhlcBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    start_date: String,
    end_date: String,
    interval: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl IndexHistoryOhlcBuilder {
    /// Set `interval` on the pending request.
    fn interval<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.interval = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = value.into_string();
        slf
    }

    /// Replace the required `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<OhlcTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().index_history_ohlc(&symbol, &start_date, &end_date);
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        ohlc_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().index_history_ohlc(&symbol, &start_date, &end_date);
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| ohlc_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `index_history_ohlc` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().index_history_ohlc(&symbol, &start_date, &end_date);
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match ohlc_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `index_history_ohlc` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().index_history_ohlc(&symbol, &start_date, &end_date);
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match ohlc_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch intraday price history for an index.
///
/// - Retrieves historical indices price reports. Exchanges typically generate a price report every second for popular indices like SPX.
/// - When the ``interval`` parameter is specified, the returned data represents the price at the exact time of each timestamp. If the timestamp in the response is 10:30:00, the price field represents the price at that exact time of the day.
/// - A price update from the exchange is omitted if the price remained the same from the previous update.
/// - Multi-day requests are limited to 1 month of data.
///
/// Defaults (upstream):
/// - `interval`: `"1s"`
/// - `start_time`: `"09:30:00"`
/// - `end_time`: `"16:00:00"`
#[pyclass(module = "thetadatadx", name = "IndexHistoryPriceBuilder")]
pub struct IndexHistoryPriceBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    date: Option<String>,
    interval: Option<String>,
    start_time: Option<String>,
    end_time: Option<String>,
    start_date: Option<String>,
    end_date: Option<String>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl IndexHistoryPriceBuilder {
    /// Set `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = Some(value.into_string());
        slf
    }

    /// Set `interval` on the pending request.
    fn interval<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.interval = Some(value.into_string());
        slf
    }

    /// Set `start_time` on the pending request.
    fn start_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.start_time = Some(value.into_string());
        slf
    }

    /// Set `end_time` on the pending request.
    fn end_time<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.end_time = Some(value.into_string());
        slf
    }

    /// Set `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = Some(value.into_string());
        slf
    }

    /// Set `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = Some(value.into_string());
        slf
    }

    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<PriceTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().index_history_price(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        price_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().index_history_price(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| price_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `index_history_price` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().index_history_price(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match price_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `index_history_price` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Under `bulk_fetch = "auto"` a large history pull may fan out across concurrent sub-requests: every chunk is still delivered exactly once, but chunks from different sub-requests interleave in arrival order rather than the single-stream order (`bulk_fetch = "off"` restores it). Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let date = self.date.clone();
        let interval = self.interval.clone();
        let start_time = self.start_time.clone();
        let end_time = self.end_time.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().index_history_price(&symbol);
            if let Some(value) = &date {
                request = request.date(value.as_str());
            }
            if let Some(value) = &interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = &start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = &end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = &start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = &end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match price_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Fetch the index price at a specific time of day across a date range.
///
/// - Retrieves historical indices price reports. Exchanges typically generate a price report every second for popular indices like SPX.
/// - The ``time_of_day`` parameter represents the 00:00:00.000 ET that the price should be provided for.
#[pyclass(module = "thetadatadx", name = "IndexAtTimePriceBuilder")]
pub struct IndexAtTimePriceBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    start_date: String,
    end_date: String,
    time_of_day: String,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl IndexAtTimePriceBuilder {
    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = value.into_string();
        slf
    }

    /// Replace the required `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = value.into_string();
        slf
    }

    /// Replace the required `time_of_day` on the pending request.
    fn time_of_day<'py>(mut slf: PyRefMut<'py, Self>, value: PyTimeArg) -> PyRefMut<'py, Self> {
        slf.time_of_day = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<IndexPriceAtTimeTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().index_at_time_price(&symbol, &start_date, &end_date, &time_of_day);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        index_price_at_time_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().index_at_time_price(&symbol, &start_date, &end_date, &time_of_day);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| index_price_at_time_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `index_at_time_price` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().index_at_time_price(&symbol, &start_date, &end_date, &time_of_day);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match index_price_at_time_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `index_at_time_price` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let time_of_day = self.time_of_day.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().index_at_time_price(&symbol, &start_date, &end_date, &time_of_day);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match index_price_at_time_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

/// Check whether the market is open today.
///
/// - Retrieves current day equity market schedule
/// - *On days when the market closes early at 1:00 PM ET; eligible options will trade until 1:15 PM.
/// - **Some NYSE exchanges will continue late trading until 5:00 PM ET on early close days.
#[pyclass(module = "thetadatadx", name = "CalendarOpenTodayBuilder")]
pub struct CalendarOpenTodayBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl CalendarOpenTodayBuilder {
    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<CalendarDayList>> {
        let client = self.client.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().calendar_open_today();
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        calendar_days_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().calendar_open_today();
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| calendar_days_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Get calendar information for a specific date.
///
/// - Retrieves equity market schedule for a given date
/// - Note: Holiday data is available 01/01/2012 through the end of the calendar year that immediately follows the current year
/// - *On days when the market closes early at 1:00 PM ET; eligible options will trade until 1:15 PM.
/// - **Some NYSE exchanges will continue late trading until 5:00 PM ET on early close days.
#[pyclass(module = "thetadatadx", name = "CalendarOnDateBuilder")]
pub struct CalendarOnDateBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    date: String,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl CalendarOnDateBuilder {
    /// Replace the required `date` on the pending request.
    fn date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.date = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<CalendarDayList>> {
        let client = self.client.clone();
        let date = self.date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().calendar_on_date(&date);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        calendar_days_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let date = self.date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().calendar_on_date(&date);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| calendar_days_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Get equity market holidays and early-close days for a year (vendor `year_holidays` endpoint — only non-standard days, not every trading day).
///
/// - Retrieves equity market holidays for a given year
/// - Note: Holiday data is available 01/01/2012 through the end of the calendar year that immediately follows the current year
/// - *On days when the market closes early at 1:00 PM ET; eligible options will trade until 1:15 PM.
/// - **Some NYSE exchanges will continue late trading until 5:00 PM ET on early close days.
#[pyclass(module = "thetadatadx", name = "CalendarYearBuilder")]
pub struct CalendarYearBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    year: String,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl CalendarYearBuilder {
    /// Replace the required `year` on the pending request.
    fn year<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.year = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<CalendarDayList>> {
        let client = self.client.clone();
        let year = self.year.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().calendar_year(&year);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        calendar_days_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let year = self.year.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().calendar_year(&year);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| calendar_days_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

}

/// Fetch end-of-day interest rate history.
///
/// - Returns the interest rate reported. Depending on the rate, reports can occur in the morning or the afternoon.
/// - Valid `symbol` values per upstream `RateType` enum:
///   `SOFR`, `TREASURY_M1`, `TREASURY_M3`, `TREASURY_M6`,
///   `TREASURY_Y1`, `TREASURY_Y2`, `TREASURY_Y3`, `TREASURY_Y5`,
///   `TREASURY_Y7`, `TREASURY_Y10`, `TREASURY_Y20`, `TREASURY_Y30`.
#[pyclass(module = "thetadatadx", name = "InterestRateHistoryEodBuilder")]
pub struct InterestRateHistoryEodBuilder {
    client: std::sync::Arc<thetadatadx::Client>,
    symbol: String,
    start_date: String,
    end_date: String,
    timeout_ms: Option<u64>,
}

#[pymethods]
impl InterestRateHistoryEodBuilder {
    /// Replace the required `symbol` on the pending request.
    fn symbol<'py>(mut slf: PyRefMut<'py, Self>, value: PyStringArg) -> PyRefMut<'py, Self> {
        slf.symbol = value.into_string();
        slf
    }

    /// Replace the required `start_date` on the pending request.
    fn start_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.start_date = value.into_string();
        slf
    }

    /// Replace the required `end_date` on the pending request.
    fn end_date<'py>(mut slf: PyRefMut<'py, Self>, value: PyDateArg) -> PyRefMut<'py, Self> {
        slf.end_date = value.into_string();
        slf
    }

    /// Apply a per-request deadline in milliseconds.
    fn timeout_ms<'py>(mut slf: PyRefMut<'py, Self>, value: u64) -> PyRefMut<'py, Self> {
        slf.timeout_ms = Some(value);
        slf
    }

    /// Execute the request and return a typed list wrapper.
    ///
    /// Chain `.to_polars()` / `.to_pandas()` / `.to_arrow()` / `.to_list()`
    /// on the result to convert to the downstream representation.
    fn list(&self, py: Python<'_>) -> PyResult<Py<InterestRateTickList>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let ticks = run_blocking(py, async move {
            let mut request = client.market_data().interest_rate_history_eod(&symbol, &start_date, &end_date);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        })?;
        interest_rate_ticks_to_pyclass_list(py, ticks)
    }

    /// Async companion to `list()` — awaitable yields the typed list wrapper.
    fn list_async<'py>(&self, py: Python<'py>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        spawn_awaitable(py, async move {
            let mut request = client.market_data().interest_rate_history_eod(&symbol, &start_date, &end_date);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| interest_rate_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Stream chunks of `interest_rate_history_eod` rows into `handler` without materialising the full response in memory. `handler(chunk: list[Tick]) -> None` is called once per gRPC chunk; the chunk is freed before the stream's next chunk is fetched. A `RuntimeError` raised by `handler` aborts the stream and propagates as the method's return value.
    fn stream(&self, py: Python<'_>, handler: Py<PyAny>) -> PyResult<()> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        // Callback PyErr is captured in a Mutex<Option<PyErr>>
        // because the chunk closure is `FnMut`, not `FnOnce`, and
        // can't move-out of `&mut Option<PyErr>`. The Mutex also
        // gives us `Send`, which `run_blocking`'s bound requires.
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let stream_result = run_blocking(py, async move {
            let mut request = client.market_data().interest_rate_history_eod(&symbol, &start_date, &end_date);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.stream_ticks(|chunk| {
                if cb_err_for_closure.lock().unwrap().is_some() {
                    return;
                }
                Python::attach(|py| {
                    let py_list = match interest_rate_ticks_vec_to_pylist(py, chunk) {
                        Ok(list) => list,
                        Err(e) => {
                            *cb_err_for_closure.lock().unwrap() = Some(e);
                            return;
                        }
                    };
                    if let Err(e) = handler_for_closure.call1(py, (py_list,)) {
                        *cb_err_for_closure.lock().unwrap() = Some(e);
                    }
                });
            }).await
        });
        // Surface the callback PyErr before any later stream/deadline
        // error observed while draining after the callback stopped
        // processing; the callback exception is the proximate cause.
        if let Some(py_err) = callback_error.lock().unwrap().take() {
            return Err(py_err);
        }
        stream_result?;
        Ok(())
    }

    /// Async companion to `stream()` — awaitable yields `None` when the streamed response of `interest_rate_history_eod` rows finishes. `handler(chunk: list[Tick]) -> None` runs once per gRPC chunk, one call at a time, on a worker thread; a stream's next chunk is not fetched until its handler call returns. Cancelling the awaitable drops the in-flight gRPC stream (RST_STREAM on the underlying h2 stream) and fetches no further chunks. A handler already running when cancellation lands runs to completion — Python is not interruptible mid-call — so one final `handler` call may finish after the awaitable is cancelled or its deadline expires.
    fn stream_async<'py>(&self, py: Python<'py>, handler: Py<PyAny>) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        let symbol = self.symbol.clone();
        let start_date = self.start_date.clone();
        let end_date = self.end_date.clone();
        let timeout_ms = self.timeout_ms;
        let handler_arc = std::sync::Arc::new(handler);
        let handler_for_closure = std::sync::Arc::clone(&handler_arc);
        let callback_error: std::sync::Arc<std::sync::Mutex<Option<PyErr>>> =
            std::sync::Arc::new(std::sync::Mutex::new(None));
        let cb_err_for_closure = std::sync::Arc::clone(&callback_error);
        let cb_err_for_convert = std::sync::Arc::clone(&callback_error);
        spawn_awaitable(py, async move {
            let mut request = client.market_data().interest_rate_history_eod(&symbol, &start_date, &end_date);
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            Ok::<_, thetadatadx::Error>(request.stream_ticks_async(|chunk| {
                // Copy the chunk out synchronously so nothing borrowed
                // is held across the await, then run the GIL-bound
                // handler on the blocking pool: a slow handler parks a
                // pool thread, never a shared async worker driving other
                // in-flight market-data calls. The handler Py<PyAny> is
                // Arc'd once (Send + Sync); we clone the Arc per chunk
                // (no GIL needed), never clone_ref.
                let owned = if cb_err_for_closure.lock().unwrap().is_some() {
                    None
                } else {
                    Some(chunk)
                };
                let handler_for_task = std::sync::Arc::clone(&handler_for_closure);
                let cb_err_for_task = std::sync::Arc::clone(&cb_err_for_closure);
                let cb_err_for_join = std::sync::Arc::clone(&cb_err_for_closure);
                async move {
                    let Some(owned) = owned else { return; };
                    // GIL acquired strictly inside spawn_blocking — never
                    // held on the async side while awaiting the join, so a
                    // pool thread waiting on the GIL cannot deadlock the
                    // task awaiting it.
                    let join = tokio::task::spawn_blocking(move || {
                        Python::attach(|py| {
                            let py_list = match interest_rate_ticks_vec_to_pylist(py, owned) {
                                Ok(list) => list,
                                Err(e) => {
                                    *cb_err_for_task.lock().unwrap() = Some(e);
                                    return;
                                }
                            };
                            if let Err(e) = handler_for_task.call1(py, (py_list,)) {
                                *cb_err_for_task.lock().unwrap() = Some(e);
                            }
                        })
                    }).await;
                    if let Err(join_err) = join {
                        // A panic in the handler task surfaces as a
                        // re-raised RuntimeError instead of being swallowed.
                        crate::async_runtime::capture_join_error(&cb_err_for_join, join_err);
                    }
                }
            }).await)
        }, move |py, stream_result| {
            // Post-await converter — reacquired GIL. Re-raise any
            // captured callback PyErr before any later stream/deadline
            // error observed after the callback stopped processing.
            if let Some(py_err) = cb_err_for_convert.lock().unwrap().take() {
                return Err(py_err);
            }
            stream_result.map_err(to_py_err)?;
            Ok::<_, PyErr>(py.None())
        })
    }
}

fn register_generated_market_data_builders(m: &Bound<'_, PyModule>) -> PyResult<()> {
    m.add_class::<StockListSymbolsBuilder>()?;
    m.add_class::<StockListDatesBuilder>()?;
    m.add_class::<StockSnapshotOhlcBuilder>()?;
    m.add_class::<StockSnapshotTradeBuilder>()?;
    m.add_class::<StockSnapshotQuoteBuilder>()?;
    m.add_class::<StockSnapshotMarketValueBuilder>()?;
    m.add_class::<StockHistoryEodBuilder>()?;
    m.add_class::<StockHistoryOhlcBuilder>()?;
    m.add_class::<StockHistoryTradeBuilder>()?;
    m.add_class::<StockHistoryQuoteBuilder>()?;
    m.add_class::<StockHistoryTradeQuoteBuilder>()?;
    m.add_class::<StockAtTimeTradeBuilder>()?;
    m.add_class::<StockAtTimeQuoteBuilder>()?;
    m.add_class::<OptionListSymbolsBuilder>()?;
    m.add_class::<OptionListDatesBuilder>()?;
    m.add_class::<OptionListExpirationsBuilder>()?;
    m.add_class::<OptionListStrikesBuilder>()?;
    m.add_class::<OptionListContractsBuilder>()?;
    m.add_class::<OptionSnapshotOhlcBuilder>()?;
    m.add_class::<OptionSnapshotTradeBuilder>()?;
    m.add_class::<OptionSnapshotQuoteBuilder>()?;
    m.add_class::<OptionSnapshotOpenInterestBuilder>()?;
    m.add_class::<OptionSnapshotMarketValueBuilder>()?;
    m.add_class::<OptionSnapshotGreeksImpliedVolatilityBuilder>()?;
    m.add_class::<OptionSnapshotGreeksAllBuilder>()?;
    m.add_class::<OptionSnapshotGreeksFirstOrderBuilder>()?;
    m.add_class::<OptionSnapshotGreeksSecondOrderBuilder>()?;
    m.add_class::<OptionSnapshotGreeksThirdOrderBuilder>()?;
    m.add_class::<OptionHistoryEodBuilder>()?;
    m.add_class::<OptionHistoryOhlcBuilder>()?;
    m.add_class::<OptionHistoryTradeBuilder>()?;
    m.add_class::<OptionHistoryQuoteBuilder>()?;
    m.add_class::<OptionHistoryTradeQuoteBuilder>()?;
    m.add_class::<OptionHistoryOpenInterestBuilder>()?;
    m.add_class::<OptionHistoryGreeksEodBuilder>()?;
    m.add_class::<OptionHistoryGreeksAllBuilder>()?;
    m.add_class::<OptionHistoryTradeGreeksAllBuilder>()?;
    m.add_class::<OptionHistoryGreeksFirstOrderBuilder>()?;
    m.add_class::<OptionHistoryTradeGreeksFirstOrderBuilder>()?;
    m.add_class::<OptionHistoryGreeksSecondOrderBuilder>()?;
    m.add_class::<OptionHistoryTradeGreeksSecondOrderBuilder>()?;
    m.add_class::<OptionHistoryGreeksThirdOrderBuilder>()?;
    m.add_class::<OptionHistoryTradeGreeksThirdOrderBuilder>()?;
    m.add_class::<OptionHistoryGreeksImpliedVolatilityBuilder>()?;
    m.add_class::<OptionHistoryTradeGreeksImpliedVolatilityBuilder>()?;
    m.add_class::<OptionAtTimeTradeBuilder>()?;
    m.add_class::<OptionAtTimeQuoteBuilder>()?;
    m.add_class::<IndexListSymbolsBuilder>()?;
    m.add_class::<IndexListDatesBuilder>()?;
    m.add_class::<IndexSnapshotOhlcBuilder>()?;
    m.add_class::<IndexSnapshotPriceBuilder>()?;
    m.add_class::<IndexSnapshotMarketValueBuilder>()?;
    m.add_class::<IndexHistoryEodBuilder>()?;
    m.add_class::<IndexHistoryOhlcBuilder>()?;
    m.add_class::<IndexHistoryPriceBuilder>()?;
    m.add_class::<IndexAtTimePriceBuilder>()?;
    m.add_class::<CalendarOpenTodayBuilder>()?;
    m.add_class::<CalendarOnDateBuilder>()?;
    m.add_class::<CalendarYearBuilder>()?;
    m.add_class::<InterestRateHistoryEodBuilder>()?;
    Ok(())
}

#[pymethods]
impl MarketDataView {
    /// List all available stock ticker symbols.
    ///
    /// A symbol can be defined as a unique identifier for a stock / underlying asset. Common terms also include: root, ticker, and underlying. This endpoint returns all traded symbols for stocks. This endpoint is updated overnight.
    #[pyo3(signature = (*, timeout_ms=None))]
    fn stock_list_symbols(
        &self,
        py: Python<'_>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<StringList>> {
        let values: Vec<String> = run_blocking(py, async move {
            let call = self.client.market_data().stock_list_symbols();
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        })?;
        strings_to_string_list(py, values, "symbol")
    }

    /// List all available stock ticker symbols.
    ///
    /// A symbol can be defined as a unique identifier for a stock / underlying asset. Common terms also include: root, ticker, and underlying. This endpoint returns all traded symbols for stocks. This endpoint is updated overnight.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (*, timeout_ms=None))]
    fn stock_list_symbols_async<'py>(
        &self,
        py: Python<'py>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let call = client.market_data().stock_list_symbols();
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        }, |py, values| strings_to_string_list(py, values, "symbol").map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `stock_list_symbols`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `stock_list_symbols()` for the sync signature; `stock_list_symbols_async()` for the awaitable companion.
    #[pyo3(signature = ())]
    fn stock_list_symbols_builder(
        &self,
    ) -> StockListSymbolsBuilder {
        StockListSymbolsBuilder {
            client: self.client.clone(),
            timeout_ms: None,
        }
    }

    /// List available dates for a stock by request type (EOD, TRADE, QUOTE, etc.).
    ///
    /// Lists all dates of data that are available for a stock with a given request type and symbol. This endpoint is updated overnight.
    #[pyo3(signature = (request_type, symbol, *, timeout_ms=None))]
    fn stock_list_dates(
        &self,
        py: Python<'_>,
        request_type: PyStringArg,
        symbol: PyStringArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<StringList>> {
        let values: Vec<String> = run_blocking(py, async move {
            let call = self.client.market_data().stock_list_dates(request_type.as_str(), symbol.as_str());
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        })?;
        strings_to_string_list(py, values, "date")
    }

    /// List available dates for a stock by request type (EOD, TRADE, QUOTE, etc.).
    ///
    /// Lists all dates of data that are available for a stock with a given request type and symbol. This endpoint is updated overnight.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (request_type, symbol, *, timeout_ms=None))]
    fn stock_list_dates_async<'py>(
        &self,
        py: Python<'py>,
        request_type: PyStringArg,
        symbol: PyStringArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let call = client.market_data().stock_list_dates(request_type.as_str(), symbol.as_str());
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        }, |py, values| strings_to_string_list(py, values, "date").map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `stock_list_dates`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `stock_list_dates()` for the sync signature; `stock_list_dates_async()` for the awaitable companion.
    #[pyo3(signature = (request_type, symbol))]
    fn stock_list_dates_builder(
        &self,
        request_type: PyStringArg,
        symbol: PyStringArg,
    ) -> StockListDatesBuilder {
        StockListDatesBuilder {
            client: self.client.clone(),
            request_type: request_type.into_string(),
            symbol: symbol.into_string(),
            timeout_ms: None,
        }
    }

    /// Get the latest OHLC snapshot for one or more stocks.
    ///
    /// Provides a real-time Open, High, Low, Close for the current day.
    /// * Returns a real-time session OHLC from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    /// * Returns a 15-minute delayed session OHLC from the UTP & CTA feeds if the account has the stocks value subscription.
    /// * Theta Data resets its snapshot cache at midnight ET every day. This endpoint may not work on a weekend where there were no eligible messages sent over exchange feeds. We recommend using historic requests during the weekend.
    ///
    /// Defaults (upstream):
    /// - `venue`: `"nqb"`
    #[pyo3(signature = (symbols, *, venue=None, min_time=None, timeout_ms=None))]
    fn stock_snapshot_ohlc(
        &self,
        py: Python<'_>,
        symbols: PySymbols,
        venue: Option<PyStringArg>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<OhlcTickList>> {
        let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
        let mut request = self.client.market_data().stock_snapshot_ohlc(&refs);
        if let Some(value) = venue {
            request = request.venue(value.as_str());
        }
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        ohlc_ticks_to_pyclass_list(py, ticks)
    }

    /// Get the latest OHLC snapshot for one or more stocks.
    ///
    /// Provides a real-time Open, High, Low, Close for the current day.
    /// * Returns a real-time session OHLC from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    /// * Returns a 15-minute delayed session OHLC from the UTP & CTA feeds if the account has the stocks value subscription.
    /// * Theta Data resets its snapshot cache at midnight ET every day. This endpoint may not work on a weekend where there were no eligible messages sent over exchange feeds. We recommend using historic requests during the weekend.
    ///
    /// Defaults (upstream):
    /// - `venue`: `"nqb"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbols, *, venue=None, min_time=None, timeout_ms=None))]
    fn stock_snapshot_ohlc_async<'py>(
        &self,
        py: Python<'py>,
        symbols: PySymbols,
        venue: Option<PyStringArg>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().stock_snapshot_ohlc(&refs);
            if let Some(value) = venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| ohlc_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `stock_snapshot_ohlc`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `stock_snapshot_ohlc()` for the sync signature; `stock_snapshot_ohlc_async()` for the awaitable companion.
    #[pyo3(signature = (symbols))]
    fn stock_snapshot_ohlc_builder(
        &self,
        symbols: PySymbols,
    ) -> StockSnapshotOhlcBuilder {
        StockSnapshotOhlcBuilder {
            client: self.client.clone(),
            symbols: symbols.into_vec(),
            venue: None,
            min_time: None,
            timeout_ms: None,
        }
    }

    /// Get the latest trade snapshot for one or more stocks.
    ///
    /// Returns a real-time last trade from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    ///
    /// - Theta Data resets its snapshot cache at midnight ET every day. This endpoint may not work on a weekend where there were no eligible messages sent over exchange feeds. We recommend using historic requests during the weekend.
    ///
    /// Defaults (upstream):
    /// - `venue`: `"nqb"`
    #[pyo3(signature = (symbols, *, venue=None, min_time=None, timeout_ms=None))]
    fn stock_snapshot_trade(
        &self,
        py: Python<'_>,
        symbols: PySymbols,
        venue: Option<PyStringArg>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<TradeTickList>> {
        let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
        let mut request = self.client.market_data().stock_snapshot_trade(&refs);
        if let Some(value) = venue {
            request = request.venue(value.as_str());
        }
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        trade_ticks_to_pyclass_list(py, ticks)
    }

    /// Get the latest trade snapshot for one or more stocks.
    ///
    /// Returns a real-time last trade from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    ///
    /// - Theta Data resets its snapshot cache at midnight ET every day. This endpoint may not work on a weekend where there were no eligible messages sent over exchange feeds. We recommend using historic requests during the weekend.
    ///
    /// Defaults (upstream):
    /// - `venue`: `"nqb"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbols, *, venue=None, min_time=None, timeout_ms=None))]
    fn stock_snapshot_trade_async<'py>(
        &self,
        py: Python<'py>,
        symbols: PySymbols,
        venue: Option<PyStringArg>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().stock_snapshot_trade(&refs);
            if let Some(value) = venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `stock_snapshot_trade`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `stock_snapshot_trade()` for the sync signature; `stock_snapshot_trade_async()` for the awaitable companion.
    #[pyo3(signature = (symbols))]
    fn stock_snapshot_trade_builder(
        &self,
        symbols: PySymbols,
    ) -> StockSnapshotTradeBuilder {
        StockSnapshotTradeBuilder {
            client: self.client.clone(),
            symbols: symbols.into_vec(),
            venue: None,
            min_time: None,
            timeout_ms: None,
        }
    }

    /// Get the latest NBBO quote snapshot for one or more stocks.
    ///
    /// * Returns a real-time last BBO quote from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    /// * Returns a 15-minute delayed NBBO quote from the UTP & CTA feeds account has the stocks value subscription subscription.
    /// - Theta Data resets its snapshot cache at midnight ET every day. This endpoint may not work on a weekend where there were no eligible messages sent over exchange feeds. We recommend using historic requests during the weekend.
    ///
    /// Defaults (upstream):
    /// - `venue`: `"nqb"`
    #[pyo3(signature = (symbols, *, venue=None, min_time=None, timeout_ms=None))]
    fn stock_snapshot_quote(
        &self,
        py: Python<'_>,
        symbols: PySymbols,
        venue: Option<PyStringArg>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<QuoteTickList>> {
        let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
        let mut request = self.client.market_data().stock_snapshot_quote(&refs);
        if let Some(value) = venue {
            request = request.venue(value.as_str());
        }
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        quote_ticks_to_pyclass_list(py, ticks)
    }

    /// Get the latest NBBO quote snapshot for one or more stocks.
    ///
    /// * Returns a real-time last BBO quote from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    /// * Returns a 15-minute delayed NBBO quote from the UTP & CTA feeds account has the stocks value subscription subscription.
    /// - Theta Data resets its snapshot cache at midnight ET every day. This endpoint may not work on a weekend where there were no eligible messages sent over exchange feeds. We recommend using historic requests during the weekend.
    ///
    /// Defaults (upstream):
    /// - `venue`: `"nqb"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbols, *, venue=None, min_time=None, timeout_ms=None))]
    fn stock_snapshot_quote_async<'py>(
        &self,
        py: Python<'py>,
        symbols: PySymbols,
        venue: Option<PyStringArg>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().stock_snapshot_quote(&refs);
            if let Some(value) = venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| quote_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `stock_snapshot_quote`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `stock_snapshot_quote()` for the sync signature; `stock_snapshot_quote_async()` for the awaitable companion.
    #[pyo3(signature = (symbols))]
    fn stock_snapshot_quote_builder(
        &self,
        symbols: PySymbols,
    ) -> StockSnapshotQuoteBuilder {
        StockSnapshotQuoteBuilder {
            client: self.client.clone(),
            symbols: symbols.into_vec(),
            venue: None,
            min_time: None,
            timeout_ms: None,
        }
    }

    /// Get the latest market value snapshot for one or more stocks.
    ///
    /// * Returns a real-time market value derived from the last BBO quote from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    /// * Returns a 15-minute delayed market value derived from an NBBO quote from the UTP & CTA feeds if the account has the stocks value subscription subscription.
    /// - Theta Data resets its snapshot cache at midnight ET every day. This endpoint may not work on a weekend where there were no eligible messages sent over exchange feeds. We recommend using historic requests during the weekend.
    ///
    /// Defaults (upstream):
    /// - `venue`: `"nqb"`
    #[pyo3(signature = (symbols, *, venue=None, min_time=None, timeout_ms=None))]
    fn stock_snapshot_market_value(
        &self,
        py: Python<'_>,
        symbols: PySymbols,
        venue: Option<PyStringArg>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<MarketValueTickList>> {
        let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
        let mut request = self.client.market_data().stock_snapshot_market_value(&refs);
        if let Some(value) = venue {
            request = request.venue(value.as_str());
        }
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        market_value_ticks_to_pyclass_list(py, ticks)
    }

    /// Get the latest market value snapshot for one or more stocks.
    ///
    /// * Returns a real-time market value derived from the last BBO quote from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    /// * Returns a 15-minute delayed market value derived from an NBBO quote from the UTP & CTA feeds if the account has the stocks value subscription subscription.
    /// - Theta Data resets its snapshot cache at midnight ET every day. This endpoint may not work on a weekend where there were no eligible messages sent over exchange feeds. We recommend using historic requests during the weekend.
    ///
    /// Defaults (upstream):
    /// - `venue`: `"nqb"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbols, *, venue=None, min_time=None, timeout_ms=None))]
    fn stock_snapshot_market_value_async<'py>(
        &self,
        py: Python<'py>,
        symbols: PySymbols,
        venue: Option<PyStringArg>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().stock_snapshot_market_value(&refs);
            if let Some(value) = venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| market_value_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `stock_snapshot_market_value`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `stock_snapshot_market_value()` for the sync signature; `stock_snapshot_market_value_async()` for the awaitable companion.
    #[pyo3(signature = (symbols))]
    fn stock_snapshot_market_value_builder(
        &self,
        symbols: PySymbols,
    ) -> StockSnapshotMarketValueBuilder {
        StockSnapshotMarketValueBuilder {
            client: self.client.clone(),
            symbols: symbols.into_vec(),
            venue: None,
            min_time: None,
            timeout_ms: None,
        }
    }

    /// Fetch end-of-day stock data for a date range. Returns OHLCV + bid/ask per trading day.
    ///
    /// Since the equity SIPs only generate a partial EOD report, Theta Data generates a national EOD report at 17:15 ET each day. ``created`` represents the datetime the report was generated and ``last_trade`` represents the datetime of the last trade. The quote in the response represents the last NBBO reported by CTA or UTP at the time of report generation. You can read more about EOD & OHLC data here. Theta Data plans to avail SIP EOD reports in the near future.
    #[pyo3(signature = (symbol, start_date, end_date, *, timeout_ms=None))]
    fn stock_history_eod(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<EodTickList>> {
        let mut request = self.client.market_data().stock_history_eod(symbol.as_str(), start_date.as_str(), end_date.as_str());
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        eod_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch end-of-day stock data for a date range. Returns OHLCV + bid/ask per trading day.
    ///
    /// Since the equity SIPs only generate a partial EOD report, Theta Data generates a national EOD report at 17:15 ET each day. ``created`` represents the datetime the report was generated and ``last_trade`` represents the datetime of the last trade. The quote in the response represents the last NBBO reported by CTA or UTP at the time of report generation. You can read more about EOD & OHLC data here. Theta Data plans to avail SIP EOD reports in the near future.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, start_date, end_date, *, timeout_ms=None))]
    fn stock_history_eod_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_history_eod(symbol.as_str(), start_date.as_str(), end_date.as_str());
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| eod_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `stock_history_eod`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `stock_history_eod()` for the sync signature; `stock_history_eod_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, start_date, end_date))]
    fn stock_history_eod_builder(
        &self,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
    ) -> StockHistoryEodBuilder {
        StockHistoryEodBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            start_date: start_date.into_string(),
            end_date: end_date.into_string(),
            timeout_ms: None,
        }
    }

    /// Fetch intraday OHLC bars for a stock on a single date.
    ///
    /// - Aggregated OHLC bars that use SIP rules for each bar. Time timestamp of the bar represents the opening time of the bar. For a trade to be part of the bar:  ``bar time`` <= ``trade time`` < ``bar timestamp + ivl``, where ivl is the specified interval size in milliseconds. 
    /// - Set the ``venue`` parameter to ``nqb`` to access current-day real-time historic data from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `venue`: `"nqb"`
    #[pyo3(signature = (symbol, *, date=None, interval=None, start_time=None, end_time=None, venue=None, start_date=None, end_date=None, timeout_ms=None))]
    fn stock_history_ohlc(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        venue: Option<PyStringArg>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<OhlcTickList>> {
        let mut request = self.client.market_data().stock_history_ohlc(symbol.as_str());
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = interval {
            request = request.interval(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = venue {
            request = request.venue(value.as_str());
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        ohlc_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch intraday OHLC bars for a stock on a single date.
    ///
    /// - Aggregated OHLC bars that use SIP rules for each bar. Time timestamp of the bar represents the opening time of the bar. For a trade to be part of the bar:  ``bar time`` <= ``trade time`` < ``bar timestamp + ivl``, where ivl is the specified interval size in milliseconds. 
    /// - Set the ``venue`` parameter to ``nqb`` to access current-day real-time historic data from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `venue`: `"nqb"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, *, date=None, interval=None, start_time=None, end_time=None, venue=None, start_date=None, end_date=None, timeout_ms=None))]
    fn stock_history_ohlc_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        venue: Option<PyStringArg>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_history_ohlc(symbol.as_str());
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| ohlc_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `stock_history_ohlc`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `stock_history_ohlc()` for the sync signature; `stock_history_ohlc_async()` for the awaitable companion.
    #[pyo3(signature = (symbol))]
    fn stock_history_ohlc_builder(
        &self,
        symbol: PyStringArg,
    ) -> StockHistoryOhlcBuilder {
        StockHistoryOhlcBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            date: None,
            interval: None,
            start_time: None,
            end_time: None,
            venue: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch all trades for a stock on a given date.
    ///
    /// Returns every trade reported by UTP & CTA. Set the ``venue`` parameter to ``nqb`` to access current-day real-time historic data from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `venue`: `"nqb"`
    #[pyo3(signature = (symbol, *, date=None, start_time=None, end_time=None, venue=None, start_date=None, end_date=None, timeout_ms=None))]
    fn stock_history_trade(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        venue: Option<PyStringArg>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<TradeTickList>> {
        let mut request = self.client.market_data().stock_history_trade(symbol.as_str());
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = venue {
            request = request.venue(value.as_str());
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        trade_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch all trades for a stock on a given date.
    ///
    /// Returns every trade reported by UTP & CTA. Set the ``venue`` parameter to ``nqb`` to access current-day real-time historic data from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `venue`: `"nqb"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, *, date=None, start_time=None, end_time=None, venue=None, start_date=None, end_date=None, timeout_ms=None))]
    fn stock_history_trade_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        venue: Option<PyStringArg>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_history_trade(symbol.as_str());
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `stock_history_trade`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `stock_history_trade()` for the sync signature; `stock_history_trade_async()` for the awaitable companion.
    #[pyo3(signature = (symbol))]
    fn stock_history_trade_builder(
        &self,
        symbol: PyStringArg,
    ) -> StockHistoryTradeBuilder {
        StockHistoryTradeBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            date: None,
            start_time: None,
            end_time: None,
            venue: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch NBBO quotes for a stock on a given date at a given interval.
    ///
    /// - Returns every NBBO quote reported by UTP and CTA. 
    /// - If the ``interval`` parameter is specified, the quote for each interval represents the last quote prior to the interval's timestamp. 
    /// - Set the ``venue`` parameter to ``nqb`` to access current-day real-time historic data from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `venue`: `"nqb"`
    #[pyo3(signature = (symbol, *, date=None, interval=None, start_time=None, end_time=None, venue=None, start_date=None, end_date=None, timeout_ms=None))]
    fn stock_history_quote(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        venue: Option<PyStringArg>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<QuoteTickList>> {
        let mut request = self.client.market_data().stock_history_quote(symbol.as_str());
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = interval {
            request = request.interval(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = venue {
            request = request.venue(value.as_str());
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        quote_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch NBBO quotes for a stock on a given date at a given interval.
    ///
    /// - Returns every NBBO quote reported by UTP and CTA. 
    /// - If the ``interval`` parameter is specified, the quote for each interval represents the last quote prior to the interval's timestamp. 
    /// - Set the ``venue`` parameter to ``nqb`` to access current-day real-time historic data from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `venue`: `"nqb"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, *, date=None, interval=None, start_time=None, end_time=None, venue=None, start_date=None, end_date=None, timeout_ms=None))]
    fn stock_history_quote_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        venue: Option<PyStringArg>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_history_quote(symbol.as_str());
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| quote_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `stock_history_quote`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `stock_history_quote()` for the sync signature; `stock_history_quote_async()` for the awaitable companion.
    #[pyo3(signature = (symbol))]
    fn stock_history_quote_builder(
        &self,
        symbol: PyStringArg,
    ) -> StockHistoryQuoteBuilder {
        StockHistoryQuoteBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            date: None,
            interval: None,
            start_time: None,
            end_time: None,
            venue: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch combined trade + quote ticks for a stock on a given date. Returns raw DataTable.
    ///
    /// Returns every trade reported by UTP & CTA paired with the last BBO quote reported by UTP or CTA at the time of trade. A quote is matched with a trade if its timestamp ``<=`` the trade timestamp. If you prefer to match quotes with timestamps that are ``<`` the trade timestamp, specify the ``exclusive`` parameter to ``true``. Set the ``venue`` parameter to ``nqb`` to access current-day real-time historic data from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `exclusive`: `true`
    /// - `venue`: `"nqb"`
    #[pyo3(signature = (symbol, *, date=None, start_time=None, end_time=None, exclusive=true, venue=None, start_date=None, end_date=None, timeout_ms=None))]
    fn stock_history_trade_quote(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        exclusive: Option<bool>,
        venue: Option<PyStringArg>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<TradeQuoteTickList>> {
        let mut request = self.client.market_data().stock_history_trade_quote(symbol.as_str());
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = exclusive {
            request = request.exclusive(value);
        }
        if let Some(value) = venue {
            request = request.venue(value.as_str());
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        trade_quote_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch combined trade + quote ticks for a stock on a given date. Returns raw DataTable.
    ///
    /// Returns every trade reported by UTP & CTA paired with the last BBO quote reported by UTP or CTA at the time of trade. A quote is matched with a trade if its timestamp ``<=`` the trade timestamp. If you prefer to match quotes with timestamps that are ``<`` the trade timestamp, specify the ``exclusive`` parameter to ``true``. Set the ``venue`` parameter to ``nqb`` to access current-day real-time historic data from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `exclusive`: `true`
    /// - `venue`: `"nqb"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, *, date=None, start_time=None, end_time=None, exclusive=None, venue=None, start_date=None, end_date=None, timeout_ms=None))]
    fn stock_history_trade_quote_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        exclusive: Option<bool>,
        venue: Option<PyStringArg>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_history_trade_quote(symbol.as_str());
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = exclusive {
                request = request.exclusive(value);
            }
            if let Some(value) = venue {
                request = request.venue(value.as_str());
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_quote_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `stock_history_trade_quote`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `stock_history_trade_quote()` for the sync signature; `stock_history_trade_quote_async()` for the awaitable companion.
    #[pyo3(signature = (symbol))]
    fn stock_history_trade_quote_builder(
        &self,
        symbol: PyStringArg,
    ) -> StockHistoryTradeQuoteBuilder {
        StockHistoryTradeQuoteBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            date: None,
            start_time: None,
            end_time: None,
            exclusive: None,
            venue: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch the trade at a specific time of day across a date range.
    ///
    /// #### Real-time request:
    /// - Returns a real-time session from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    /// - Returns a 15-minute delayed session from the UTP & CTA feeds account has the stocks value subscription subscription.
    ///
    /// #### Historical request:
    /// Returns the last trade reported by UTP & CTA feeds at a specified millisecond of the day.
    /// Trade condition mappings can be found here.
    ///
    /// Defaults (upstream):
    /// - `venue`: `"nqb"`
    #[pyo3(signature = (symbol, start_date, end_date, time_of_day, *, venue=None, timeout_ms=None))]
    fn stock_at_time_trade(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        time_of_day: PyTimeArg,
        venue: Option<PyStringArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<TradeTickList>> {
        let mut request = self.client.market_data().stock_at_time_trade(symbol.as_str(), start_date.as_str(), end_date.as_str(), time_of_day.as_str());
        if let Some(value) = venue {
            request = request.venue(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        trade_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch the trade at a specific time of day across a date range.
    ///
    /// #### Real-time request:
    /// - Returns a real-time session from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    /// - Returns a 15-minute delayed session from the UTP & CTA feeds account has the stocks value subscription subscription.
    ///
    /// #### Historical request:
    /// Returns the last trade reported by UTP & CTA feeds at a specified millisecond of the day.
    /// Trade condition mappings can be found here.
    ///
    /// Defaults (upstream):
    /// - `venue`: `"nqb"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, start_date, end_date, time_of_day, *, venue=None, timeout_ms=None))]
    fn stock_at_time_trade_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        time_of_day: PyTimeArg,
        venue: Option<PyStringArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_at_time_trade(symbol.as_str(), start_date.as_str(), end_date.as_str(), time_of_day.as_str());
            if let Some(value) = venue {
                request = request.venue(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `stock_at_time_trade`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `stock_at_time_trade()` for the sync signature; `stock_at_time_trade_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, start_date, end_date, time_of_day))]
    fn stock_at_time_trade_builder(
        &self,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        time_of_day: PyTimeArg,
    ) -> StockAtTimeTradeBuilder {
        StockAtTimeTradeBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            start_date: start_date.into_string(),
            end_date: end_date.into_string(),
            time_of_day: time_of_day.into_string(),
            venue: None,
            timeout_ms: None,
        }
    }

    /// Fetch the quote at a specific time of day across a date range.
    ///
    /// #### Real-time request:
    ///   - Subscription tier standard or higher will default to NQB.
    ///   - Real-time last BBO quote at-time_of_day-time from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    ///   - 15-minute delayed NBBO quote at-time_of_day-time from the UTP & CTA feeds account has the stocks value subscription subscription.
    ///
    /// #### Historical request:
    ///   Returns the last NBBO quote reported by UTP & CTA feeds at a specified millisecond of the day.
    ///
    /// Defaults (upstream):
    /// - `venue`: `"nqb"`
    #[pyo3(signature = (symbol, start_date, end_date, time_of_day, *, venue=None, timeout_ms=None))]
    fn stock_at_time_quote(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        time_of_day: PyTimeArg,
        venue: Option<PyStringArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<QuoteTickList>> {
        let mut request = self.client.market_data().stock_at_time_quote(symbol.as_str(), start_date.as_str(), end_date.as_str(), time_of_day.as_str());
        if let Some(value) = venue {
            request = request.venue(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        quote_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch the quote at a specific time of day across a date range.
    ///
    /// #### Real-time request:
    ///   - Subscription tier standard or higher will default to NQB.
    ///   - Real-time last BBO quote at-time_of_day-time from the Nasdaq Basic feed if the account has a stocks standard or pro subscription.
    ///   - 15-minute delayed NBBO quote at-time_of_day-time from the UTP & CTA feeds account has the stocks value subscription subscription.
    ///
    /// #### Historical request:
    ///   Returns the last NBBO quote reported by UTP & CTA feeds at a specified millisecond of the day.
    ///
    /// Defaults (upstream):
    /// - `venue`: `"nqb"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, start_date, end_date, time_of_day, *, venue=None, timeout_ms=None))]
    fn stock_at_time_quote_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        time_of_day: PyTimeArg,
        venue: Option<PyStringArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().stock_at_time_quote(symbol.as_str(), start_date.as_str(), end_date.as_str(), time_of_day.as_str());
            if let Some(value) = venue {
                request = request.venue(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| quote_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `stock_at_time_quote`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `stock_at_time_quote()` for the sync signature; `stock_at_time_quote_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, start_date, end_date, time_of_day))]
    fn stock_at_time_quote_builder(
        &self,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        time_of_day: PyTimeArg,
    ) -> StockAtTimeQuoteBuilder {
        StockAtTimeQuoteBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            start_date: start_date.into_string(),
            end_date: end_date.into_string(),
            time_of_day: time_of_day.into_string(),
            venue: None,
            timeout_ms: None,
        }
    }

    /// List all available option underlying symbols.
    ///
    /// A symbol can be defined as a unique identifier for a stock / underlying asset. Common terms also include: root, ticker, and underlying. This endpoint returns all traded symbols for options. This endpoint is updated overnight.
    #[pyo3(signature = (*, timeout_ms=None))]
    fn option_list_symbols(
        &self,
        py: Python<'_>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<StringList>> {
        let values: Vec<String> = run_blocking(py, async move {
            let call = self.client.market_data().option_list_symbols();
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        })?;
        strings_to_string_list(py, values, "symbol")
    }

    /// List all available option underlying symbols.
    ///
    /// A symbol can be defined as a unique identifier for a stock / underlying asset. Common terms also include: root, ticker, and underlying. This endpoint returns all traded symbols for options. This endpoint is updated overnight.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (*, timeout_ms=None))]
    fn option_list_symbols_async<'py>(
        &self,
        py: Python<'py>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let call = client.market_data().option_list_symbols();
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        }, |py, values| strings_to_string_list(py, values, "symbol").map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_list_symbols`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_list_symbols()` for the sync signature; `option_list_symbols_async()` for the awaitable companion.
    #[pyo3(signature = ())]
    fn option_list_symbols_builder(
        &self,
    ) -> OptionListSymbolsBuilder {
        OptionListSymbolsBuilder {
            client: self.client.clone(),
            timeout_ms: None,
        }
    }

    /// List available dates for an option contract by request type.
    ///
    /// Lists all dates of data that are available for an option with a given symbol, request type, and expiration.
    /// This endpoint is updated overnight.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    #[pyo3(signature = (request_type, symbol, expiration, *, timeout_ms=None))]
    fn option_list_dates(
        &self,
        py: Python<'_>,
        request_type: PyStringArg,
        symbol: PyStringArg,
        expiration: PyDateArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<StringList>> {
        let values: Vec<String> = run_blocking(py, async move {
            let call = self.client.market_data().option_list_dates(request_type.as_str(), symbol.as_str(), expiration.as_str());
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        })?;
        strings_to_string_list(py, values, "date")
    }

    /// List available dates for an option contract by request type.
    ///
    /// Lists all dates of data that are available for an option with a given symbol, request type, and expiration.
    /// This endpoint is updated overnight.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (request_type, symbol, expiration, *, timeout_ms=None))]
    fn option_list_dates_async<'py>(
        &self,
        py: Python<'py>,
        request_type: PyStringArg,
        symbol: PyStringArg,
        expiration: PyDateArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let call = client.market_data().option_list_dates(request_type.as_str(), symbol.as_str(), expiration.as_str());
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        }, |py, values| strings_to_string_list(py, values, "date").map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_list_dates`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_list_dates()` for the sync signature; `option_list_dates_async()` for the awaitable companion.
    #[pyo3(signature = (request_type, symbol, expiration))]
    fn option_list_dates_builder(
        &self,
        request_type: PyStringArg,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionListDatesBuilder {
        OptionListDatesBuilder {
            client: self.client.clone(),
            request_type: request_type.into_string(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            timeout_ms: None,
        }
    }

    /// List available expiration dates for an option underlying.
    ///
    /// Lists all dates of expirations that are available for an option with a given symbol.
    /// This endpoint is updated overnight.
    #[pyo3(signature = (symbol, *, timeout_ms=None))]
    fn option_list_expirations(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<StringList>> {
        let values: Vec<String> = run_blocking(py, async move {
            let call = self.client.market_data().option_list_expirations(symbol.as_str());
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        })?;
        strings_to_string_list(py, values, "expiration")
    }

    /// List available expiration dates for an option underlying.
    ///
    /// Lists all dates of expirations that are available for an option with a given symbol.
    /// This endpoint is updated overnight.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, *, timeout_ms=None))]
    fn option_list_expirations_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let call = client.market_data().option_list_expirations(symbol.as_str());
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        }, |py, values| strings_to_string_list(py, values, "expiration").map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_list_expirations`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_list_expirations()` for the sync signature; `option_list_expirations_async()` for the awaitable companion.
    #[pyo3(signature = (symbol))]
    fn option_list_expirations_builder(
        &self,
        symbol: PyStringArg,
    ) -> OptionListExpirationsBuilder {
        OptionListExpirationsBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            timeout_ms: None,
        }
    }

    /// List available strike prices for an option at a given expiration.
    ///
    /// Lists all strikes that are available for an option with a given symbol and expiration date.
    /// This endpoint is updated overnight.
    #[pyo3(signature = (symbol, expiration, *, timeout_ms=None))]
    fn option_list_strikes(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<StringList>> {
        let values: Vec<String> = run_blocking(py, async move {
            let call = self.client.market_data().option_list_strikes(symbol.as_str(), expiration.as_str());
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        })?;
        strings_to_string_list(py, values, "strike")
    }

    /// List available strike prices for an option at a given expiration.
    ///
    /// Lists all strikes that are available for an option with a given symbol and expiration date.
    /// This endpoint is updated overnight.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, timeout_ms=None))]
    fn option_list_strikes_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let call = client.market_data().option_list_strikes(symbol.as_str(), expiration.as_str());
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        }, |py, values| strings_to_string_list(py, values, "strike").map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_list_strikes`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_list_strikes()` for the sync signature; `option_list_strikes_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_list_strikes_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionListStrikesBuilder {
        OptionListStrikesBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            timeout_ms: None,
        }
    }

    /// List all option contracts traded or quoted on a given date, optionally filtered to a symbol.
    ///
    /// Lists all contracts that were traded or quoted on a particular date.
    ///
    /// If the ``symbol`` parameter is specified, the returned contracts will be filtered to match the symbol.
    /// When ``symbol`` is omitted the full universe of contracts for that date is returned.
    /// This endpoint is updated real-time.
    #[pyo3(signature = (request_type, date, *, symbol=None, max_dte=None, timeout_ms=None))]
    fn option_list_contracts(
        &self,
        py: Python<'_>,
        request_type: PyStringArg,
        date: PyDateArg,
        symbol: Option<PyStringArg>,
        max_dte: Option<i32>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<OptionContractList>> {
        let mut request = self.client.market_data().option_list_contracts(request_type.as_str(), date.as_str());
        if let Some(value) = symbol {
            request = request.symbol(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        option_contracts_to_pyclass_list(py, ticks)
    }

    /// List all option contracts traded or quoted on a given date, optionally filtered to a symbol.
    ///
    /// Lists all contracts that were traded or quoted on a particular date.
    ///
    /// If the ``symbol`` parameter is specified, the returned contracts will be filtered to match the symbol.
    /// When ``symbol`` is omitted the full universe of contracts for that date is returned.
    /// This endpoint is updated real-time.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (request_type, date, *, symbol=None, max_dte=None, timeout_ms=None))]
    fn option_list_contracts_async<'py>(
        &self,
        py: Python<'py>,
        request_type: PyStringArg,
        date: PyDateArg,
        symbol: Option<PyStringArg>,
        max_dte: Option<i32>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_list_contracts(request_type.as_str(), date.as_str());
            if let Some(value) = symbol {
                request = request.symbol(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| option_contracts_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_list_contracts`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_list_contracts()` for the sync signature; `option_list_contracts_async()` for the awaitable companion.
    #[pyo3(signature = (request_type, date))]
    fn option_list_contracts_builder(
        &self,
        request_type: PyStringArg,
        date: PyDateArg,
    ) -> OptionListContractsBuilder {
        OptionListContractsBuilder {
            client: self.client.clone(),
            request_type: request_type.into_string(),
            date: date.into_string(),
            symbol: None,
            max_dte: None,
            timeout_ms: None,
        }
    }

    /// Get the latest OHLC snapshot for an option contract.
    ///
    /// - Retrieve a real-time last ohlc of an option contract for the trading day.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, max_dte=None, strike_range=None, min_time=None, timeout_ms=None))]
    fn option_snapshot_ohlc(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<pyo3::types::PyList>> {
        let mut request = self.client.market_data().option_snapshot_ohlc(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        ohlc_ticks_vec_to_pylist(py, ticks)
    }

    /// Get the latest OHLC snapshot for an option contract.
    ///
    /// - Retrieve a real-time last ohlc of an option contract for the trading day.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, max_dte=None, strike_range=None, min_time=None, timeout_ms=None))]
    fn option_snapshot_ohlc_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_ohlc(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| ohlc_ticks_vec_to_pylist(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_snapshot_ohlc`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_snapshot_ohlc()` for the sync signature; `option_snapshot_ohlc_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_snapshot_ohlc_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionSnapshotOhlcBuilder {
        OptionSnapshotOhlcBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            max_dte: None,
            strike_range: None,
            min_time: None,
            timeout_ms: None,
        }
    }

    /// Get the latest trade snapshot for an option contract.
    ///
    /// - Retrieve the real-time last trade of an option contract.
    /// - This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, strike_range=None, min_time=None, timeout_ms=None))]
    fn option_snapshot_trade(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<pyo3::types::PyList>> {
        let mut request = self.client.market_data().option_snapshot_trade(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        trade_ticks_vec_to_pylist(py, ticks)
    }

    /// Get the latest trade snapshot for an option contract.
    ///
    /// - Retrieve the real-time last trade of an option contract.
    /// - This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, strike_range=None, min_time=None, timeout_ms=None))]
    fn option_snapshot_trade_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_trade(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_ticks_vec_to_pylist(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_snapshot_trade`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_snapshot_trade()` for the sync signature; `option_snapshot_trade_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_snapshot_trade_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionSnapshotTradeBuilder {
        OptionSnapshotTradeBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            strike_range: None,
            min_time: None,
            timeout_ms: None,
        }
    }

    /// Get the latest NBBO quote snapshot for an option contract.
    ///
    /// - Retrieve a real-time last NBBO quote of an option contract.
    /// - This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, max_dte=None, strike_range=None, min_time=None, timeout_ms=None))]
    fn option_snapshot_quote(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<pyo3::types::PyList>> {
        let mut request = self.client.market_data().option_snapshot_quote(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        quote_ticks_vec_to_pylist(py, ticks)
    }

    /// Get the latest NBBO quote snapshot for an option contract.
    ///
    /// - Retrieve a real-time last NBBO quote of an option contract.
    /// - This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, max_dte=None, strike_range=None, min_time=None, timeout_ms=None))]
    fn option_snapshot_quote_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_quote(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| quote_ticks_vec_to_pylist(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_snapshot_quote`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_snapshot_quote()` for the sync signature; `option_snapshot_quote_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_snapshot_quote_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionSnapshotQuoteBuilder {
        OptionSnapshotQuoteBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            max_dte: None,
            strike_range: None,
            min_time: None,
            timeout_ms: None,
        }
    }

    /// Get the latest open interest snapshot for an option contract.
    ///
    /// - Retrieve the last open interest message of an option contract.
    /// - Open interest is reported around 06:30 ET every morning by OPRA and reflects the open interest at the end of the previous trading day.
    /// - This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, max_dte=None, strike_range=None, min_time=None, timeout_ms=None))]
    fn option_snapshot_open_interest(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<pyo3::types::PyList>> {
        let mut request = self.client.market_data().option_snapshot_open_interest(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        open_interest_ticks_vec_to_pylist(py, ticks)
    }

    /// Get the latest open interest snapshot for an option contract.
    ///
    /// - Retrieve the last open interest message of an option contract.
    /// - Open interest is reported around 06:30 ET every morning by OPRA and reflects the open interest at the end of the previous trading day.
    /// - This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, max_dte=None, strike_range=None, min_time=None, timeout_ms=None))]
    fn option_snapshot_open_interest_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_open_interest(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| open_interest_ticks_vec_to_pylist(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_snapshot_open_interest`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_snapshot_open_interest()` for the sync signature; `option_snapshot_open_interest_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_snapshot_open_interest_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionSnapshotOpenInterestBuilder {
        OptionSnapshotOpenInterestBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            max_dte: None,
            strike_range: None,
            min_time: None,
            timeout_ms: None,
        }
    }

    /// Get the latest market value snapshot for an option contract.
    ///
    /// * Returns a real-time market value derived from the last NBBO quote of an option contract.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, max_dte=None, strike_range=None, min_time=None, timeout_ms=None))]
    fn option_snapshot_market_value(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<pyo3::types::PyList>> {
        let mut request = self.client.market_data().option_snapshot_market_value(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        market_value_ticks_vec_to_pylist(py, ticks)
    }

    /// Get the latest market value snapshot for an option contract.
    ///
    /// * Returns a real-time market value derived from the last NBBO quote of an option contract.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, max_dte=None, strike_range=None, min_time=None, timeout_ms=None))]
    fn option_snapshot_market_value_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_market_value(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| market_value_ticks_vec_to_pylist(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_snapshot_market_value`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_snapshot_market_value()` for the sync signature; `option_snapshot_market_value_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_snapshot_market_value_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionSnapshotMarketValueBuilder {
        OptionSnapshotMarketValueBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            max_dte: None,
            strike_range: None,
            min_time: None,
            timeout_ms: None,
        }
    }

    /// Get implied volatility snapshot for an option contract (from ThetaData server).
    ///
    /// Returns implied volatilies calculated using the national best bid, mid, and ask price
    /// of the option respectively. The underlying price represents whatever the last underlying price was at the
    /// ``underlying_timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    /// - `use_market_value`: `false`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, annual_dividend=None, rate_type=None, rate_value=None, stock_price=None, version=None, max_dte=None, strike_range=None, min_time=None, use_market_value=false, timeout_ms=None))]
    fn option_snapshot_greeks_implied_volatility(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        stock_price: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        use_market_value: Option<bool>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<pyo3::types::PyList>> {
        let mut request = self.client.market_data().option_snapshot_greeks_implied_volatility(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = annual_dividend {
            request = request.annual_dividend(value);
        }
        if let Some(value) = rate_type {
            request = request.rate_type(value.as_str());
        }
        if let Some(value) = rate_value {
            request = request.rate_value(value);
        }
        if let Some(value) = stock_price {
            request = request.stock_price(value);
        }
        if let Some(value) = version {
            request = request.version(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(value) = use_market_value {
            request = request.use_market_value(value);
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        iv_ticks_vec_to_pylist(py, ticks)
    }

    /// Get implied volatility snapshot for an option contract (from ThetaData server).
    ///
    /// Returns implied volatilies calculated using the national best bid, mid, and ask price
    /// of the option respectively. The underlying price represents whatever the last underlying price was at the
    /// ``underlying_timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    /// - `use_market_value`: `false`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, annual_dividend=None, rate_type=None, rate_value=None, stock_price=None, version=None, max_dte=None, strike_range=None, min_time=None, use_market_value=None, timeout_ms=None))]
    fn option_snapshot_greeks_implied_volatility_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        stock_price: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        use_market_value: Option<bool>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_greeks_implied_volatility(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = annual_dividend {
                request = request.annual_dividend(value);
            }
            if let Some(value) = rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = rate_value {
                request = request.rate_value(value);
            }
            if let Some(value) = stock_price {
                request = request.stock_price(value);
            }
            if let Some(value) = version {
                request = request.version(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(value) = use_market_value {
                request = request.use_market_value(value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| iv_ticks_vec_to_pylist(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_snapshot_greeks_implied_volatility`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_snapshot_greeks_implied_volatility()` for the sync signature; `option_snapshot_greeks_implied_volatility_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_snapshot_greeks_implied_volatility_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionSnapshotGreeksImpliedVolatilityBuilder {
        OptionSnapshotGreeksImpliedVolatilityBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            annual_dividend: None,
            rate_type: None,
            rate_value: None,
            stock_price: None,
            version: None,
            max_dte: None,
            strike_range: None,
            min_time: None,
            use_market_value: None,
            timeout_ms: None,
        }
    }

    /// Get all Greeks snapshot for an option contract (from ThetaData server).
    ///
    /// - Retrieve a real-time last greeks calculation for all option contracts that lie on a provided expiration.
    /// > This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    /// - `use_market_value`: `false`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, annual_dividend=None, rate_type=None, rate_value=None, stock_price=None, version=None, max_dte=None, strike_range=None, min_time=None, use_market_value=false, timeout_ms=None))]
    fn option_snapshot_greeks_all(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        stock_price: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        use_market_value: Option<bool>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<pyo3::types::PyList>> {
        let mut request = self.client.market_data().option_snapshot_greeks_all(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = annual_dividend {
            request = request.annual_dividend(value);
        }
        if let Some(value) = rate_type {
            request = request.rate_type(value.as_str());
        }
        if let Some(value) = rate_value {
            request = request.rate_value(value);
        }
        if let Some(value) = stock_price {
            request = request.stock_price(value);
        }
        if let Some(value) = version {
            request = request.version(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(value) = use_market_value {
            request = request.use_market_value(value);
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        greeks_all_ticks_vec_to_pylist(py, ticks)
    }

    /// Get all Greeks snapshot for an option contract (from ThetaData server).
    ///
    /// - Retrieve a real-time last greeks calculation for all option contracts that lie on a provided expiration.
    /// > This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    /// - `use_market_value`: `false`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, annual_dividend=None, rate_type=None, rate_value=None, stock_price=None, version=None, max_dte=None, strike_range=None, min_time=None, use_market_value=None, timeout_ms=None))]
    fn option_snapshot_greeks_all_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        stock_price: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        use_market_value: Option<bool>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_greeks_all(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = annual_dividend {
                request = request.annual_dividend(value);
            }
            if let Some(value) = rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = rate_value {
                request = request.rate_value(value);
            }
            if let Some(value) = stock_price {
                request = request.stock_price(value);
            }
            if let Some(value) = version {
                request = request.version(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(value) = use_market_value {
                request = request.use_market_value(value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_all_ticks_vec_to_pylist(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_snapshot_greeks_all`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_snapshot_greeks_all()` for the sync signature; `option_snapshot_greeks_all_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_snapshot_greeks_all_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionSnapshotGreeksAllBuilder {
        OptionSnapshotGreeksAllBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            annual_dividend: None,
            rate_type: None,
            rate_value: None,
            stock_price: None,
            version: None,
            max_dte: None,
            strike_range: None,
            min_time: None,
            use_market_value: None,
            timeout_ms: None,
        }
    }

    /// Get first-order Greeks snapshot (delta, theta, rho) for an option contract.
    ///
    /// - Retrieve a real-time last greeks calculation for all option contracts that lie on a provided expiration.
    /// > This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    /// - `use_market_value`: `false`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, annual_dividend=None, rate_type=None, rate_value=None, stock_price=None, version=None, max_dte=None, strike_range=None, min_time=None, use_market_value=false, timeout_ms=None))]
    fn option_snapshot_greeks_first_order(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        stock_price: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        use_market_value: Option<bool>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<pyo3::types::PyList>> {
        let mut request = self.client.market_data().option_snapshot_greeks_first_order(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = annual_dividend {
            request = request.annual_dividend(value);
        }
        if let Some(value) = rate_type {
            request = request.rate_type(value.as_str());
        }
        if let Some(value) = rate_value {
            request = request.rate_value(value);
        }
        if let Some(value) = stock_price {
            request = request.stock_price(value);
        }
        if let Some(value) = version {
            request = request.version(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(value) = use_market_value {
            request = request.use_market_value(value);
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        greeks_first_order_ticks_vec_to_pylist(py, ticks)
    }

    /// Get first-order Greeks snapshot (delta, theta, rho) for an option contract.
    ///
    /// - Retrieve a real-time last greeks calculation for all option contracts that lie on a provided expiration.
    /// > This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    /// - `use_market_value`: `false`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, annual_dividend=None, rate_type=None, rate_value=None, stock_price=None, version=None, max_dte=None, strike_range=None, min_time=None, use_market_value=None, timeout_ms=None))]
    fn option_snapshot_greeks_first_order_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        stock_price: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        use_market_value: Option<bool>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_greeks_first_order(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = annual_dividend {
                request = request.annual_dividend(value);
            }
            if let Some(value) = rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = rate_value {
                request = request.rate_value(value);
            }
            if let Some(value) = stock_price {
                request = request.stock_price(value);
            }
            if let Some(value) = version {
                request = request.version(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(value) = use_market_value {
                request = request.use_market_value(value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_first_order_ticks_vec_to_pylist(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_snapshot_greeks_first_order`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_snapshot_greeks_first_order()` for the sync signature; `option_snapshot_greeks_first_order_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_snapshot_greeks_first_order_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionSnapshotGreeksFirstOrderBuilder {
        OptionSnapshotGreeksFirstOrderBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            annual_dividend: None,
            rate_type: None,
            rate_value: None,
            stock_price: None,
            version: None,
            max_dte: None,
            strike_range: None,
            min_time: None,
            use_market_value: None,
            timeout_ms: None,
        }
    }

    /// Get second-order Greeks snapshot (gamma, vanna, charm) for an option contract.
    ///
    /// - Retrieve a real-time last second order greeks calculation for all option contracts that lie on a provided expiration.
    /// > This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    /// - `use_market_value`: `false`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, annual_dividend=None, rate_type=None, rate_value=None, stock_price=None, version=None, max_dte=None, strike_range=None, min_time=None, use_market_value=false, timeout_ms=None))]
    fn option_snapshot_greeks_second_order(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        stock_price: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        use_market_value: Option<bool>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<pyo3::types::PyList>> {
        let mut request = self.client.market_data().option_snapshot_greeks_second_order(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = annual_dividend {
            request = request.annual_dividend(value);
        }
        if let Some(value) = rate_type {
            request = request.rate_type(value.as_str());
        }
        if let Some(value) = rate_value {
            request = request.rate_value(value);
        }
        if let Some(value) = stock_price {
            request = request.stock_price(value);
        }
        if let Some(value) = version {
            request = request.version(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(value) = use_market_value {
            request = request.use_market_value(value);
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        greeks_second_order_ticks_vec_to_pylist(py, ticks)
    }

    /// Get second-order Greeks snapshot (gamma, vanna, charm) for an option contract.
    ///
    /// - Retrieve a real-time last second order greeks calculation for all option contracts that lie on a provided expiration.
    /// > This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    /// - `use_market_value`: `false`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, annual_dividend=None, rate_type=None, rate_value=None, stock_price=None, version=None, max_dte=None, strike_range=None, min_time=None, use_market_value=None, timeout_ms=None))]
    fn option_snapshot_greeks_second_order_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        stock_price: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        use_market_value: Option<bool>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_greeks_second_order(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = annual_dividend {
                request = request.annual_dividend(value);
            }
            if let Some(value) = rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = rate_value {
                request = request.rate_value(value);
            }
            if let Some(value) = stock_price {
                request = request.stock_price(value);
            }
            if let Some(value) = version {
                request = request.version(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(value) = use_market_value {
                request = request.use_market_value(value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_second_order_ticks_vec_to_pylist(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_snapshot_greeks_second_order`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_snapshot_greeks_second_order()` for the sync signature; `option_snapshot_greeks_second_order_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_snapshot_greeks_second_order_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionSnapshotGreeksSecondOrderBuilder {
        OptionSnapshotGreeksSecondOrderBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            annual_dividend: None,
            rate_type: None,
            rate_value: None,
            stock_price: None,
            version: None,
            max_dte: None,
            strike_range: None,
            min_time: None,
            use_market_value: None,
            timeout_ms: None,
        }
    }

    /// Get third-order Greeks snapshot (speed, color, ultima) for an option contract.
    ///
    /// - Retrieve a real-time last third order greeks calculation for all option contracts that lie on a provided expiration.
    /// > This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    /// - `use_market_value`: `false`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, annual_dividend=None, rate_type=None, rate_value=None, stock_price=None, version=None, max_dte=None, strike_range=None, min_time=None, use_market_value=false, timeout_ms=None))]
    fn option_snapshot_greeks_third_order(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        stock_price: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        use_market_value: Option<bool>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<pyo3::types::PyList>> {
        let mut request = self.client.market_data().option_snapshot_greeks_third_order(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = annual_dividend {
            request = request.annual_dividend(value);
        }
        if let Some(value) = rate_type {
            request = request.rate_type(value.as_str());
        }
        if let Some(value) = rate_value {
            request = request.rate_value(value);
        }
        if let Some(value) = stock_price {
            request = request.stock_price(value);
        }
        if let Some(value) = version {
            request = request.version(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(value) = use_market_value {
            request = request.use_market_value(value);
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        greeks_third_order_ticks_vec_to_pylist(py, ticks)
    }

    /// Get third-order Greeks snapshot (speed, color, ultima) for an option contract.
    ///
    /// - Retrieve a real-time last third order greeks calculation for all option contracts that lie on a provided expiration.
    /// > This endpoint will return no data if the market was closed for the day. Theta Data resets the snapshot cache at midnight ET every night.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    /// - `use_market_value`: `false`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, annual_dividend=None, rate_type=None, rate_value=None, stock_price=None, version=None, max_dte=None, strike_range=None, min_time=None, use_market_value=None, timeout_ms=None))]
    fn option_snapshot_greeks_third_order_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        stock_price: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        min_time: Option<PyTimeArg>,
        use_market_value: Option<bool>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_snapshot_greeks_third_order(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = annual_dividend {
                request = request.annual_dividend(value);
            }
            if let Some(value) = rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = rate_value {
                request = request.rate_value(value);
            }
            if let Some(value) = stock_price {
                request = request.stock_price(value);
            }
            if let Some(value) = version {
                request = request.version(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(value) = use_market_value {
                request = request.use_market_value(value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_third_order_ticks_vec_to_pylist(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_snapshot_greeks_third_order`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_snapshot_greeks_third_order()` for the sync signature; `option_snapshot_greeks_third_order_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_snapshot_greeks_third_order_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionSnapshotGreeksThirdOrderBuilder {
        OptionSnapshotGreeksThirdOrderBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            annual_dividend: None,
            rate_type: None,
            rate_value: None,
            stock_price: None,
            version: None,
            max_dte: None,
            strike_range: None,
            min_time: None,
            use_market_value: None,
            timeout_ms: None,
        }
    }

    /// Fetch end-of-day option data for a contract over a date range.
    ///
    /// - Since OPRA does not provide a national EOD report for options, Theta Data generates a national EOD report at 17:15 ET each day.
    /// - ``created`` represents the datetime the report was generated and ``last_trade`` represents the datetime of the last trade. 
    /// - The quote in the response represents the last NBBO reported by OPRA at the time of report generation. 
    /// - You can read more about EOD & OHLC data here.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    #[pyo3(signature = (symbol, expiration, start_date, end_date, *, strike=None, right=None, max_dte=None, strike_range=None, timeout_ms=None))]
    fn option_history_eod(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<EodTickList>> {
        let mut request = self.client.market_data().option_history_eod(symbol.as_str(), expiration.as_str(), start_date.as_str(), end_date.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        eod_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch end-of-day option data for a contract over a date range.
    ///
    /// - Since OPRA does not provide a national EOD report for options, Theta Data generates a national EOD report at 17:15 ET each day.
    /// - ``created`` represents the datetime the report was generated and ``last_trade`` represents the datetime of the last trade. 
    /// - The quote in the response represents the last NBBO reported by OPRA at the time of report generation. 
    /// - You can read more about EOD & OHLC data here.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, start_date, end_date, *, strike=None, right=None, max_dte=None, strike_range=None, timeout_ms=None))]
    fn option_history_eod_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_eod(symbol.as_str(), expiration.as_str(), start_date.as_str(), end_date.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| eod_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_eod`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_eod()` for the sync signature; `option_history_eod_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration, start_date, end_date))]
    fn option_history_eod_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
    ) -> OptionHistoryEodBuilder {
        OptionHistoryEodBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            start_date: start_date.into_string(),
            end_date: end_date.into_string(),
            strike: None,
            right: None,
            max_dte: None,
            strike_range: None,
            timeout_ms: None,
        }
    }

    /// Fetch intraday OHLC bars for an option contract.
    ///
    /// - Aggregated OHLC bars that use SIP rules for each bar. 
    /// - Time timestamp of the bar represents the opening time of the bar. For a trade to be part of the bar:  ``bar timestamp`` <= ``trade time`` < ``bar timestamp + interval``.
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, interval=None, start_time=None, end_time=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_ohlc(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<OhlcTickList>> {
        let mut request = self.client.market_data().option_history_ohlc(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = interval {
            request = request.interval(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        ohlc_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch intraday OHLC bars for an option contract.
    ///
    /// - Aggregated OHLC bars that use SIP rules for each bar. 
    /// - Time timestamp of the bar represents the opening time of the bar. For a trade to be part of the bar:  ``bar timestamp`` <= ``trade time`` < ``bar timestamp + interval``.
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, interval=None, start_time=None, end_time=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_ohlc_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_ohlc(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| ohlc_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_ohlc`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_ohlc()` for the sync signature; `option_history_ohlc_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_history_ohlc_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionHistoryOhlcBuilder {
        OptionHistoryOhlcBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            date: None,
            interval: None,
            start_time: None,
            end_time: None,
            strike_range: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch all trades for an option contract on a given date.
    ///
    /// - Returns every trade reported by OPRA. 
    /// - Trade condition mappings can be found here.
    /// - Extended trade conditions are not reported by OPRA for options, so they can be ignored.
    /// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, start_time=None, end_time=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_trade(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<TradeTickList>> {
        let mut request = self.client.market_data().option_history_trade(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        trade_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch all trades for an option contract on a given date.
    ///
    /// - Returns every trade reported by OPRA. 
    /// - Trade condition mappings can be found here.
    /// - Extended trade conditions are not reported by OPRA for options, so they can be ignored.
    /// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, start_time=None, end_time=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_trade_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_trade`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_trade()` for the sync signature; `option_history_trade_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_history_trade_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionHistoryTradeBuilder {
        OptionHistoryTradeBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            date: None,
            start_time: None,
            end_time: None,
            max_dte: None,
            strike_range: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch NBBO quotes for an option contract on a given date.
    ///
    /// - Returns every NBBO quote reported by OPRA. 
    /// - If the ``interval`` parameter is specified, the quote for each interval represents the last quote at the interval's timestamp.
    /// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, interval=None, start_time=None, end_time=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_quote(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<QuoteTickList>> {
        let mut request = self.client.market_data().option_history_quote(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = interval {
            request = request.interval(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        quote_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch NBBO quotes for an option contract on a given date.
    ///
    /// - Returns every NBBO quote reported by OPRA. 
    /// - If the ``interval`` parameter is specified, the quote for each interval represents the last quote at the interval's timestamp.
    /// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, interval=None, start_time=None, end_time=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_quote_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_quote(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| quote_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_quote`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_quote()` for the sync signature; `option_history_quote_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_history_quote_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionHistoryQuoteBuilder {
        OptionHistoryQuoteBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            date: None,
            interval: None,
            start_time: None,
            end_time: None,
            max_dte: None,
            strike_range: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch combined trade + quote ticks for an option contract.
    ///
    /// - Returns every trade reported by OPRA paired with the last NBBO quote reported by OPRA at the time of trade.
    /// - A quote is matched with a trade if its timestamp ``<=`` the trade timestamp. 
    /// - To match trades with quotes timestamps that are ``<`` the trade timestamp, specify the ``exclusive``parameter to ``true``. After thorough testing, we have determined that using ``exclusive=true`` might yield better results for various applications.
    /// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `exclusive`: `true`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, start_time=None, end_time=None, exclusive=true, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_trade_quote(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        exclusive: Option<bool>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<TradeQuoteTickList>> {
        let mut request = self.client.market_data().option_history_trade_quote(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = exclusive {
            request = request.exclusive(value);
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        trade_quote_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch combined trade + quote ticks for an option contract.
    ///
    /// - Returns every trade reported by OPRA paired with the last NBBO quote reported by OPRA at the time of trade.
    /// - A quote is matched with a trade if its timestamp ``<=`` the trade timestamp. 
    /// - To match trades with quotes timestamps that are ``<`` the trade timestamp, specify the ``exclusive``parameter to ``true``. After thorough testing, we have determined that using ``exclusive=true`` might yield better results for various applications.
    /// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `exclusive`: `true`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, start_time=None, end_time=None, exclusive=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_trade_quote_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        exclusive: Option<bool>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_quote(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = exclusive {
                request = request.exclusive(value);
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_quote_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_trade_quote`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_trade_quote()` for the sync signature; `option_history_trade_quote_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_history_trade_quote_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionHistoryTradeQuoteBuilder {
        OptionHistoryTradeQuoteBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            date: None,
            start_time: None,
            end_time: None,
            exclusive: None,
            max_dte: None,
            strike_range: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch open interest history for an option contract.
    ///
    /// - Open Interest is normally reported once per day by OPRA at approximately 06:30 ET.
    /// - A new open interest message might not be sent by OPRA if there is no open interest for the option contract.
    /// - The reported open interest represents the open interest at the end of the previous trading day.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_open_interest(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<OpenInterestTickList>> {
        let mut request = self.client.market_data().option_history_open_interest(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        open_interest_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch open interest history for an option contract.
    ///
    /// - Open Interest is normally reported once per day by OPRA at approximately 06:30 ET.
    /// - A new open interest message might not be sent by OPRA if there is no open interest for the option contract.
    /// - The reported open interest represents the open interest at the end of the previous trading day.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_open_interest_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_open_interest(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| open_interest_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_open_interest`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_open_interest()` for the sync signature; `option_history_open_interest_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_history_open_interest_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionHistoryOpenInterestBuilder {
        OptionHistoryOpenInterestBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            date: None,
            max_dte: None,
            strike_range: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch end-of-day Greeks history for an option contract.
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration.
    /// - Uses Theta Data's EOD reports that get generated at 17:15 ET each day. The closing option price and closing underlying price are used for the greeks calculation.
    /// - **Any ``expiration=*`` request must be made day by day.**
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    /// - `underlyer_use_nbbo`: `false`
    #[pyo3(signature = (symbol, expiration, start_date, end_date, *, strike=None, right=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, underlyer_use_nbbo=false, max_dte=None, strike_range=None, timeout_ms=None))]
    fn option_history_greeks_eod(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        underlyer_use_nbbo: Option<bool>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<GreeksEodTickList>> {
        let mut request = self.client.market_data().option_history_greeks_eod(symbol.as_str(), expiration.as_str(), start_date.as_str(), end_date.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = annual_dividend {
            request = request.annual_dividend(value);
        }
        if let Some(value) = rate_type {
            request = request.rate_type(value.as_str());
        }
        if let Some(value) = rate_value {
            request = request.rate_value(value);
        }
        if let Some(value) = version {
            request = request.version(value.as_str());
        }
        if let Some(value) = underlyer_use_nbbo {
            request = request.underlyer_use_nbbo(value);
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        greeks_eod_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch end-of-day Greeks history for an option contract.
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration.
    /// - Uses Theta Data's EOD reports that get generated at 17:15 ET each day. The closing option price and closing underlying price are used for the greeks calculation.
    /// - **Any ``expiration=*`` request must be made day by day.**
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    /// - `underlyer_use_nbbo`: `false`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, start_date, end_date, *, strike=None, right=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, underlyer_use_nbbo=None, max_dte=None, strike_range=None, timeout_ms=None))]
    fn option_history_greeks_eod_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        underlyer_use_nbbo: Option<bool>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_eod(symbol.as_str(), expiration.as_str(), start_date.as_str(), end_date.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = annual_dividend {
                request = request.annual_dividend(value);
            }
            if let Some(value) = rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = rate_value {
                request = request.rate_value(value);
            }
            if let Some(value) = version {
                request = request.version(value.as_str());
            }
            if let Some(value) = underlyer_use_nbbo {
                request = request.underlyer_use_nbbo(value);
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_eod_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_greeks_eod`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_greeks_eod()` for the sync signature; `option_history_greeks_eod_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration, start_date, end_date))]
    fn option_history_greeks_eod_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
    ) -> OptionHistoryGreeksEodBuilder {
        OptionHistoryGreeksEodBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            start_date: start_date.into_string(),
            end_date: end_date.into_string(),
            strike: None,
            right: None,
            annual_dividend: None,
            rate_type: None,
            rate_value: None,
            version: None,
            underlyer_use_nbbo: None,
            max_dte: None,
            strike_range: None,
            timeout_ms: None,
        }
    }

    /// Fetch all Greeks history for an option contract (intraday, sampled by interval).
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration. 
    /// - Calculated using the option and underlying midpoint price. If an interval size is specified (*highly recommended*), the option quote used in the calculation follows the same rules as the quote endpoint. 
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, interval=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_greeks_all(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<GreeksAllTickList>> {
        let mut request = self.client.market_data().option_history_greeks_all(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = interval {
            request = request.interval(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = annual_dividend {
            request = request.annual_dividend(value);
        }
        if let Some(value) = rate_type {
            request = request.rate_type(value.as_str());
        }
        if let Some(value) = rate_value {
            request = request.rate_value(value);
        }
        if let Some(value) = version {
            request = request.version(value.as_str());
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        greeks_all_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch all Greeks history for an option contract (intraday, sampled by interval).
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration. 
    /// - Calculated using the option and underlying midpoint price. If an interval size is specified (*highly recommended*), the option quote used in the calculation follows the same rules as the quote endpoint. 
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, interval=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_greeks_all_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_all(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = annual_dividend {
                request = request.annual_dividend(value);
            }
            if let Some(value) = rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = rate_value {
                request = request.rate_value(value);
            }
            if let Some(value) = version {
                request = request.version(value.as_str());
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_all_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_greeks_all`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_greeks_all()` for the sync signature; `option_history_greeks_all_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_history_greeks_all_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionHistoryGreeksAllBuilder {
        OptionHistoryGreeksAllBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            date: None,
            interval: None,
            start_time: None,
            end_time: None,
            annual_dividend: None,
            rate_type: None,
            rate_value: None,
            version: None,
            strike_range: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch all Greeks on each trade for an option contract.
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration. 
    /// - Calculates greeks for every trade reported by OPRA.
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_trade_greeks_all(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<TradeGreeksAllTickList>> {
        let mut request = self.client.market_data().option_history_trade_greeks_all(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = annual_dividend {
            request = request.annual_dividend(value);
        }
        if let Some(value) = rate_type {
            request = request.rate_type(value.as_str());
        }
        if let Some(value) = rate_value {
            request = request.rate_value(value);
        }
        if let Some(value) = version {
            request = request.version(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        trade_greeks_all_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch all Greeks on each trade for an option contract.
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration. 
    /// - Calculates greeks for every trade reported by OPRA.
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_trade_greeks_all_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_all(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = annual_dividend {
                request = request.annual_dividend(value);
            }
            if let Some(value) = rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = rate_value {
                request = request.rate_value(value);
            }
            if let Some(value) = version {
                request = request.version(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_greeks_all_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_trade_greeks_all`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_trade_greeks_all()` for the sync signature; `option_history_trade_greeks_all_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_history_trade_greeks_all_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionHistoryTradeGreeksAllBuilder {
        OptionHistoryTradeGreeksAllBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            date: None,
            start_time: None,
            end_time: None,
            annual_dividend: None,
            rate_type: None,
            rate_value: None,
            version: None,
            max_dte: None,
            strike_range: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch first-order Greeks history (intraday, sampled by interval).
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration. 
    /// - Calculated using the option and underlying midpoint price. If an interval size is specified (*highly recommended*), the option quote used in the calculation follows the same rules as the quote endpoint. 
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, interval=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_greeks_first_order(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<GreeksFirstOrderTickList>> {
        let mut request = self.client.market_data().option_history_greeks_first_order(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = interval {
            request = request.interval(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = annual_dividend {
            request = request.annual_dividend(value);
        }
        if let Some(value) = rate_type {
            request = request.rate_type(value.as_str());
        }
        if let Some(value) = rate_value {
            request = request.rate_value(value);
        }
        if let Some(value) = version {
            request = request.version(value.as_str());
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        greeks_first_order_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch first-order Greeks history (intraday, sampled by interval).
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration. 
    /// - Calculated using the option and underlying midpoint price. If an interval size is specified (*highly recommended*), the option quote used in the calculation follows the same rules as the quote endpoint. 
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, interval=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_greeks_first_order_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_first_order(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = annual_dividend {
                request = request.annual_dividend(value);
            }
            if let Some(value) = rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = rate_value {
                request = request.rate_value(value);
            }
            if let Some(value) = version {
                request = request.version(value.as_str());
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_first_order_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_greeks_first_order`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_greeks_first_order()` for the sync signature; `option_history_greeks_first_order_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_history_greeks_first_order_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionHistoryGreeksFirstOrderBuilder {
        OptionHistoryGreeksFirstOrderBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            date: None,
            interval: None,
            start_time: None,
            end_time: None,
            annual_dividend: None,
            rate_type: None,
            rate_value: None,
            version: None,
            strike_range: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch first-order Greeks on each trade for an option contract.
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration.
    /// - Calculates greeks for every trade reported by OPRA.
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_trade_greeks_first_order(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<TradeGreeksFirstOrderTickList>> {
        let mut request = self.client.market_data().option_history_trade_greeks_first_order(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = annual_dividend {
            request = request.annual_dividend(value);
        }
        if let Some(value) = rate_type {
            request = request.rate_type(value.as_str());
        }
        if let Some(value) = rate_value {
            request = request.rate_value(value);
        }
        if let Some(value) = version {
            request = request.version(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        trade_greeks_first_order_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch first-order Greeks on each trade for an option contract.
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration.
    /// - Calculates greeks for every trade reported by OPRA.
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_trade_greeks_first_order_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_first_order(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = annual_dividend {
                request = request.annual_dividend(value);
            }
            if let Some(value) = rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = rate_value {
                request = request.rate_value(value);
            }
            if let Some(value) = version {
                request = request.version(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_greeks_first_order_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_trade_greeks_first_order`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_trade_greeks_first_order()` for the sync signature; `option_history_trade_greeks_first_order_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_history_trade_greeks_first_order_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionHistoryTradeGreeksFirstOrderBuilder {
        OptionHistoryTradeGreeksFirstOrderBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            date: None,
            start_time: None,
            end_time: None,
            annual_dividend: None,
            rate_type: None,
            rate_value: None,
            version: None,
            max_dte: None,
            strike_range: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch second-order Greeks history (intraday, sampled by interval).
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration. 
    /// - Calculated using the option and underlying midpoint price. If an interval size is specified (*highly recommended*), the option quote used in the calculation follows the same rules as the quote endpoint. 
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, interval=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_greeks_second_order(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<GreeksSecondOrderTickList>> {
        let mut request = self.client.market_data().option_history_greeks_second_order(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = interval {
            request = request.interval(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = annual_dividend {
            request = request.annual_dividend(value);
        }
        if let Some(value) = rate_type {
            request = request.rate_type(value.as_str());
        }
        if let Some(value) = rate_value {
            request = request.rate_value(value);
        }
        if let Some(value) = version {
            request = request.version(value.as_str());
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        greeks_second_order_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch second-order Greeks history (intraday, sampled by interval).
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration. 
    /// - Calculated using the option and underlying midpoint price. If an interval size is specified (*highly recommended*), the option quote used in the calculation follows the same rules as the quote endpoint. 
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, interval=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_greeks_second_order_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_second_order(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = annual_dividend {
                request = request.annual_dividend(value);
            }
            if let Some(value) = rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = rate_value {
                request = request.rate_value(value);
            }
            if let Some(value) = version {
                request = request.version(value.as_str());
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_second_order_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_greeks_second_order`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_greeks_second_order()` for the sync signature; `option_history_greeks_second_order_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_history_greeks_second_order_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionHistoryGreeksSecondOrderBuilder {
        OptionHistoryGreeksSecondOrderBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            date: None,
            interval: None,
            start_time: None,
            end_time: None,
            annual_dividend: None,
            rate_type: None,
            rate_value: None,
            version: None,
            strike_range: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch second-order Greeks on each trade for an option contract.
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration.
    /// - Calculates greeks for every trade reported by OPRA.
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_trade_greeks_second_order(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<TradeGreeksSecondOrderTickList>> {
        let mut request = self.client.market_data().option_history_trade_greeks_second_order(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = annual_dividend {
            request = request.annual_dividend(value);
        }
        if let Some(value) = rate_type {
            request = request.rate_type(value.as_str());
        }
        if let Some(value) = rate_value {
            request = request.rate_value(value);
        }
        if let Some(value) = version {
            request = request.version(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        trade_greeks_second_order_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch second-order Greeks on each trade for an option contract.
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration.
    /// - Calculates greeks for every trade reported by OPRA.
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_trade_greeks_second_order_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_second_order(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = annual_dividend {
                request = request.annual_dividend(value);
            }
            if let Some(value) = rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = rate_value {
                request = request.rate_value(value);
            }
            if let Some(value) = version {
                request = request.version(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_greeks_second_order_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_trade_greeks_second_order`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_trade_greeks_second_order()` for the sync signature; `option_history_trade_greeks_second_order_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_history_trade_greeks_second_order_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionHistoryTradeGreeksSecondOrderBuilder {
        OptionHistoryTradeGreeksSecondOrderBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            date: None,
            start_time: None,
            end_time: None,
            annual_dividend: None,
            rate_type: None,
            rate_value: None,
            version: None,
            max_dte: None,
            strike_range: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch third-order Greeks history (intraday, sampled by interval).
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration. 
    /// - Calculated using the option and underlying midpoint price. If an interval size is specified (*highly recommended*), the option quote used in the calculation follows the same rules as the quote endpoint. 
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, interval=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_greeks_third_order(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<GreeksThirdOrderTickList>> {
        let mut request = self.client.market_data().option_history_greeks_third_order(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = interval {
            request = request.interval(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = annual_dividend {
            request = request.annual_dividend(value);
        }
        if let Some(value) = rate_type {
            request = request.rate_type(value.as_str());
        }
        if let Some(value) = rate_value {
            request = request.rate_value(value);
        }
        if let Some(value) = version {
            request = request.version(value.as_str());
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        greeks_third_order_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch third-order Greeks history (intraday, sampled by interval).
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration. 
    /// - Calculated using the option and underlying midpoint price. If an interval size is specified (*highly recommended*), the option quote used in the calculation follows the same rules as the quote endpoint. 
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, interval=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_greeks_third_order_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_third_order(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = annual_dividend {
                request = request.annual_dividend(value);
            }
            if let Some(value) = rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = rate_value {
                request = request.rate_value(value);
            }
            if let Some(value) = version {
                request = request.version(value.as_str());
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| greeks_third_order_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_greeks_third_order`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_greeks_third_order()` for the sync signature; `option_history_greeks_third_order_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_history_greeks_third_order_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionHistoryGreeksThirdOrderBuilder {
        OptionHistoryGreeksThirdOrderBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            date: None,
            interval: None,
            start_time: None,
            end_time: None,
            annual_dividend: None,
            rate_type: None,
            rate_value: None,
            version: None,
            strike_range: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch third-order Greeks on each trade for an option contract.
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration.
    /// - Calculates greeks for every trade reported by OPRA.
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_trade_greeks_third_order(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<TradeGreeksThirdOrderTickList>> {
        let mut request = self.client.market_data().option_history_trade_greeks_third_order(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = annual_dividend {
            request = request.annual_dividend(value);
        }
        if let Some(value) = rate_type {
            request = request.rate_type(value.as_str());
        }
        if let Some(value) = rate_value {
            request = request.rate_value(value);
        }
        if let Some(value) = version {
            request = request.version(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        trade_greeks_third_order_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch third-order Greeks on each trade for an option contract.
    ///
    /// - Returns the data for all contracts that share the same provided symbol and expiration.
    /// - Calculates greeks for every trade reported by OPRA.
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_trade_greeks_third_order_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_third_order(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = annual_dividend {
                request = request.annual_dividend(value);
            }
            if let Some(value) = rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = rate_value {
                request = request.rate_value(value);
            }
            if let Some(value) = version {
                request = request.version(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_greeks_third_order_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_trade_greeks_third_order`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_trade_greeks_third_order()` for the sync signature; `option_history_trade_greeks_third_order_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_history_trade_greeks_third_order_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionHistoryTradeGreeksThirdOrderBuilder {
        OptionHistoryTradeGreeksThirdOrderBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            date: None,
            start_time: None,
            end_time: None,
            annual_dividend: None,
            rate_type: None,
            rate_value: None,
            version: None,
            max_dte: None,
            strike_range: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch implied volatility history (intraday, sampled by interval).
    ///
    /// - Returns implied volatilies calculated using the national best bid, mid, and ask price of the option respectively. 
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, interval=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_greeks_implied_volatility(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<IvTickList>> {
        let mut request = self.client.market_data().option_history_greeks_implied_volatility(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = interval {
            request = request.interval(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = annual_dividend {
            request = request.annual_dividend(value);
        }
        if let Some(value) = rate_type {
            request = request.rate_type(value.as_str());
        }
        if let Some(value) = rate_value {
            request = request.rate_value(value);
        }
        if let Some(value) = version {
            request = request.version(value.as_str());
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        iv_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch implied volatility history (intraday, sampled by interval).
    ///
    /// - Returns implied volatilies calculated using the national best bid, mid, and ask price of the option respectively. 
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, interval=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_greeks_implied_volatility_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_greeks_implied_volatility(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = annual_dividend {
                request = request.annual_dividend(value);
            }
            if let Some(value) = rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = rate_value {
                request = request.rate_value(value);
            }
            if let Some(value) = version {
                request = request.version(value.as_str());
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| iv_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_greeks_implied_volatility`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_greeks_implied_volatility()` for the sync signature; `option_history_greeks_implied_volatility_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_history_greeks_implied_volatility_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionHistoryGreeksImpliedVolatilityBuilder {
        OptionHistoryGreeksImpliedVolatilityBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            date: None,
            interval: None,
            start_time: None,
            end_time: None,
            annual_dividend: None,
            rate_type: None,
            rate_value: None,
            version: None,
            strike_range: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch implied volatility on each trade for an option contract.
    ///
    /// - Returns implied volatilies calculated using the trade reported by OPRA. 
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_trade_greeks_implied_volatility(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<TradeGreeksImpliedVolatilityTickList>> {
        let mut request = self.client.market_data().option_history_trade_greeks_implied_volatility(symbol.as_str(), expiration.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = annual_dividend {
            request = request.annual_dividend(value);
        }
        if let Some(value) = rate_type {
            request = request.rate_type(value.as_str());
        }
        if let Some(value) = rate_value {
            request = request.rate_value(value);
        }
        if let Some(value) = version {
            request = request.version(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        trade_greeks_implied_volatility_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch implied volatility on each trade for an option contract.
    ///
    /// - Returns implied volatilies calculated using the trade reported by OPRA. 
    /// - The underlying price represents whatever the last underlying price was at the ``timestamp`` field. You can read more about how Theta Data calculates greeks [here](/articles/option-greeks).
    /// - Multi-day requests are limited to 1 month of data, and must specify an expiration.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    /// - `rate_type`: `"sofr"`
    /// - `version`: `"latest"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, *, strike=None, right=None, date=None, start_time=None, end_time=None, annual_dividend=None, rate_type=None, rate_value=None, version=None, max_dte=None, strike_range=None, start_date=None, end_date=None, timeout_ms=None))]
    fn option_history_trade_greeks_implied_volatility_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        date: Option<PyDateArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        annual_dividend: Option<f64>,
        rate_type: Option<PyStringArg>,
        rate_value: Option<f64>,
        version: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_history_trade_greeks_implied_volatility(symbol.as_str(), expiration.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = annual_dividend {
                request = request.annual_dividend(value);
            }
            if let Some(value) = rate_type {
                request = request.rate_type(value.as_str());
            }
            if let Some(value) = rate_value {
                request = request.rate_value(value);
            }
            if let Some(value) = version {
                request = request.version(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_greeks_implied_volatility_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_history_trade_greeks_implied_volatility`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_history_trade_greeks_implied_volatility()` for the sync signature; `option_history_trade_greeks_implied_volatility_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration))]
    fn option_history_trade_greeks_implied_volatility_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
    ) -> OptionHistoryTradeGreeksImpliedVolatilityBuilder {
        OptionHistoryTradeGreeksImpliedVolatilityBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            strike: None,
            right: None,
            date: None,
            start_time: None,
            end_time: None,
            annual_dividend: None,
            rate_type: None,
            rate_value: None,
            version: None,
            max_dte: None,
            strike_range: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch the trade at a specific time of day across a date range for an option.
    ///
    /// - Returns the last trade reported by OPRA at a specified millisecond of the day.
    /// - Trade condition mappings can be found here.
    /// - Extended trade conditions are not reported by OPRA for options, so they can be ignored.
    /// - The ``time_of_day``parameter represents the 00:00:00.000 ET that the trade should be provided for.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    #[pyo3(signature = (symbol, expiration, start_date, end_date, time_of_day, *, strike=None, right=None, max_dte=None, strike_range=None, timeout_ms=None))]
    fn option_at_time_trade(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        time_of_day: PyTimeArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<TradeTickList>> {
        let mut request = self.client.market_data().option_at_time_trade(symbol.as_str(), expiration.as_str(), start_date.as_str(), end_date.as_str(), time_of_day.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        trade_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch the trade at a specific time of day across a date range for an option.
    ///
    /// - Returns the last trade reported by OPRA at a specified millisecond of the day.
    /// - Trade condition mappings can be found here.
    /// - Extended trade conditions are not reported by OPRA for options, so they can be ignored.
    /// - The ``time_of_day``parameter represents the 00:00:00.000 ET that the trade should be provided for.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, start_date, end_date, time_of_day, *, strike=None, right=None, max_dte=None, strike_range=None, timeout_ms=None))]
    fn option_at_time_trade_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        time_of_day: PyTimeArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_at_time_trade(symbol.as_str(), expiration.as_str(), start_date.as_str(), end_date.as_str(), time_of_day.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| trade_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_at_time_trade`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_at_time_trade()` for the sync signature; `option_at_time_trade_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration, start_date, end_date, time_of_day))]
    fn option_at_time_trade_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        time_of_day: PyTimeArg,
    ) -> OptionAtTimeTradeBuilder {
        OptionAtTimeTradeBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            start_date: start_date.into_string(),
            end_date: end_date.into_string(),
            time_of_day: time_of_day.into_string(),
            strike: None,
            right: None,
            max_dte: None,
            strike_range: None,
            timeout_ms: None,
        }
    }

    /// Fetch the quote at a specific time of day across a date range for an option.
    ///
    /// - Returns the last NBBO quote reported by OPRA at a specified millisecond of the day.
    /// - The ``time_of_day``parameter represents the 00:00:00.000 ET that the quote should be provided for.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    #[pyo3(signature = (symbol, expiration, start_date, end_date, time_of_day, *, strike=None, right=None, max_dte=None, strike_range=None, timeout_ms=None))]
    fn option_at_time_quote(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        time_of_day: PyTimeArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<QuoteTickList>> {
        let mut request = self.client.market_data().option_at_time_quote(symbol.as_str(), expiration.as_str(), start_date.as_str(), end_date.as_str(), time_of_day.as_str());
        if let Some(value) = strike {
            request = request.strike(value.as_str());
        }
        if let Some(value) = right {
            request = request.right(value.as_str());
        }
        if let Some(value) = max_dte {
            request = request.max_dte(value);
        }
        if let Some(value) = strike_range {
            request = request.strike_range(value);
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        quote_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch the quote at a specific time of day across a date range for an option.
    ///
    /// - Returns the last NBBO quote reported by OPRA at a specified millisecond of the day.
    /// - The ``time_of_day``parameter represents the 00:00:00.000 ET that the quote should be provided for.
    ///
    /// Defaults (upstream):
    /// - `strike`: `"*"`
    /// - `right`: `"both"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, expiration, start_date, end_date, time_of_day, *, strike=None, right=None, max_dte=None, strike_range=None, timeout_ms=None))]
    fn option_at_time_quote_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        expiration: PyDateArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        time_of_day: PyTimeArg,
        strike: Option<PyStringArg>,
        right: Option<PyStringArg>,
        max_dte: Option<i32>,
        strike_range: Option<i32>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().option_at_time_quote(symbol.as_str(), expiration.as_str(), start_date.as_str(), end_date.as_str(), time_of_day.as_str());
            if let Some(value) = strike {
                request = request.strike(value.as_str());
            }
            if let Some(value) = right {
                request = request.right(value.as_str());
            }
            if let Some(value) = max_dte {
                request = request.max_dte(value);
            }
            if let Some(value) = strike_range {
                request = request.strike_range(value);
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| quote_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `option_at_time_quote`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `option_at_time_quote()` for the sync signature; `option_at_time_quote_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, expiration, start_date, end_date, time_of_day))]
    fn option_at_time_quote_builder(
        &self,
        symbol: PyStringArg,
        expiration: PyDateArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        time_of_day: PyTimeArg,
    ) -> OptionAtTimeQuoteBuilder {
        OptionAtTimeQuoteBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            expiration: expiration.into_string(),
            start_date: start_date.into_string(),
            end_date: end_date.into_string(),
            time_of_day: time_of_day.into_string(),
            strike: None,
            right: None,
            max_dte: None,
            strike_range: None,
            timeout_ms: None,
        }
    }

    /// List all available index symbols.
    ///
    /// A symbol can be defined as a unique identifier for a stock / underlying asset. Common terms also include: root, ticker, and underlying. This endpoint returns all traded symbols for options. This endpoint is updated overnight.
    #[pyo3(signature = (*, timeout_ms=None))]
    fn index_list_symbols(
        &self,
        py: Python<'_>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<StringList>> {
        let values: Vec<String> = run_blocking(py, async move {
            let call = self.client.market_data().index_list_symbols();
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        })?;
        strings_to_string_list(py, values, "symbol")
    }

    /// List all available index symbols.
    ///
    /// A symbol can be defined as a unique identifier for a stock / underlying asset. Common terms also include: root, ticker, and underlying. This endpoint returns all traded symbols for options. This endpoint is updated overnight.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (*, timeout_ms=None))]
    fn index_list_symbols_async<'py>(
        &self,
        py: Python<'py>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let call = client.market_data().index_list_symbols();
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        }, |py, values| strings_to_string_list(py, values, "symbol").map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `index_list_symbols`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `index_list_symbols()` for the sync signature; `index_list_symbols_async()` for the awaitable companion.
    #[pyo3(signature = ())]
    fn index_list_symbols_builder(
        &self,
    ) -> IndexListSymbolsBuilder {
        IndexListSymbolsBuilder {
            client: self.client.clone(),
            timeout_ms: None,
        }
    }

    /// List available dates for an index symbol.
    ///
    /// Lists all dates of data that are available for a index with a given request type and symbol. This endpoint is updated overnight.
    #[pyo3(signature = (symbol, *, timeout_ms=None))]
    fn index_list_dates(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<StringList>> {
        let values: Vec<String> = run_blocking(py, async move {
            let call = self.client.market_data().index_list_dates(symbol.as_str());
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        })?;
        strings_to_string_list(py, values, "date")
    }

    /// List available dates for an index symbol.
    ///
    /// Lists all dates of data that are available for a index with a given request type and symbol. This endpoint is updated overnight.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, *, timeout_ms=None))]
    fn index_list_dates_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let call = client.market_data().index_list_dates(symbol.as_str());
            if let Some(ms) = timeout_ms {
                match tokio::time::timeout(std::time::Duration::from_millis(ms), call).await {
                    Ok(inner) => inner,
                    Err(_) => Err(thetadatadx::Error::Timeout { duration_ms: ms }),
                }
            } else {
                call.await
            }
        }, |py, values| strings_to_string_list(py, values, "date").map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `index_list_dates`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `index_list_dates()` for the sync signature; `index_list_dates_async()` for the awaitable companion.
    #[pyo3(signature = (symbol))]
    fn index_list_dates_builder(
        &self,
        symbol: PyStringArg,
    ) -> IndexListDatesBuilder {
        IndexListDatesBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            timeout_ms: None,
        }
    }

    /// Get the latest OHLC snapshot for one or more indices.
    ///
    /// - Retrieves the real-time current day OHLC.
    /// - Exchanges typically generate a price report every second for popular indices like SPX.
    #[pyo3(signature = (symbols, *, min_time=None, timeout_ms=None))]
    fn index_snapshot_ohlc(
        &self,
        py: Python<'_>,
        symbols: PySymbols,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<OhlcTickList>> {
        let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
        let mut request = self.client.market_data().index_snapshot_ohlc(&refs);
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        ohlc_ticks_to_pyclass_list(py, ticks)
    }

    /// Get the latest OHLC snapshot for one or more indices.
    ///
    /// - Retrieves the real-time current day OHLC.
    /// - Exchanges typically generate a price report every second for popular indices like SPX.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbols, *, min_time=None, timeout_ms=None))]
    fn index_snapshot_ohlc_async<'py>(
        &self,
        py: Python<'py>,
        symbols: PySymbols,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().index_snapshot_ohlc(&refs);
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| ohlc_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `index_snapshot_ohlc`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `index_snapshot_ohlc()` for the sync signature; `index_snapshot_ohlc_async()` for the awaitable companion.
    #[pyo3(signature = (symbols))]
    fn index_snapshot_ohlc_builder(
        &self,
        symbols: PySymbols,
    ) -> IndexSnapshotOhlcBuilder {
        IndexSnapshotOhlcBuilder {
            client: self.client.clone(),
            symbols: symbols.into_vec(),
            min_time: None,
            timeout_ms: None,
        }
    }

    /// Get the latest price snapshot for one or more indices.
    ///
    /// - Retrieves a real-time last index price.
    /// - Exchanges typically generate a price report every second for popular indices like SPX.
    #[pyo3(signature = (symbols, *, min_time=None, timeout_ms=None))]
    fn index_snapshot_price(
        &self,
        py: Python<'_>,
        symbols: PySymbols,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<PriceTickList>> {
        let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
        let mut request = self.client.market_data().index_snapshot_price(&refs);
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        price_ticks_to_pyclass_list(py, ticks)
    }

    /// Get the latest price snapshot for one or more indices.
    ///
    /// - Retrieves a real-time last index price.
    /// - Exchanges typically generate a price report every second for popular indices like SPX.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbols, *, min_time=None, timeout_ms=None))]
    fn index_snapshot_price_async<'py>(
        &self,
        py: Python<'py>,
        symbols: PySymbols,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().index_snapshot_price(&refs);
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| price_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `index_snapshot_price`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `index_snapshot_price()` for the sync signature; `index_snapshot_price_async()` for the awaitable companion.
    #[pyo3(signature = (symbols))]
    fn index_snapshot_price_builder(
        &self,
        symbols: PySymbols,
    ) -> IndexSnapshotPriceBuilder {
        IndexSnapshotPriceBuilder {
            client: self.client.clone(),
            symbols: symbols.into_vec(),
            min_time: None,
            timeout_ms: None,
        }
    }

    /// Get the latest market value snapshot for one or more indices.
    ///
    /// - Retrieves a real-time last index market value.
    /// - Exchanges typically generate a price report every second for popular indices like SPX.
    #[pyo3(signature = (symbols, *, min_time=None, timeout_ms=None))]
    fn index_snapshot_market_value(
        &self,
        py: Python<'_>,
        symbols: PySymbols,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<MarketValueTickList>> {
        let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
        let mut request = self.client.market_data().index_snapshot_market_value(&refs);
        if let Some(value) = min_time {
            request = request.min_time(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        market_value_ticks_to_pyclass_list(py, ticks)
    }

    /// Get the latest market value snapshot for one or more indices.
    ///
    /// - Retrieves a real-time last index market value.
    /// - Exchanges typically generate a price report every second for popular indices like SPX.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbols, *, min_time=None, timeout_ms=None))]
    fn index_snapshot_market_value_async<'py>(
        &self,
        py: Python<'py>,
        symbols: PySymbols,
        min_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let refs: Vec<&str> = symbols.iter().map(|s| s.as_str()).collect();
            let mut request = client.market_data().index_snapshot_market_value(&refs);
            if let Some(value) = min_time {
                request = request.min_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| market_value_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `index_snapshot_market_value`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `index_snapshot_market_value()` for the sync signature; `index_snapshot_market_value_async()` for the awaitable companion.
    #[pyo3(signature = (symbols))]
    fn index_snapshot_market_value_builder(
        &self,
        symbols: PySymbols,
    ) -> IndexSnapshotMarketValueBuilder {
        IndexSnapshotMarketValueBuilder {
            client: self.client.clone(),
            symbols: symbols.into_vec(),
            min_time: None,
            timeout_ms: None,
        }
    }

    /// Fetch end-of-day index data for a date range.
    ///
    /// - Since the indices feeds do not provide a national EOD report, Theta Data generates a national EOD report at 17:15 each day.
    #[pyo3(signature = (symbol, start_date, end_date, *, timeout_ms=None))]
    fn index_history_eod(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<EodTickList>> {
        let mut request = self.client.market_data().index_history_eod(symbol.as_str(), start_date.as_str(), end_date.as_str());
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        eod_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch end-of-day index data for a date range.
    ///
    /// - Since the indices feeds do not provide a national EOD report, Theta Data generates a national EOD report at 17:15 each day.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, start_date, end_date, *, timeout_ms=None))]
    fn index_history_eod_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().index_history_eod(symbol.as_str(), start_date.as_str(), end_date.as_str());
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| eod_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `index_history_eod`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `index_history_eod()` for the sync signature; `index_history_eod_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, start_date, end_date))]
    fn index_history_eod_builder(
        &self,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
    ) -> IndexHistoryEodBuilder {
        IndexHistoryEodBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            start_date: start_date.into_string(),
            end_date: end_date.into_string(),
            timeout_ms: None,
        }
    }

    /// Fetch intraday OHLC bars for an index.
    ///
    /// - Aggregated OHLC bars that use SIP rules for each bar.
    /// - Time timestamp of the bar represents the opening time of the bar. For a trade to be part of the bar:  ``bar timestamp`` <= ``trade time`` < ``bar timestamp + interval``.
    /// - Exchanges typically generate a price report every second for popular indices like SPX.
    ///
    /// Defaults (upstream):
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    #[pyo3(signature = (symbol, start_date, end_date, *, interval=None, start_time=None, end_time=None, timeout_ms=None))]
    fn index_history_ohlc(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<OhlcTickList>> {
        let mut request = self.client.market_data().index_history_ohlc(symbol.as_str(), start_date.as_str(), end_date.as_str());
        if let Some(value) = interval {
            request = request.interval(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        ohlc_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch intraday OHLC bars for an index.
    ///
    /// - Aggregated OHLC bars that use SIP rules for each bar.
    /// - Time timestamp of the bar represents the opening time of the bar. For a trade to be part of the bar:  ``bar timestamp`` <= ``trade time`` < ``bar timestamp + interval``.
    /// - Exchanges typically generate a price report every second for popular indices like SPX.
    ///
    /// Defaults (upstream):
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, start_date, end_date, *, interval=None, start_time=None, end_time=None, timeout_ms=None))]
    fn index_history_ohlc_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().index_history_ohlc(symbol.as_str(), start_date.as_str(), end_date.as_str());
            if let Some(value) = interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| ohlc_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `index_history_ohlc`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `index_history_ohlc()` for the sync signature; `index_history_ohlc_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, start_date, end_date))]
    fn index_history_ohlc_builder(
        &self,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
    ) -> IndexHistoryOhlcBuilder {
        IndexHistoryOhlcBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            start_date: start_date.into_string(),
            end_date: end_date.into_string(),
            interval: None,
            start_time: None,
            end_time: None,
            timeout_ms: None,
        }
    }

    /// Fetch intraday price history for an index.
    ///
    /// - Retrieves historical indices price reports. Exchanges typically generate a price report every second for popular indices like SPX.
    /// - When the ``interval`` parameter is specified, the returned data represents the price at the exact time of each timestamp. If the timestamp in the response is 10:30:00, the price field represents the price at that exact time of the day.
    /// - A price update from the exchange is omitted if the price remained the same from the previous update.
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    #[pyo3(signature = (symbol, *, date=None, interval=None, start_time=None, end_time=None, start_date=None, end_date=None, timeout_ms=None))]
    fn index_history_price(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<PriceTickList>> {
        let mut request = self.client.market_data().index_history_price(symbol.as_str());
        if let Some(value) = date {
            request = request.date(value.as_str());
        }
        if let Some(value) = interval {
            request = request.interval(value.as_str());
        }
        if let Some(value) = start_time {
            request = request.start_time(value.as_str());
        }
        if let Some(value) = end_time {
            request = request.end_time(value.as_str());
        }
        if let Some(value) = start_date {
            request = request.start_date(value.as_str());
        }
        if let Some(value) = end_date {
            request = request.end_date(value.as_str());
        }
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        price_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch intraday price history for an index.
    ///
    /// - Retrieves historical indices price reports. Exchanges typically generate a price report every second for popular indices like SPX.
    /// - When the ``interval`` parameter is specified, the returned data represents the price at the exact time of each timestamp. If the timestamp in the response is 10:30:00, the price field represents the price at that exact time of the day.
    /// - A price update from the exchange is omitted if the price remained the same from the previous update.
    /// - Multi-day requests are limited to 1 month of data.
    ///
    /// Defaults (upstream):
    /// - `interval`: `"1s"`
    /// - `start_time`: `"09:30:00"`
    /// - `end_time`: `"16:00:00"`
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, *, date=None, interval=None, start_time=None, end_time=None, start_date=None, end_date=None, timeout_ms=None))]
    fn index_history_price_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        date: Option<PyDateArg>,
        interval: Option<PyStringArg>,
        start_time: Option<PyTimeArg>,
        end_time: Option<PyTimeArg>,
        start_date: Option<PyDateArg>,
        end_date: Option<PyDateArg>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().index_history_price(symbol.as_str());
            if let Some(value) = date {
                request = request.date(value.as_str());
            }
            if let Some(value) = interval {
                request = request.interval(value.as_str());
            }
            if let Some(value) = start_time {
                request = request.start_time(value.as_str());
            }
            if let Some(value) = end_time {
                request = request.end_time(value.as_str());
            }
            if let Some(value) = start_date {
                request = request.start_date(value.as_str());
            }
            if let Some(value) = end_date {
                request = request.end_date(value.as_str());
            }
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| price_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `index_history_price`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `index_history_price()` for the sync signature; `index_history_price_async()` for the awaitable companion.
    #[pyo3(signature = (symbol))]
    fn index_history_price_builder(
        &self,
        symbol: PyStringArg,
    ) -> IndexHistoryPriceBuilder {
        IndexHistoryPriceBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            date: None,
            interval: None,
            start_time: None,
            end_time: None,
            start_date: None,
            end_date: None,
            timeout_ms: None,
        }
    }

    /// Fetch the index price at a specific time of day across a date range.
    ///
    /// - Retrieves historical indices price reports. Exchanges typically generate a price report every second for popular indices like SPX.
    /// - The ``time_of_day`` parameter represents the 00:00:00.000 ET that the price should be provided for.
    #[pyo3(signature = (symbol, start_date, end_date, time_of_day, *, timeout_ms=None))]
    fn index_at_time_price(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        time_of_day: PyTimeArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<IndexPriceAtTimeTickList>> {
        let mut request = self.client.market_data().index_at_time_price(symbol.as_str(), start_date.as_str(), end_date.as_str(), time_of_day.as_str());
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        index_price_at_time_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch the index price at a specific time of day across a date range.
    ///
    /// - Retrieves historical indices price reports. Exchanges typically generate a price report every second for popular indices like SPX.
    /// - The ``time_of_day`` parameter represents the 00:00:00.000 ET that the price should be provided for.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, start_date, end_date, time_of_day, *, timeout_ms=None))]
    fn index_at_time_price_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        time_of_day: PyTimeArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().index_at_time_price(symbol.as_str(), start_date.as_str(), end_date.as_str(), time_of_day.as_str());
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| index_price_at_time_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `index_at_time_price`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `index_at_time_price()` for the sync signature; `index_at_time_price_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, start_date, end_date, time_of_day))]
    fn index_at_time_price_builder(
        &self,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        time_of_day: PyTimeArg,
    ) -> IndexAtTimePriceBuilder {
        IndexAtTimePriceBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            start_date: start_date.into_string(),
            end_date: end_date.into_string(),
            time_of_day: time_of_day.into_string(),
            timeout_ms: None,
        }
    }

    /// Check whether the market is open today.
    ///
    /// - Retrieves current day equity market schedule
    /// - *On days when the market closes early at 1:00 PM ET; eligible options will trade until 1:15 PM.
    /// - **Some NYSE exchanges will continue late trading until 5:00 PM ET on early close days.
    #[pyo3(signature = (*, timeout_ms=None))]
    fn calendar_open_today(
        &self,
        py: Python<'_>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<pyo3::types::PyList>> {
        let mut request = self.client.market_data().calendar_open_today();
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        calendar_days_vec_to_pylist(py, ticks)
    }

    /// Check whether the market is open today.
    ///
    /// - Retrieves current day equity market schedule
    /// - *On days when the market closes early at 1:00 PM ET; eligible options will trade until 1:15 PM.
    /// - **Some NYSE exchanges will continue late trading until 5:00 PM ET on early close days.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (*, timeout_ms=None))]
    fn calendar_open_today_async<'py>(
        &self,
        py: Python<'py>,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().calendar_open_today();
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| calendar_days_vec_to_pylist(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `calendar_open_today`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `calendar_open_today()` for the sync signature; `calendar_open_today_async()` for the awaitable companion.
    #[pyo3(signature = ())]
    fn calendar_open_today_builder(
        &self,
    ) -> CalendarOpenTodayBuilder {
        CalendarOpenTodayBuilder {
            client: self.client.clone(),
            timeout_ms: None,
        }
    }

    /// Get calendar information for a specific date.
    ///
    /// - Retrieves equity market schedule for a given date
    /// - Note: Holiday data is available 01/01/2012 through the end of the calendar year that immediately follows the current year
    /// - *On days when the market closes early at 1:00 PM ET; eligible options will trade until 1:15 PM.
    /// - **Some NYSE exchanges will continue late trading until 5:00 PM ET on early close days.
    #[pyo3(signature = (date, *, timeout_ms=None))]
    fn calendar_on_date(
        &self,
        py: Python<'_>,
        date: PyDateArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<pyo3::types::PyList>> {
        let mut request = self.client.market_data().calendar_on_date(date.as_str());
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        calendar_days_vec_to_pylist(py, ticks)
    }

    /// Get calendar information for a specific date.
    ///
    /// - Retrieves equity market schedule for a given date
    /// - Note: Holiday data is available 01/01/2012 through the end of the calendar year that immediately follows the current year
    /// - *On days when the market closes early at 1:00 PM ET; eligible options will trade until 1:15 PM.
    /// - **Some NYSE exchanges will continue late trading until 5:00 PM ET on early close days.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (date, *, timeout_ms=None))]
    fn calendar_on_date_async<'py>(
        &self,
        py: Python<'py>,
        date: PyDateArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().calendar_on_date(date.as_str());
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| calendar_days_vec_to_pylist(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `calendar_on_date`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `calendar_on_date()` for the sync signature; `calendar_on_date_async()` for the awaitable companion.
    #[pyo3(signature = (date))]
    fn calendar_on_date_builder(
        &self,
        date: PyDateArg,
    ) -> CalendarOnDateBuilder {
        CalendarOnDateBuilder {
            client: self.client.clone(),
            date: date.into_string(),
            timeout_ms: None,
        }
    }

    /// Get equity market holidays and early-close days for a year (vendor `year_holidays` endpoint — only non-standard days, not every trading day).
    ///
    /// - Retrieves equity market holidays for a given year
    /// - Note: Holiday data is available 01/01/2012 through the end of the calendar year that immediately follows the current year
    /// - *On days when the market closes early at 1:00 PM ET; eligible options will trade until 1:15 PM.
    /// - **Some NYSE exchanges will continue late trading until 5:00 PM ET on early close days.
    #[pyo3(signature = (year, *, timeout_ms=None))]
    fn calendar_year(
        &self,
        py: Python<'_>,
        year: PyStringArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<pyo3::types::PyList>> {
        let mut request = self.client.market_data().calendar_year(year.as_str());
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        calendar_days_vec_to_pylist(py, ticks)
    }

    /// Get equity market holidays and early-close days for a year (vendor `year_holidays` endpoint — only non-standard days, not every trading day).
    ///
    /// - Retrieves equity market holidays for a given year
    /// - Note: Holiday data is available 01/01/2012 through the end of the calendar year that immediately follows the current year
    /// - *On days when the market closes early at 1:00 PM ET; eligible options will trade until 1:15 PM.
    /// - **Some NYSE exchanges will continue late trading until 5:00 PM ET on early close days.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (year, *, timeout_ms=None))]
    fn calendar_year_async<'py>(
        &self,
        py: Python<'py>,
        year: PyStringArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().calendar_year(year.as_str());
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| calendar_days_vec_to_pylist(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `calendar_year`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `calendar_year()` for the sync signature; `calendar_year_async()` for the awaitable companion.
    #[pyo3(signature = (year))]
    fn calendar_year_builder(
        &self,
        year: PyStringArg,
    ) -> CalendarYearBuilder {
        CalendarYearBuilder {
            client: self.client.clone(),
            year: year.into_string(),
            timeout_ms: None,
        }
    }

    /// Fetch end-of-day interest rate history.
    ///
    /// - Returns the interest rate reported. Depending on the rate, reports can occur in the morning or the afternoon.
    /// - Valid `symbol` values per upstream `RateType` enum:
    ///   `SOFR`, `TREASURY_M1`, `TREASURY_M3`, `TREASURY_M6`,
    ///   `TREASURY_Y1`, `TREASURY_Y2`, `TREASURY_Y3`, `TREASURY_Y5`,
    ///   `TREASURY_Y7`, `TREASURY_Y10`, `TREASURY_Y20`, `TREASURY_Y30`.
    #[pyo3(signature = (symbol, start_date, end_date, *, timeout_ms=None))]
    fn interest_rate_history_eod(
        &self,
        py: Python<'_>,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Py<InterestRateTickList>> {
        let mut request = self.client.market_data().interest_rate_history_eod(symbol.as_str(), start_date.as_str(), end_date.as_str());
        if let Some(ms) = timeout_ms {
            request = request.with_deadline(std::time::Duration::from_millis(ms));
        }
        let ticks = run_blocking(py, async move { request.await })?;
        interest_rate_ticks_to_pyclass_list(py, ticks)
    }

    /// Fetch end-of-day interest rate history.
    ///
    /// - Returns the interest rate reported. Depending on the rate, reports can occur in the morning or the afternoon.
    /// - Valid `symbol` values per upstream `RateType` enum:
    ///   `SOFR`, `TREASURY_M1`, `TREASURY_M3`, `TREASURY_M6`,
    ///   `TREASURY_Y1`, `TREASURY_Y2`, `TREASURY_Y3`, `TREASURY_Y5`,
    ///   `TREASURY_Y7`, `TREASURY_Y10`, `TREASURY_Y20`, `TREASURY_Y30`.
    ///
    ///
    /// Async companion — returns an awaitable (`asyncio.Future`).
    /// Shares the same shared tokio runtime as the sync variant; no
    /// second runtime is created per call.
    #[pyo3(signature = (symbol, start_date, end_date, *, timeout_ms=None))]
    fn interest_rate_history_eod_async<'py>(
        &self,
        py: Python<'py>,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
        timeout_ms: Option<u64>,
    ) -> PyResult<Bound<'py, PyAny>> {
        let client = self.client.clone();
        spawn_awaitable(py, async move {
            let mut request = client.market_data().interest_rate_history_eod(symbol.as_str(), start_date.as_str(), end_date.as_str());
            if let Some(ms) = timeout_ms {
                request = request.with_deadline(std::time::Duration::from_millis(ms));
            }
            request.await
        }, |py, ticks| interest_rate_ticks_to_pyclass_list(py, ticks).map(|p| p.into_any()))
    }

    /// Create a fluent-builder for `interest_rate_history_eod`. Chain setters then call `.list()`
    /// (or `.list_async()`) to execute the request; the returned typed list
    /// wrapper exposes the chainable terminals `.to_list()` / `.to_arrow()`
    /// / `.to_pandas()` / `.to_polars()`.
    ///
    /// See `interest_rate_history_eod()` for the sync signature; `interest_rate_history_eod_async()` for the awaitable companion.
    #[pyo3(signature = (symbol, start_date, end_date))]
    fn interest_rate_history_eod_builder(
        &self,
        symbol: PyStringArg,
        start_date: PyDateArg,
        end_date: PyDateArg,
    ) -> InterestRateHistoryEodBuilder {
        InterestRateHistoryEodBuilder {
            client: self.client.clone(),
            symbol: symbol.into_string(),
            start_date: start_date.into_string(),
            end_date: end_date.into_string(),
            timeout_ms: None,
        }
    }

}
